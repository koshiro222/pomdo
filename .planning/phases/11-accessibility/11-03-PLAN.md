---
phase: 11-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/todos/TodoItem.tsx]
autonomous: true
requirements: [A11Y-04]

must_haves:
  truths:
    - "ユーザーは全てのアイコンボタンに適切なARIAラベルが付与されていることを確認できる"
    - "スクリーンリーダーでアイコンボタンの目的が読み上げられる"
  artifacts:
    - path: "src/components/todos/TodoItem.tsx"
      provides: "Todo削除ボタン"
      contains: 'aria-label="削除"'
  key_links:
    - from: "TodoItem.tsx"
      to: "スクリーンリーダー"
      via: "aria-label属性"
      pattern: "aria-label"
---

<objective>
主要なアイコンボタンに日本語ARIAラベルを付与し、スクリーンリーダー利用者がボタンの目的を理解できるようにする。

Purpose: テキストラベルのないアイコンボタンに対して、スクリーンリーダーで読み上げられるラベルを提供する。
Output: 更新されたTodoItem.tsx
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accessibility/11-CONTEXT.md
@.planning/phases/11-accessibility/11-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/bgm/BgmPlayer.tsx (既存のARIAラベルパターン):
```tsx
<motion.button
  {...tapAnimation}
  onClick={() => setIsExpanded(!isExpanded)}
  className="text-cf-subtext hover:text-cf-primary cursor-pointer transition-colors"
  aria-label={isExpanded ? '縮小' : 'プレイリスト'}
>
  <List className="w-5 h-5" />
</motion.button>
```

From src/components/timer/TimerControls.tsx (既存のARIAラベルパターン):
```tsx
<motion.button
  {...hoverAnimation}
  {...tapAnimation}
  onClick={onReset}
  className="size-14 rounded-full glass flex items-center justify-center text-cf-subtext hover:text-cf-text transition-colors hover:bg-white/10"
  aria-label="リセット"
>
  <RotateCcw className="text-3xl" />
</motion.button>
```

From src/components/bgm/TrackItem.tsx (既にARIAラベルあり):
```tsx
<motion.button
  {...tapAnimation}
  onClick={() => setShowEditDialog(true)}
  className="text-cf-subtext hover:text-cf-text"
  aria-label="編集"
>
  <Edit2 size={16} />
</motion.button>
<motion.button
  {...tapAnimation}
  onClick={() => setShowDeleteConfirm(true)}
  className="text-cf-subtext hover:text-cf-danger"
  aria-label="削除"
>
  <Trash2 size={16} />
</motion.button>
```

From src/components/todos/TodoItem.tsx (現在の実装 - ARIAラベルなし):
```tsx
// 削除ボタン（line 95-104）
<motion.button
  {...tapAnimation}
  onClick={(e) => {
    e.stopPropagation()
    onDelete(id)
  }}
  className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
>
  <Trash2 className="text-lg" />
</motion.button>
```

From CONTEXT.md (ユーザー決定):
- 日本語ラベル（「編集」「削除」「閉じる」「縮小」「プレイリスト」等）
- 各コンポーネントに直接記述（既存パターン：BgmPlayer, TimerControlsに準拠）
- 対象範囲: 主要なアイコンボタンのみ（TodoItem削除、TrackItem編集・削除等）
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TodoItem削除ボタンにARIAラベル付与 (A11Y-04)</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在の削除ボタン実装を確認）
    - src/components/bgm/BgmPlayer.tsx（ARIAラベルパターン参考）
    - src/components/timer/TimerControls.tsx（ARIAラベルパターン参考）
  </read_first>
  <action>
    src/components/todos/TodoItem.tsxの削除ボタン（Trash2）にaria-labelを追加する:

    修正箇所（line 95-104付近）:
    ```tsx
    // 修正前
    <motion.button
      {...tapAnimation}
      onClick={(e) => {
        e.stopPropagation()
        onDelete(id)
      }}
      className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
    >
      <Trash2 className="text-lg" />
    </motion.button>

    // 修正後
    <motion.button
      {...tapAnimation}
      onClick={(e) => {
        e.stopPropagation()
        onDelete(id)
      }}
      className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
      aria-label="削除"
    >
      <Trash2 className="text-lg" />
    </motion.button>
    ```

    注意点:
    - TrackItem.tsxの削除ボタンには既に `aria-label="削除"` が付与されているため修正不要
    - BgmPlayer.tsx、TimerControls.tsxにも既にARIAラベルが付与されているため修正不要
  </action>
  <verify>
    <automated>grep -q 'aria-label="削除"' src/components/todos/TodoItem.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TodoItem.tsxの削除ボタンに `aria-label="削除"` が存在する
  </acceptance_criteria>
  <done>
    - TodoItemの削除ボタンに日本語ARIAラベルが付与されている
    - スクリーンリーダーで「削除」と読み上げられる
  </done>
</task>

</tasks>

<verification>
## 検証手順

### 自動検証
```bash
# ARIAラベル付与の確認
grep -q 'aria-label="削除"' src/components/todos/TodoItem.tsx && echo "PASS" || echo "FAIL"
```

### 手動検証
1. **ARIAラベル正確性 (A11Y-04)**:
   - `npm run dev` で開発サーバーを起動
   - ブラウザで http://localhost:5173 を開く
   - 開発者ツールでTodoItemの削除ボタンを検査
   - `aria-label="削除"` 属性が存在することを確認
   - （オプション）macOS VoiceOverを起動し、削除ボタンをフォーカスして「削除」と読み上げられることを確認
</verification>

<success_criteria>
- [ ] TodoItem.tsxの削除ボタンに `aria-label="削除"` が付与されている
- [ ] 既存のARIAラベル（BgmPlayer、TimerControls、TrackItem）が維持されている
</success_criteria>

<output>
After completion, create `.planning/phases/11-accessibility/11-03-SUMMARY.md`
</output>
