import { describe, it } from 'vitest'

/**
 * Phase 16: TodoListカードヘッダー統一テスト
 *
 * 検証項目:
 * - HEADER-03: ヘッダーに「Tasks」テキストが表示される
 * - CheckSquareアイコンが削除されている
 * - ヘッダーテキストスタイルが統一されている
 * - バッジが右上に維持されている
 */

describe('TodoList Card Header', () => {
  it.todo('should display "Tasks" text in the header')
  it.todo('should not have CheckSquare icon')
  it.todo('should have unified header text style: text-xs uppercase tracking-widest font-bold text-cf-text')
  it.todo('should have remaining todos badge on the right')
  it.todo('should have flex items-center justify-between mb-4 layout')
})
