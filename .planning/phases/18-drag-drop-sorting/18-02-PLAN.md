---
phase: 18-drag-drop-sorting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/storage.ts, src/hooks/useTodos.ts, src/core/store/todos.ts]
autonomous: true
requirements: [DND-02]
user_setup: []

must_haves:
  truths:
    - "ゲストユーザーが並び替えたタスクの順序がlocalStorageに保存される"
    - "useTodos.reorderTodoがゲスト/ログイン双方で動作する"
  artifacts:
    - path: "src/lib/storage.ts"
      provides: "Todo型のorderフィールド、reorderメソッド"
      exports: ["Todo", "storage.reorder"]
    - path: "src/hooks/useTodos.ts"
      provides: "reorderTodoメソッド"
      exports: ["reorderTodo"]
  key_links:
    - from: "src/hooks/useTodos.ts reorderTodo"
      to: "src/lib/storage.ts storage.reorder"
      via: "ゲストモード時"
      pattern: "storage.reorder"
    - from: "src/hooks/useTodos.ts reorderTodo"
      to: "src/app/routers/todos.ts reorder"
      via: "ログイン時 tRPC mutation"
      pattern: "reorderMutation"
---

<objective>
ゲストユーザー用のorder永続化（localStorage）とuseTodosフックにreorderTodoメソッドを追加する。

Purpose: ゲストモードでのタスク並び替え順序を永続化し、useTodosフックでゲスト/ログイン双方のreorder操作を統一的に扱う。
Output: orderフィールド付きTodo型、reorderメソッド付きstorage、reorderTodoメソッド付きuseTodos
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-drag-drop-sorting/18-CONTEXT.md
@.planning/phases/18-drag-drop-sorting/18-RESEARCH.md

<interfaces>
<!-- 既存のTodo型定義 -->
From src/lib/storage.ts:
```typescript
export type Todo = {
  id: string
  title: string
  completed: boolean
  completedPomodoros?: number
  createdAt: string
  updatedAt: string
}
```

<!-- 既存のuseTodos構造 -->
From src/hooks/useTodos.ts:
```typescript
export function useTodos() {
  // ...
  return {
    todos,
    selectedTodoId,
    loading,
    error,
    addTodo,
    updateTodo,
    deleteTodo,
    setSelectedTodoId,
    incrementCompletedPomodoros,
    refetch,
    migrateToApi,
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: storage.tsにorderフィールドとreorderメソッドを追加</name>
  <files>src/lib/storage.ts</files>
  <read_first>
    - src/lib/storage.ts（現在のTodo型とstorageオブジェクトを確認）
  </read_first>
  <behavior>
    - Todo型にorder: numberフィールドが追加される
    - storage.reorder(id, newOrder)でlocalStorage内の配列を並び替える
    - 並び替え後、全タスクのorder値が再採番される
  </behavior>
  <action>
src/lib/storage.tsに以下の変更を加える:

1. Todo型にorderフィールドを追加:
```typescript
export type Todo = {
  id: string
  title: string
  completed: boolean
  completedPomodoros?: number
  order: number  // 追加
  createdAt: string
  updatedAt: string
}
```

2. addTodoメソッドでorder値を設定（最大order + 1）:
```typescript
addTodo(todo: NewTodo): Todo {
  const todos = this.getTodos()
  const now = new Date().toISOString()
  const maxOrder = todos.length > 0 ? Math.max(...todos.map(t => t.order ?? 0)) : -1
  const newTodo: Todo = {
    id: crypto.randomUUID(),
    ...todo,
    order: maxOrder + 1,  // 追加
    createdAt: now,
    updatedAt: now,
  }
  // ...
}
```

3. reorderメソッドを追加:
```typescript
reorder(id: string, newOrder: number): boolean {
  const todos = this.getTodos()
  const oldIndex = todos.findIndex(t => t.id === id)
  if (oldIndex === -1) return false

  const [moved] = todos.splice(oldIndex, 1)
  todos.splice(newOrder, 0, moved)

  // order値を再採番
  todos.forEach((t, i) => { t.order = i })
  saveTodos(todos)
  return true
},
```

4. getTodos内でorderフィールドがない古いデータにorderを付与:
```typescript
getTodos(): Todo[] {
  try {
    const data = localStorage.getItem(STORAGE_KEY)
    const todos = data ? JSON.parse(data) : []
    // 古いデータにorderを付与（createdAt順）
    return todos.map((t: Todo, i: number) => ({
      ...t,
      order: t.order ?? i
    }))
  } catch {
    return []
  }
},
```
  </action>
  <verify>
    <automated>grep -q "order: number" src/lib/storage.ts && grep -q "reorder(" src/lib/storage.ts && echo "order field and reorder method exist" || echo "MISSING"</automated>
  </verify>
  <done>
    - Todo型にorder: numberフィールドが存在する
    - storage.reorder(id, newOrder)メソッドが存在する
    - addTodoが最大order + 1を新規タスクに設定する
  </done>
</task>

<task type="auto">
  <name>Task 2: useTodos.tsにreorderTodoメソッドを追加</name>
  <files>src/hooks/useTodos.ts, src/core/store/todos.ts</files>
  <read_first>
    - src/hooks/useTodos.ts（現在のフック構造を確認）
    - src/core/store/todos.ts（Zustandストア構造を確認）
    - src/lib/storage.ts（reorderメソッドを確認）
  </read_first>
  <behavior>
    - useTodos.reorderTodo(id, newOrder)がゲスト/ログイン双方で動作する
    - ゲスト時: storage.reorderを呼び出し、ローカルストアを更新
    - ログイン時: tRPC reorder mutationを呼び出す
  </behavior>
  <action>
1. src/core/store/todos.tsにreorderアクションを追加:
```typescript
reorderLocalTodo: (id: string, newOrder: number) => {
  const todos = [...get().localTodos]
  const oldIndex = todos.findIndex(t => t.id === id)
  if (oldIndex === -1) return

  const [moved] = todos.splice(oldIndex, 1)
  todos.splice(newOrder, 0, moved)

  // order値を再採番
  todos.forEach((t, i) => { t.order = i })
  set({ localTodos: todos })
},
```

2. src/hooks/useTodos.tsにreorderTodoメソッドを追加:

まずtRPC mutationを定義:
```typescript
const reorderMutation = trpc.todos.reorder.useMutation({
  onSuccess: () => {
    utils.todos.getAll.invalidate()
  },
})
```

次にストアからreorderLocalTodoを取得:
```typescript
const {
  // ...existing
  reorderLocalTodo,
} = useTodosStore()
```

reorderTodo関数を実装:
```typescript
const reorderTodo = useCallback(
  async (id: string, newOrder: number) => {
    try {
      if (user) {
        await reorderMutation.mutateAsync({ id, newOrder })
        return true
      } else {
        storage.reorder(id, newOrder)
        reorderLocalTodo(id, newOrder)
        return true
      }
    } catch (e) {
      const msg = e instanceof Error ? e.message : 'Failed to reorder todo'
      if (!user) setError(msg)
      return false
    }
  },
  [user, reorderMutation, reorderLocalTodo, setError],
)
```

戻り値にreorderTodoを追加:
```typescript
return {
  // ...existing
  reorderTodo,
}
```
  </action>
  <verify>
    <automated>grep -q "reorderTodo" src/hooks/useTodos.ts && echo "reorderTodo exists" || echo "MISSING: reorderTodo"</automated>
  </verify>
  <done>
    - useTodosがreorderTodoメソッドをエクスポートする
    - ゲスト時はstorage.reorderを呼び出す
    - ログイン時はtRPC reorder mutationを呼び出す
    - npm run buildが成功する
  </done>
</task>

</tasks>

<verification>
- TypeScriptコンパイル: `npm run build`が成功
- 型整合性: Todo型にorderフィールドが存在
- ストア整合性: reorderLocalTodoアクションが存在
</verification>

<success_criteria>
- Todo型にorder: numberフィールドが追加されている
- storage.reorder(id, newOrder)が実装されている
- useTodos.reorderTodoがゲスト/ログイン双方で動作する
</success_criteria>

<output>
After completion, create `.planning/phases/18-drag-drop-sorting/18-02-SUMMARY.md`
</output>
