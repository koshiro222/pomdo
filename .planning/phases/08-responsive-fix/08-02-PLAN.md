---
phase: 08-responsive-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/timer/TimerDisplay.tsx
autonomous: true
requirements:
  - RESP-05
must_haves:
  truths:
    - "ユーザーはタイマー部分に過剰な余白がないレイアウトを見る"
    - "モードタブ（Focus/Short Break/Long Break）がTimerWidgetのパディングと視覚的に調和する"
  artifacts:
    - path: "src/components/timer/TimerDisplay.tsx"
      provides: "タイマー表示とモードタブ"
      contains: "top-4 left-4"
  key_links:
    - from: "TimerDisplay.tsx モードタブ"
      to: "TimerWidget p-6パディング"
      via: "absolute positioning"
      pattern: "top-4 left-4"
---

<objective>
タイマー部分の二重パディングを解消し、視覚的に調和したレイアウトにする。

Purpose: TimerWidgetのp-6パディングに対し、TimerDisplayのモードタブ位置（top-6 left-6）が二重パディングを生んでいた問題を解決する。
Output: 修正されたTimerDisplay.tsx（top-6 left-6 → top-4 left-4）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-responsive-fix/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/timer/TimerDisplay.tsx:
```typescript
// TimerWidget structure (from App.tsx lines 65-66):
// <div className="flex flex-col h-full items-center p-6">
//   <TimerDisplay ... />

// TimerDisplay.tsx line 27 (current):
<div className="absolute top-6 left-6 flex gap-2">

// Target:
<div className="absolute top-4 left-4 flex gap-2">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimerDisplay.tsxのモードタブ位置を調整</name>
  <files>src/components/timer/TimerDisplay.tsx</files>
  <read_first>
    - src/components/timer/TimerDisplay.tsx（モードタブの位置を確認）
    - src/App.tsx（TimerWidgetのp-6パディングを確認）
  </read_first>
  <action>
    src/components/timer/TimerDisplay.tsxのモードタブコンテナ（line 27付近）の位置を変更する:

    変更前: `<div className="absolute top-6 left-6 flex gap-2">`
    変更後: `<div className="absolute top-4 left-4 flex gap-2">`

    これにより:
    - TimerWidgetのp-6（24px）パディングに対し、モードタブがtop-4 left-4（16px）で配置
    - 視覚的な余白が24px + 24px = 48pxから24px + 16px = 40pxに削減
    - 二重パディング感が解消される
  </action>
  <verify>
    <automated>grep -n 'top-4 left-4' src/components/timer/TimerDisplay.tsx | head -1 | grep -q 'top-4 left-4' && echo "PASS: top-4 left-4 found" || echo "FAIL: top-4 left-4 not found"</automated>
  </verify>
  <done>
    - TimerDisplay.tsxのモードタブ位置がtop-4 left-4に変更されている
  </done>
  <acceptance_criteria>
    - `grep 'top-4 left-4' src/components/timer/TimerDisplay.tsx` が成功する
    - `grep 'top-6 left-6' src/components/timer/TimerDisplay.tsx` が失敗する
  </acceptance_criteria>
</task>

</tasks>

<verification>
## 検証手順

1. **型チェック**: `npm run build` が成功すること
2. **位置確認**: TimerDisplay.tsxでモードタブが`top-4 left-4`を持つこと
</verification>

<success_criteria>
- `npm run build` がエラーなく完了する
- TimerDisplay.tsxのモードタブ位置がtop-4 left-4である
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-fix/08-02-SUMMARY.md`
</output>
