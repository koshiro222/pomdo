---
phase: 18-drag-drop-sorting
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage.ts
  - src/hooks/useTodos.ts
autonomous: false
requirements: [DND-01, DND-02]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "TypeScriptビルドが成功する"
    - "ゲストユーザーがタスクを追加できる"
    - "ゲストユーザーがタスクをドラッグ&ドロップで並び替えできる"
  artifacts:
    - path: "src/lib/storage.ts"
      provides: "NewTodo型（order除外）"
      contains: "Omit<Todo, 'id' | 'order' | 'createdAt' | 'updatedAt'>"
    - path: "src/hooks/useTodos.ts"
      provides: "addTodo呼び出し（order未指定）"
      pattern: "storage.addTodo\\(\\{ title \\}\\)"
  key_links:
    - from: "src/hooks/useTodos.ts addTodo"
      to: "src/lib/storage.ts addTodo"
      via: "storage.addTodo({ title })"
      pattern: "storage\\.addTodo\\(\\{ title \\}\\)"
---

<objective>
TypeScriptコンパイルエラーを修正し、ドラッグ&ドロップ並び替え機能を正常に動作させる。

Purpose: NewTodo型の定義不備によりアプリが実行できない状態を解消する
Output: ビルドが成功し、D&D機能が完全に動作する状態
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
@/Users/koshiro/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-drag-drop-sorting/18-CONTEXT.md
@.planning/phases/18-drag-drop-sorting/18-VERIFICATION.md

## Gap Details

**Verification Gap:** TypeScriptコンパイルエラー
- `src/hooks/useTodos.ts(81,41): error TS2345: Argument of type '{ title: string; completed: false; }' is not assignable to parameter of type 'NewTodo'. Property 'order' is missing`

**Root Cause:**
- `src/lib/storage.ts` L13: `NewTodo = Omit<Todo, 'id' | 'createdAt' | 'updatedAt'>` — `order`が除外されていない
- 18-02-SUMMARY.mdには「NewTodo型からorderを除外」と記録されているが、実際のコードには未反映

**Fix Required:**
1. `src/lib/storage.ts` L13: `NewTodo`型に`'order'`を除外リストに追加
2. `src/hooks/useTodos.ts` L81: `completed: false`を削除（addTodo内部で設定される）
</context>

<tasks>

<task type="auto">
  <name>Task 1: NewTodo型からorderを除外</name>
  <files>src/lib/storage.ts</files>
  <read_first>
    - src/lib/storage.ts（現在の型定義を確認）
  </read_first>
  <behavior>
    - NewTodo型がOmit<Todo, 'id' | 'order' | 'createdAt' | 'updatedAt'>になる
    - addTodo関数がNewTodoを受け取り、内部でorderを自動採番する
  </behavior>
  <action>
    `src/lib/storage.ts` L13を修正:

    **変更前:**
    ```typescript
    export type NewTodo = Omit<Todo, 'id' | 'createdAt' | 'updatedAt'>
    ```

    **変更後:**
    ```typescript
    export type NewTodo = Omit<Todo, 'id' | 'order' | 'createdAt' | 'updatedAt'>
    ```

    これにより、addTodo呼び出し側でorderを指定する必要がなくなり、addTodo内部で`maxOrder + 1`として自動採番される（L69-73のロジックは変更不要）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/storage.ts`に`Omit<Todo, 'id' | 'order' | 'createdAt' | 'updatedAt'>`が存在する
    - TypeScriptビルドが成功する（エラー数0）
  </acceptance_criteria>
  <done>NewTodo型からorderが除外され、ビルドエラーが解消</done>
</task>

<task type="auto">
  <name>Task 2: useTodos.addTodo呼び出しを簡素化</name>
  <files>src/hooks/useTodos.ts, src/lib/storage.ts</files>
  <read_first>
    - src/hooks/useTodos.ts（現在のaddTodo呼び出しを確認）
    - src/lib/storage.ts（addTodo関数の実装を確認）
  </read_first>
  <behavior>
    - storage.addTodo({ title })でタスクを追加できる
    - completedフィールドはaddTodo内部でfalseに設定される
  </behavior>
  <action>
    **2つのファイルを修正:**

    **1. src/hooks/useTodos.ts L81:**

    **変更前:**
    ```typescript
    const added = storage.addTodo({ title, completed: false })
    ```

    **変更後:**
    ```typescript
    const added = storage.addTodo({ title })
    ```

    **2. src/lib/storage.ts addTodo関数（L70-76）:**

    **変更前:**
    ```typescript
    const newTodo: Todo = {
      id: crypto.randomUUID(),
      ...todo,
      order: maxOrder + 1,
      createdAt: now,
      updatedAt: now,
    }
    ```

    **変更後:**
    ```typescript
    const newTodo: Todo = {
      id: crypto.randomUUID(),
      completed: false,
      ...todo,
      order: maxOrder + 1,
      createdAt: now,
      updatedAt: now,
    }
    ```

    これで`...todo`のcompletedがundefinedでも、デフォルト値falseが使用される。NewTodo型から`order`と`completed`を除外する方針に合わせる。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useTodos.ts`に`storage.addTodo({ title })`が存在する
    - `src/lib/storage.ts`のaddTodo関数に`completed: false`のデフォルト設定がある
    - TypeScriptビルドが成功する
  </acceptance_criteria>
  <done>addTodo呼び出しが簡素化され、ビルドが成功</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TypeScriptビルドエラーの修正（NewTodo型からorder除外、addTodo呼び出しの簡素化） - server at http://localhost:5173
  </what-built>
  <how-to-verify>
    1. `npm run dev`でローカルサーバーを起動（http://localhost:5173）
    2. 未ログイン状態でタスクを2つ以上追加
    3. ドラッグハンドル（左側の⋮⋮）を掴んでタスクを上下にドラッグ
    4. ドロップ後、順序が変わることを確認
    5. ページをリロードし、順序が維持されていることを確認
    6. ログイン状態でも同様に動作することを確認
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
## Automated Verification

```bash
# TypeScriptビルドが成功すること
npm run build

# ユニットテストが成功すること
npm test
```

## Manual Verification

1. ゲストモードでのタスク追加・並び替え
2. ログインモードでのタスク追加・並び替え
3. ページリロード後の順序維持確認
</verification>

<success_criteria>
- TypeScriptビルドが成功する（エラー0）
- ゲストユーザーがタスクを追加できる
- ゲストユーザーがタスクをドラッグ&ドロップで並び替えできる
- ページリロード後も順序が維持される
</success_criteria>

<output>
After completion, create `.planning/phases/18-drag-drop-sorting/18-05-SUMMARY.md`
</output>
