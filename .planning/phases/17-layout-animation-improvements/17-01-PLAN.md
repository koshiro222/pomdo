---
phase: 17-layout-animation-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/animation.ts
  - src/components/todos/TodoItem.tsx
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
user_setup: []
must_haves:
  truths:
    - "タスク追加時に既存アイテムがスムーズにスライドして下に移動する"
    - "新しいタスクが展開アニメーションで出現する（高さ展開 + フェードイン）"
    - "ページリロード時に既存タスクがアニメーションしない"
  artifacts:
    - path: "src/lib/animation.ts"
      provides: "展開アニメーションバリアント（expandInVariants）"
      contains: "export const expandInVariants"
    - path: "src/components/todos/TodoItem.tsx"
      provides: "layout prop付きTodoItem"
      contains: "layout"
  key_links:
    - from: "src/components/todos/TodoItem.tsx"
      to: "src/lib/animation.ts"
      via: "import expandInVariants"
      pattern: "import.*expandInVariants"
---

<objective>
TodoItemコンポーネントにFramer Motionのlayout propと展開アニメーションを追加し、新タスク追加時のスムーズなアニメーションを実現する。

Purpose: ユーザーがタスクを追加した際、視覚的にスムーズなフィードバックを提供し、既存タスクが自然にスペースを空ける動きを実現する。
Output: expandInVariants追加、TodoItemのlayout propとアニメーション更新
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-layout-animation-improvements/17-CONTEXT.md
@.planning/phases/17-layout-animation-improvements/17-RESEARCH.md
@.planning/phases/17-layout-animation-improvements/17-UI-SPEC.md

<interfaces>

From src/lib/animation.ts:
```typescript
export const slideInVariants: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.3, ease: [0, 0, 0.2, 1] } },
  exit: { opacity: 0, x: 20, transition: { duration: 0.2, ease: [0.4, 0, 1, 1] } },
}
export const tapAnimation = { whileTap: { scale: 0.95 } }
export const hoverAnimation = { whileHover: { scale: 1.02 } }
```

From src/components/todos/TodoItem.tsx:
```typescript
interface TodoItemProps {
  id: string; title: string; completed: boolean; completedPomodoros?: number;
  isNew?: boolean; isSelected?: boolean; onClick?: () => void;
  onToggle: (id: string) => void; onDelete: (id: string) => void;
}
// motion.div at root with: variants={slideInVariants}, initial={isNew ? 'hidden' : 'visible'}, animate="visible"
```

From src/components/todos/TodoList.tsx:
```typescript
// AnimatePresence mode="popLayout" is already implemented
// TodoItem rendered inside AnimatePresence
// newTodoId state used for isNew prop with 200ms timeout
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: expandInVariantsをanimation.tsに追加</name>
  <files>src/lib/animation.ts</files>
  <read_first>
    - src/lib/animation.ts（現在のバリアント定義を確認）
    - .planning/phases/17-layout-animation-improvements/17-UI-SPEC.md（Interaction Contract - ANIM-02仕様）
  </read_first>
  <action>
    src/lib/animation.tsに新しい展開アニメーションバリアントを追加する。

    **追加するコード（ファイル末尾）:**
    ```typescript
    /**
     * 新規Todoアイテムの展開アニメーション
     * height: 0 → auto + opacity: 0 → 1
     */
    export const expandInVariants: Variants = {
      hidden: {
        opacity: 0,
        height: 0,
        marginBottom: 0,
      },
      visible: {
        opacity: 1,
        height: "auto",
        marginBottom: 12, // gap-3相当
        transition: {
          height: { duration: 0.25, ease: [0, 0, 0.2, 1] },
          opacity: { duration: 0.2, delay: 0.05, ease: [0, 0, 0.2, 1] },
        },
      },
      exit: {
        opacity: 0,
        height: 0,
        marginBottom: 0,
        transition: {
          height: { duration: 0.2, ease: [0.4, 0, 1, 1] },
          opacity: { duration: 0.15, ease: [0.4, 0, 1, 1] },
        },
      },
    }
    ```

    **重要:**
    - import文に`Variants`型が既にあることを確認（framer-motionから）
    - 既存のslideInVariantsは削除せず、新しいバリアントを追加するのみ
  </action>
  <acceptance_criteria>
    - `src/lib/animation.ts` に `export const expandInVariants: Variants` が存在する
    - `expandInVariants` に `hidden`, `visible`, `exit` の3つのキーが存在する
    - `hidden` に `opacity: 0, height: 0, marginBottom: 0` が含まれる
    - `visible` に `opacity: 1, height: "auto"` が含まれる
  </acceptance_criteria>
  <verify>
    <automated>grep -n "export const expandInVariants" src/lib/animation.ts</automated>
  </verify>
  <done>expandInVariantsがanimation.tsにエクスポートされ、TodoItemでimport可能な状態</done>
</task>

<task type="auto">
  <name>Task 2: TodoItemにlayout propとexpandInVariantsを適用</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在の実装を確認）
    - src/lib/animation.ts（expandInVariantsが追加されたことを確認）
    - .planning/phases/17-layout-animation-improvements/17-UI-SPEC.md（ANIM-01/02仕様）
  </read_first>
  <action>
    src/components/todos/TodoItem.tsxを以下の通り変更する。

    **1. import文の変更（line 6）:**
    ```typescript
    // 変更前
    import { slideInVariants, tapAnimation, hoverAnimation } from '@/lib/animation'

    // 変更後
    import { expandInVariants, tapAnimation, hoverAnimation } from '@/lib/animation'
    ```

    **2. motion.divのprops変更（line 43-52付近）:**
    ```typescript
    // 変更前
    <motion.div
      onClick={onClick}
      {...hoverAnimation}
      variants={slideInVariants}
      initial={isNew ? 'hidden' : 'visible'}
      animate="visible"
      className={`group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors ${
        completed ? 'opacity-60' : ''
      } ${isSelected && !completed ? 'border-l-2 border-cf-primary' : ''} ${onClick && !completed ? 'cursor-pointer' : ''}`}
    >

    // 変更後
    <motion.div
      layout
      onClick={onClick}
      {...hoverAnimation}
      variants={expandInVariants}
      initial={isNew ? 'hidden' : false}
      animate="visible"
      exit="exit"
      className={`group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors ${
        completed ? 'opacity-60' : ''
      } ${isSelected && !completed ? 'border-l-2 border-cf-primary' : ''} ${onClick && !completed ? 'cursor-pointer' : ''}`}
    >
    ```

    **重要な変更点:**
    - `layout` propを追加（単体、`layout="position"`ではない）
    - `variants={expandInVariants}` に変更
    - `initial={isNew ? 'hidden' : false}` に変更（既存タスクはアニメーションなし）
    - `exit="exit"` を明示的に追加

    **注意:**
    - `{...hoverAnimation}` は維持
    - `whileHover={{ scale: 1.02 }}` と `layout` は競合しない
  </action>
  <acceptance_criteria>
    - `src/components/todos/TodoItem.tsx` の import に `expandInVariants` が含まれる
    - `motion.div` に `layout` propが存在する
    - `variants={expandInVariants}` が設定されている
    - `initial={isNew ? 'hidden' : false}` となっている（`'visible'` ではない）
    - `exit="exit"` が設定されている
  </acceptance_criteria>
  <verify>
    <automated>grep -n "layout" src/components/todos/TodoItem.tsx | head -1</automated>
  </verify>
  <done>TodoItemにlayout propが追加され、新規タスク追加時に展開アニメーションが動作する状態</done>
</task>

</tasks>

<verification>
**ANIM-01 検証:**
- 新タスク追加時、既存タスクがスムーズに下方向へスライドすること

**ANIM-02 検証:**
- 新タスクが height: 0 → auto + opacity: 0 → 1 で展開すること
- ページリロード時に既存タスクがアニメーションしないこと（initial=false）

**動作確認手順:**
1. `npm run dev` で開発サーバー起動
2. TodoListカードで新タスクを追加
3. 既存タスクがスムーズに下へスライドすることを確認
4. 新タスクが展開しながら出現することを確認
5. ページをリロードし、既存タスクがアニメーションしないことを確認
</verification>

<success_criteria>
- [ ] `expandInVariants` が `src/lib/animation.ts` に追加されている
- [ ] TodoItem の import が `expandInVariants` に変更されている
- [ ] TodoItem の `motion.div` に `layout` prop が追加されている
- [ ] TodoItem の `variants` が `expandInVariants` に変更されている
- [ ] TodoItem の `initial` が `{isNew ? 'hidden' : false}` に変更されている
</success_criteria>

<output>
After completion, create `.planning/phases/17-layout-animation-improvements/17-01-SUMMARY.md`
</output>
