---
phase: 12-physical-interaction
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/bgm/BgmPlayer.tsx
  - src/components/todos/TodoList.tsx
  - src/components/tasks/CurrentTaskCard.tsx
autonomous: false
requirements:
  - RESP-06
  - RESP-07
user_setup: []

must_haves:
  truths:
    - "ユーザーはモバイル画面でコンテンツが画面外にあふれることなく閲覧できる"
    - "ユーザーは小さい画面でアルバムアートが適切なサイズで表示され、圧迫感を感じない"
  artifacts:
    - path: "src/components/bgm/BgmPlayer.tsx"
      provides: "アルバムアート固定サイズ、overflow-hidden削除"
      contains: "w-24 h-24"
      not_contains: "sm:w-32 sm:h-32"
    - path: "src/components/todos/TodoList.tsx"
      provides: "overflow-hidden削除"
      not_contains: "overflow-hidden"
    - path: "src/components/tasks/CurrentTaskCard.tsx"
      provides: "overflow-hidden削除"
      not_contains: "overflow-hidden"
  key_links:
    - from: "各カードコンポーネント"
      to: "モバイル表示"
      via: "overflow-hidden削除"
      pattern: "bento-card.*overflow-hidden"
---

<objective>
モバイルオーバーフロー解消とアルバムアート固定サイズ化によるレスポンシブ改善

Purpose: モバイルデバイスでコンテンツが切り取られないようにし、アルバムアートの一貫した表示を確保する
Output: overflow-hidden削除とアルバムアート96px固定サイズ
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-physical-interaction/12-CONTEXT.md
@.planning/phases/12-physical-interaction/12-RESEARCH.md
@.planning/phases/12-physical-interaction/12-UI-SPEC.md

<interfaces>
<!-- 既存のカード構造 -->

From src/components/bgm/BgmPlayer.tsx:
```tsx
// 現在のアルバムアート（レスポンシブサイズ）
<div className="relative w-24 h-24 sm:w-32 sm:h-32 flex-shrink-0">

// 現在のカードルート（overflow-hiddenあり）
<div className="relative flex flex-col h-full overflow-hidden">
```

From src/components/todos/TodoList.tsx:
```tsx
// 現在のカードルート（overflow-hiddenあり）
<div className="bento-card flex flex-col min-h-64 sm:h-full overflow-hidden">
```

From src/components/tasks/CurrentTaskCard.tsx:
```tsx
// 現在のカードルート（overflow-hiddenあり）
<div className="bento-card p-6 h-full flex flex-col relative overflow-hidden">
```

**注意**: .bento-cardクラス自体にもoverflow: hiddenがあるが、これはカードデザインの一部として維持する。
今回削除するのは各コンポーネントルートのoverflow-hiddenのみ。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BgmPlayerアルバムアート固定サイズ化 + overflow-hidden削除 (RESP-06, RESP-07)</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx（現在のアルバムアートとカード構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - アルバムアートを96px固定サイズ（w-24 h-24）に変更
    - レスポンシブ縮小（sm:w-32 sm:h-32）を削除
    - 内部の円形要素も固定サイズに変更
    - カードルートのoverflow-hiddenを削除
    - アニメーション用のoverflow-hiddenは維持
  </behavior>
  <action>
    src/components/bgm/BgmPlayer.tsxの以下を修正:

    1. **アルバムアート外側** - 行14:
       ```tsx
       // Before
       <div className="relative w-24 h-24 sm:w-32 sm:h-32 flex-shrink-0">

       // After
       <div className="relative w-24 h-24 flex-shrink-0">
       ```

    2. **アルバムアート内側の円** - 行25:
       ```tsx
       // Before
       <div
         className={`w-20 h-20 sm:w-24 sm:h-24 rounded-full flex items-center justify-center ${isPlaying ? 'album-art-spinning' : ''}`}
         ...

       // After
       <div
         className={`w-20 h-20 rounded-full flex items-center justify-center ${isPlaying ? 'album-art-spinning' : ''}`}
         ...
       ```

    3. **Musicアイコン** - 行31:
       ```tsx
       // Before
       <Music className={`w-8 h-8 sm:w-10 sm:h-10 text-white ${isPlaying ? '' : 'album-art-paused'}`} />

       // After
       <Music className={`w-8 h-8 text-white ${isPlaying ? '' : 'album-art-paused'}`} />
       ```

    4. **カードルートのoverflow-hidden削除** - 行76:
       ```tsx
       // Before
       <div className="relative flex flex-col h-full overflow-hidden">

       // After
       <div className="relative flex flex-col h-full">
       ```

    **注意**: AnimatePresence内のプレイリスト展開部分（行182-248）にはoverflow-y-autoがあり、これは維持する。
  </action>
  <verify>
    <automated>grep -q "w-24 h-24 flex-shrink-0" src/components/bgm/BgmPlayer.tsx && ! grep -q "sm:w-32 sm:h-32" src/components/bgm/BgmPlayer.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsxに`w-24 h-24 flex-shrink-0`が存在する
    - src/components/bgm/BgmPlayer.tsxに`sm:w-32 sm:h-32`が存在しない
    - src/components/bgm/BgmPlayer.tsxに`sm:w-24 sm:h-24`が存在しない
    - src/components/bgm/BgmPlayer.tsxに`sm:w-10 sm:h-10`が存在しない
    - カードルートのclassNameに`overflow-hidden`が含まれていない
  </acceptance_criteria>
  <done>
    - アルバムアートが96px（w-24 h-24）で固定表示される
    - レスポンシブ縮小が削除されている
    - カードルートのoverflow-hiddenが削除されている
  </done>
</task>

<task type="auto">
  <name>Task 2: TodoList overflow-hidden削除 (RESP-06)</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（現在のカード構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - ローディング状態のカードルートのoverflow-hiddenを削除
    - メインのカードルートのoverflow-hiddenを削除
    - 内部のスクロール領域（overflow-y-auto）は維持
  </behavior>
  <action>
    src/components/todos/TodoList.tsxの以下を修正:

    1. **ローディング状態** - 行48:
       ```tsx
       // Before
       <div className="bento-card flex flex-col min-h-64 sm:h-full overflow-hidden p-4">

       // After
       <div className="bento-card flex flex-col min-h-64 sm:h-full p-4">
       ```

    2. **メインカード** - 行55:
       ```tsx
       // Before
       <div className="bento-card flex flex-col min-h-64 sm:h-full overflow-hidden">

       // After
       <div className="bento-card flex flex-col min-h-64 sm:h-full">
       ```

    **注意**: Todoリスト領域（行91）の`overflow-y-auto`は維持する。これはスクロール機能に必要。
  </action>
  <verify>
    <automated>! grep -q "bento-card.*overflow-hidden" src/components/todos/TodoList.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/todos/TodoList.tsxに`bento-card.*overflow-hidden`パターンが存在しない
    - ローディング状態のdivに`overflow-hidden`が含まれていない
    - メインカードのdivに`overflow-hidden`が含まれていない
    - `overflow-y-auto`は維持されている
  </acceptance_criteria>
  <done>
    - TodoListのカードルートからoverflow-hiddenが削除されている
    - 内部のスクロール領域は維持されている
  </done>
</task>

<task type="auto">
  <name>Task 3: CurrentTaskCard overflow-hidden削除 (RESP-06)</name>
  <files>src/components/tasks/CurrentTaskCard.tsx</files>
  <read_first>
    - src/components/tasks/CurrentTaskCard.tsx（現在のカード構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - カードルートのoverflow-hiddenを削除
    - AnimatePresenceのメニュー表示機能は維持
  </behavior>
  <action>
    src/components/tasks/CurrentTaskCard.tsxのカードルートを修正（行42）:

    ```tsx
    // Before
    <div className="bento-card p-6 h-full flex flex-col relative overflow-hidden">

    // After
    <div className="bento-card p-6 h-full flex flex-col relative">
    ```

    **注意**: AnimatePresence内のメニュー（行66-83）はabsolute positionedで表示されるため、overflow-hidden削除の影響を受けない。
  </action>
  <verify>
    <automated>! grep -q "bento-card.*overflow-hidden" src/components/tasks/CurrentTaskCard.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/tasks/CurrentTaskCard.tsxに`bento-card.*overflow-hidden`パターンが存在しない
    - カードルートのdivに`overflow-hidden`が含まれていない
    - AnimatePresenceのメニュー機能が正常に動作する
  </acceptance_criteria>
  <done>
    - CurrentTaskCardのカードルートからoverflow-hiddenが削除されている
    - メニュー表示機能が正常に動作する
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 視覚確認 (TOUCH-01, TOUCH-02, RESP-06, RESP-07)</name>
  <files>なし（視覚確認のみ）</files>
  <action>
    Phase 12の物理的インタラクション改善が完了したことを視覚的に確認する。

    確認項目:
    1. グローバルcursor定義（全button要素にpointer cursor）
    2. タッチターゲット拡大（全小さなアイコンボタンにp-3追加）
    3. アルバムアート固定サイズ化（96px）
    4. overflow-hidden削除（TodoList、CurrentTaskCard、BgmPlayer）
  </action>
  <verify>
    ## 1. 開発サーバー起動
    ```bash
    npm run dev
    ```
    ブラウザで http://localhost:5173 を開く

    ## 2. カーソル確認 (TOUCH-02)
    - 任意のボタンにマウスをホバー
    - カーソルがpointer（指の形）になることを確認
    - ドラッグハンドル（TodoItem左側のグリップ）はgrabカーソルになることを確認

    ## 3. タッチターゲット確認 (TOUCH-01)
    - DevToolsを開き、Elementsタブで以下のボタンを選択:
      - BgmPlayerの次/前/プレイリストボタン
      - TodoItemの削除ボタン
      - TrackItemの編集/削除ボタン（管理画面で確認）
    - Computed パネルでwidth/heightが44px以上であることを確認
    - ホバー時に薄い背景色（white/10）が表示されることを確認

    ## 4. アルバムアート確認 (RESP-07)
    - BgmPlayerのアルバムアートが96pxで固定表示されていることを確認
    - 画面幅を変更してもサイズが変わらないことを確認

    ## 5. オーバーフロー確認 (RESP-06)
    - DevToolsでモバイルビュー（iPhone SE等）に切り替え
    - 各カードの内容が切り取られずに表示されることを確認
    - TodoList、CurrentTaskCard、BgmPlayerでスクロールが必要な場合はスクロールできることを確認

    ## 6. ビルド確認
    ```bash
    npm run build
    ```
    エラーなくビルドが完了することを確認
  </verify>
  <done>
    - 全ての視覚的確認が完了し、ユーザーが承認している
    - ビルドが成功している
  </done>
  <resume_signal>Type "approved" または問題点を記述してください</resume_signal>
</task>

</tasks>

<verification>
## 自動検証

1. **アルバムアート固定サイズ (RESP-07)**
   - BgmPlayerに`w-24 h-24 flex-shrink-0`が存在すること
   - `sm:w-32 sm:h-32`が存在しないこと

2. **overflow-hidden削除 (RESP-06)**
   - TodoListのカードルートにoverflow-hiddenがないこと
   - CurrentTaskCardのカードルートにoverflow-hiddenがないこと
   - BgmPlayerのカードルートにoverflow-hiddenがないこと

3. **ビルド成功**
   - `npm run build`がエラーなく完了すること

## 手動検証

- モバイルビューでコンテンツが切り取られないこと
- アルバムアートが固定サイズで表示されること
- 全ボタンでpointerカーソルが表示されること
</verification>

<success_criteria>
- BgmPlayerのアルバムアートが96px（w-24 h-24）で固定表示される
- TodoList、CurrentTaskCard、BgmPlayerのカードルートからoverflow-hiddenが削除されている
- モバイルビューでコンテンツが画面外にあふれない
- npm run buildが成功する
- 視覚的確認が完了し、ユーザーが承認する
</success_criteria>

<output>
After completion, create `.planning/phases/12-physical-interaction/12-02-SUMMARY.md`
</output>
