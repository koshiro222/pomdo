---
phase: 09-stats
plan: 05
type: execute
wave: 2
depends_on: ["09-02", "09-03", "09-04"]
files_modified:
  - src/components/stats/StatsCard.tsx
autonomous: true
requirements:
  - STAT-06
  - STAT-07
  - STAT-08
user_setup: []

must_haves:
  truths:
    - "データ取得中にローディングスピナーが表示される"
    - "ローディング中も既存の統計データが表示される"
    - "データがない場合に空状態（アイコン＋メッセージ）が表示される"
    - "セッション完了後に統計が自動更新される"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "ローディング状態と空状態のUI"
      contains: "loading && completedSessions.length === 0"
      exports: ["default"]
  key_links:
    - from: "src/components/stats/StatsCard.tsx"
      to: "src/hooks/usePomodoro.ts"
      via: "loading state"
      pattern: "const { sessions, loading } = usePomodoro\\(\\)"
    - from: "src/components/stats/StatsCard.tsx"
      to: "lucide-react"
      via: "BarChart3 icon import"
      pattern: "import.*BarChart3.*from 'lucide-react'"
---

## Objective

ローディング状態と空状態のUIを実装し、データ更新時の再描画を確認する。

## Purpose

ユーザーにデータ取得中とデータなし状態を明示的に表示する。ローディング中は既存データを維持し、空状態はアイコンとメッセージで案内する。Reactの再レンダーフローで自動更新を実現する。

## Output

- ローディング状態: 既存データ維持＋スピナー表示
- 空状態: BarChart3アイコン＋メッセージ表示
- データ更新: セッション完了後に統計が自動更新

## Execution Context

@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-stats/09-CONTEXT.md
@.planning/phases/09-stats/09-RESEARCH.md
@.planning/phases/09-stats/09-UI-SPEC.md
@src/components/stats/StatsCard.tsx

## Interfaces

From src/hooks/usePomodoro.ts:
```typescript
interface UsePomodoroReturn {
  sessions: Session[]
  loading: boolean
  error: string | null
  // セッション完了時にinvalidateQueriesで自動更新される
}
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: ローディング状態を実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（既存の実装を確認するため）
    - .planning/phases/09-stats/09-UI-SPEC.md（ローディング状態パターンを確認するため）
  </read_first>
  <behavior>
    STAT-06: loadingがtrueの場合にスピナーが表示される
    STAT-06: ローディング中も既存の統計データが表示される
    スピナー: グラフ中央に配置、既存データにオーバーレイ
  </behavior>
  <action>
    ローディング状態のUIを実装する。既存データを維持したまま、スピナーをオーバーレイ表示する。

    実装内容：
    1. 既存の統計表示をrelativeコンテナでラップ
    2. loadingがtrueの場合にスピナーをオーバーレイ表示
    3. スピナーはグラフ中央に配置

    ローディング状態UI:
    ```tsx
    const { sessions, loading } = usePomodoro()
    const completedSessions = sessions.filter(s => s.completedAt !== null)

    return (
      <div className="glass rounded-3xl p-6 h-full flex flex-col overflow-y-auto min-h-0 relative">
        {/* 既存の統計表示 */}
        <p className="text-xs uppercase tracking-widest text-cf-subtext font-bold mb-4">
          Stats
        </p>

        {/* タブ切り替えとコンテンツ */}

        {/* ローディングスピナー（オーバーレイ） */}
        {loading && (
          <div className="absolute inset-0 flex items-center justify-center bg-black/20 rounded-3xl">
            <div className="w-8 h-8 border-2 border-cf-primary border-t-transparent rounded-full animate-spin" />
          </div>
        )}
      </div>
    )
    ```

    注: 既存データを維持するUXアプローチ。loading中にコンテンツを非表示にしない。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-06" --run</automated>
  </verify>
  <done>
    - loadingがtrueの場合にスピナーが表示される
    - ローディング中も既存の統計データが表示されている
    - スピナーがグラフ中央に配置されている
    - src/components/stats/StatsCard.test.tsのSTAT-06テストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 空状態を実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Task 1の変更を確認するため）
    - .planning/phases/09-stats/09-UI-SPEC.md（空状態パターンを確認するため）
  </read_first>
  <behavior>
    STAT-07: completedSessions.length === 0 && !loadingの場合に空状態が表示される
    STAT-07: BarChart3アイコンが表示される
    STAT-07: 「セッションがありません\nポモドーロを始めて記録を残しましょう」メッセージが表示される
  </behavior>
  <action>
    空状態のUIを実装する。完了済みセッションがない場合にアイコンとメッセージを表示する。

    実装内容：
    1. completedSessions.length === 0 && !loadingの条件をチェック
    2. BarChart3アイコンとメッセージを中央配置で表示
    3. タブ切り替えよりも優先して空状態を表示

    空状態UI:
    ```tsx
    import { BarChart3 } from 'lucide-react'

    const completedSessions = sessions.filter(s => s.completedAt !== null)

    if (completedSessions.length === 0 && !loading) {
      return (
        <div className="glass rounded-3xl p-6 h-full flex flex-col">
          <p className="text-xs uppercase tracking-widest text-cf-subtext font-bold mb-4">
            Stats
          </p>

          <div className="flex-1 flex flex-col items-center justify-center text-cf-subtext">
            <BarChart3 className="w-16 h-16 mb-4 opacity-50" />
            <p className="text-center">
              セッションがありません<br />
              ポモドーロを始めて記録を残しましょう
            </p>
          </div>
        </div>
      )
    }

    // 既存の統計表示（タブ切り替えなど）
    ```

    注: 空状態とローディング状態の判定順序に注意。loading優先 → データ長チェック。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-07" --run</automated>
  </verify>
  <done>
    - completedSessions.length === 0 && !loadingの場合に空状態が表示される
    - BarChart3アイコンが表示されている
    - 「セッションがありません\nポモドーロを始めて記録を残しましょう」メッセージが表示されている
    - src/components/stats/StatsCard.test.tsのSTAT-07テストがパスする
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ローディング状態と空状態のUI実装完了</what-built>
  <how-to-verify>
    npm run devで開発サーバーを起動し、以下を確認：

    1. ローディング状態の確認:
       - ブラウザのDevToolsでNetworkタブを開き、Throttlingを"Slow 3G"に設定
       - ページをリロードし、スピナーが表示されることを確認
       - 既存の統計データが表示されたままスピナーがオーバーレイされることを確認

    2. 空状態の確認:
       - localStorageをクリア（Applicationタブ → Local Storage → Clear）
       - ページをリロードし、空状態が表示されることを確認
       - BarChart3アイコンとメッセージが表示されていることを確認
       - メッセージが2行で表示されていることを確認

    3. データ更新の確認（STAT-08）:
       - ポモドーロセッションを開始・完了
       - 統計が自動的に更新されることを確認
       - タブを切り替えても統計が保持されていることを確認
  </how-to-verify>
  <resume-signal>ローディング状態と空状態が正しく表示されていることを確認したら、"approved"と入力してください。問題があれば詳細を教えてください。</resume-signal>
</task>

## Verification

1. ローディング状態が正しく表示される
   - npm run devでスピナーが表示されることを目視確認

2. 空状態が正しく表示される
   - localStorageをクリアして空状態を確認

3. データ更新が正しく動作する
   - セッション完了後に統計が自動更新される

## Success Criteria

1. ローディング中にスピナーが表示される
2. ローディング中も既存データが表示される
3. データがない場合に空状態が表示される
4. セッション完了後に統計が自動更新される
5. useEffectは使用せず、Reactの再レンダーフローで実装されている

## Output

完了後、`.planning/phases/09-stats/09-05-SUMMARY.md`を作成する
