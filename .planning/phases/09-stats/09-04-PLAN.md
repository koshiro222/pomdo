---
phase: 09-stats
plan: 04
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - src/components/stats/StatsCard.tsx
autonomous: true
requirements:
  - STAT-03
user_setup: []

must_haves:
  truths:
    - "Monthタブで月次統計が表示される"
    - "月次統計には月間合計集中時間と月間セッション数が表示される"
    - "グラフは表示されず、合計値のみ表示（Today/Weekと同じスタイル）"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "MonthlyStatsコンポーネント"
      contains: "getMonthlyStats"
      exports: ["default"]
  key_links:
    - from: "src/components/stats/StatsCard.tsx"
      to: "src/hooks/usePomodoro.ts"
      via: "sessions.filter for monthly data"
      pattern: "sessions\\.filter\\(s => .* sessionDate >= firstDay \\)"
---

## Objective

Monthタブで月次統計を表示する。今月（1日〜末日）の合計集中時間とセッション数を集計し、グラフなしで数値のみ表示する。

## Purpose

ユーザーが今月の作業統計を合計値で確認できるようにする。グラフは表示せず、Todayと同じスタイル（アイコン＋数値＋ラベル）でシンプルに表示する。

## Output

- Monthタブで月間合計集中時間と月間セッション数が表示される
- 集計期間は今月1日〜現在日時

## Execution Context

@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-stats/09-CONTEXT.md
@.planning/phases/09-stats/09-RESEARCH.md
@.planning/phases/09-stats/09-UI-SPEC.md
@src/components/stats/StatsCard.tsx

## Interfaces

From src/hooks/usePomodoro.ts:
```typescript
type Session = {
  id: string
  startedAt: string
  completedAt: string | null
  durationSecs: number
  type: 'work' | 'short_break' | 'long_break'
}
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: 月次統計集計ロジックを実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Plan 02のTodayStats実装を確認するため）
    - .planning/phases/09-stats/09-RESEARCH.md（月次統計パターンを確認するため）
  </read_first>
  <behavior>
    STAT-03: 今月1日〜末日のセッションが集計される
    STAT-03: 月間合計集中時間と月間セッション数が計算される
    集計期間: startedAt基準で今月1日〜現在
  </behavior>
  <action>
    月次統計集計ロジックを実装する。

    実装内容：
    1. 今月1日0時0分0秒のDateオブジェクトを生成
    2. sessionsを今月1日以降の完了済みworkセッションでフィルタリング
    3. reduce関数で合計集中時間とセッション数を計算

    月次統計集計:
    ```tsx
    const getMonthlyStats = (sessions: Session[]): { totalMinutes: number; totalSessions: number } => {
      const now = new Date()
      const firstDay = new Date(now.getFullYear(), now.getMonth(), 1)

      return sessions
        .filter(s => {
          const sessionDate = new Date(s.startedAt)
          return s.type === 'work' && s.completedAt !== null && sessionDate >= firstDay && sessionDate <= now
        })
        .reduce(
          (acc, s) => ({
            totalMinutes: acc.totalMinutes + Math.floor(s.durationSecs / 60),
            totalSessions: acc.totalSessions + 1,
          }),
          { totalMinutes: 0, totalSessions: 0 }
        )
    }

    const monthlyStats = getMonthlyStats(sessions)
    ```

    注: startedAt基準で集計する仕様通り実装。月末のセッションが翌月1日に完結した場合のエッジケースは無視。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-03" --run</automated>
  </verify>
  <done>
    - getMonthlyStats関数が定義されている
    - 今月1日以降のセッションが正しくフィルタリングされている
    - 月間合計集中時間と月間セッション数が正しく計算されている
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MonthタブUIを実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Task 1の変更とTodayStats実装を確認するため）
    - .planning/phases/09-stats/09-UI-SPEC.md（統計表示パターンを確認するため）
  </read_first>
  <behavior>
    STAT-03: Monthタブで月間合計集中時間が表示される
    STAT-03: Monthタブで月間セッション数が表示される
    UIスタイル: Todayと同じ（アイコン＋数値＋ラベル）
  </behavior>
  <action>
    MonthタブのUIを実装する。Todayと同じスタイル（Clockアイコン、Targetアイコン、数値表示）を使用する。

    実装内容：
    1. activeTab === 'month'の場合の条件レンダリングを追加
    2. MonthlyStatsコンポーネントを作成（またはTodayStatsと同様のインライン実装）
    3. 月間合計集中時間と月間セッション数を表示

    MonthタブUI:
    ```tsx
    {activeTab === 'month' && (
      <div className="mb-6">
        {/* 月間集中時間 */}
        <div className="flex items-center gap-3 mb-3">
          <div className="w-10 h-10 rounded-xl bg-cf-primary/20 flex items-center justify-center">
            <Clock className="w-5 h-5 text-cf-primary" />
          </div>
          <div>
            <p className="text-2xl font-bold text-cf-text">
              {monthlyStats.totalMinutes > 0 ? `${Math.floor(monthlyStats.totalMinutes / 60)}h ${monthlyStats.totalMinutes % 60}m` : '0m'}
            </p>
            <p className="text-xs text-cf-subtext">Focus Time This Month</p>
          </div>
        </div>

        {/* 月間セッション数 */}
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded-xl bg-cf-primary/20 flex items-center justify-center">
            <Target className="w-5 h-5 text-cf-primary" />
          </div>
          <div>
            <p className="text-2xl font-bold text-cf-text">
              {monthlyStats.totalSessions}
            </p>
            <p className="text-xs text-cf-subtext">Pomodoros This Month</p>
          </div>
        </div>
      </div>
    )}
    ```

    注: グラフは表示せず、数値のみ表示する仕様通り実装。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-03" --run</automated>
  </verify>
  <done>
    - Monthタブが選択できる
    - 月間合計集中時間が表示される（Clockアイコン）
    - 月間セッション数が表示される（Targetアイコン）
    - UIスタイルがTodayと同じである
    - src/components/stats/StatsCard.test.tsのSTAT-03テストがパスする
  </done>
</task>

## Verification

1. 月次統計が正しく集計される
   - 今月1日以降のセッションのみが含まれている
   - 合計集中時間とセッション数が正しい

2. UIが正しく表示される
   - npm run devでMonthタブを確認
   - アイコン、数値、ラベルが表示されている

## Success Criteria

1. Monthタブで月間合計集中時間と月間セッション数が表示される
2. 集計期間が今月1日〜現在である
3. UIスタイルがTodayと同じである
4. グラフが表示されていない（数値のみ）

## Output

完了後、`.planning/phases/09-stats/09-04-SUMMARY.md`を作成する
