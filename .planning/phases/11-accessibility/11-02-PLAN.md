---
phase: 11-accessibility
plan: 02
type: execute
wave: 1
depends_on: [11-00]
files_modified: [src/components/todos/TodoItem.tsx]
autonomous: true
requirements: [A11Y-03]

must_haves:
  truths:
    - "ユーザーはドラッグ操作可能な要素をホバーなしで認識できる"
    - "ユーザーはドラッグハンドルが常に薄く表示されていることを確認できる"
    - "ユーザーはホバー時にドラッグハンドルが強調されることを確認できる"
  artifacts:
    - path: "src/components/todos/TodoItem.tsx"
      provides: "Todoアイテム表示、ドラッグハンドル"
      contains: "opacity-30 group-hover:opacity-50"
  key_links:
    - from: "TodoItem.tsx"
      to: "ドラッグ操作"
      via: "GripVerticalコンポーネント"
      pattern: "cursor-grab"
---

<objective>
ドラッグハンドルを常時表示し、ユーザーがドラッグ操作可能な要素をキーボードなしで認識できるようにする。

Purpose: ホバーしなくてもドラッグハンドルが視認できるようにし、操作性を向上させる。
Output: 更新されたTodoItem.tsx
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-accessibility/11-CONTEXT.md
@.planning/phases/11-accessibility/11-RESEARCH.md
@.planning/phases/11-accessibility/11-00-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/todos/TodoItem.tsx (現在の実装):
```tsx
// ドラッグハンドル（line 54-60）
<motion.button
  {...tapAnimation}
  onClick={(e) => e.stopPropagation()}
  className="opacity-0 group-hover:opacity-50 transition-opacity cursor-grab active:cursor-grabbing text-cf-subtext"
>
  <GripVertical className="text-sm" />
</motion.button>

// 削除ボタン（line 95-104）
<motion.button
  {...tapAnimation}
  onClick={(e) => {
    e.stopPropagation()
    onDelete(id)
  }}
  className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
>
  <Trash2 className="text-lg" />
</motion.button>
```

From CONTEXT.md (ユーザー決定):
- ドラッグハンドル: `opacity-0` → `opacity-30` に変更し、常時薄く表示
- ホバー時: `opacity-50` に強調（現状維持）
- ARIAラベルは付与しない（role="button"のみ）

From src/components/todos/TodoItem.test.tsx (Wave 0で作成されたテスト):
```tsx
it('should have drag handle with visible opacity (not opacity-0)', () => {
  const { container } = render(<TodoItem {...defaultProps} />)
  const dragHandle = container.querySelector('button.cursor-grab')
  expect(dragHandle).toBeInTheDocument()
  // 実装後: opacity-30が設定されていることを確認
  expect(dragHandle?.className).toMatch(/opacity-\d+/)
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ドラッグハンドル常時表示 (A11Y-03)</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在のドラッグハンドル実装を確認）
  </read_first>
  <action>
    src/components/todos/TodoItem.tsxのドラッグハンドル（GripVertical）のopacityを変更する:

    修正箇所（line 57付近）:
    ```tsx
    // 修正前
    className="opacity-0 group-hover:opacity-50 transition-opacity cursor-grab active:cursor-grabbing text-cf-subtext"

    // 修正後
    className="opacity-30 group-hover:opacity-50 transition-opacity cursor-grab active:cursor-grabbing text-cf-subtext"
    ```

    変更内容:
    - `opacity-0` → `opacity-30`: 常時薄く表示（Notionパターン）
    - `group-hover:opacity-50`: ホバー時に強調（現状維持）
    - その他のクラスは変更なし

    注意:
    - role="button"は CONTEXT.md で「ARIAラベルは付与しない（role="button"のみ）」とあるが、
      現状role属性がないため追加しない（スクリーンリーダー対応はスコープ外）
  </action>
  <verify>
    <automated>npm test -- TodoItem --run 2>&1 | grep -E "(passed|failed)" | head -1</automated>
  </verify>
  <done>
    - ドラッグハンドルが常に薄く表示される（opacity-30）
    - ホバー時にドラッグハンドルが強調される（opacity-50）
    - ホバーなしでドラッグ操作可能な要素を認識できる
  </done>
</task>

</tasks>

<verification>
## 検証手順

### 自動検証
```bash
# ユニットテスト実行
npm test -- TodoItem --run

# ドラッグハンドル常時表示の確認
grep -q "opacity-30 group-hover:opacity-50" src/components/todos/TodoItem.tsx && echo "PASS" || echo "FAIL"
```

### 手動検証
1. **ドラッグハンドル視認性 (A11Y-03)**:
   - `npm run dev` で開発サーバーを起動
   - ブラウザで http://localhost:5173 を開く
   - Todoアイテムの左側にドラッグハンドル（縦点線アイコン）が薄く表示されることを確認
   - ホバーなしでハンドルが見えることを確認
   - Todoアイテムにホバーするとハンドルが少し強調されることを確認
</verification>

<success_criteria>
- [ ] ドラッグハンドルのopacityがopacity-30に変更されている
- [ ] ホバー時のopacity-50が維持されている
- [ ] ホバーなしでドラッグハンドルが視認できる
- [ ] `npm test -- TodoItem --run` がPASSする
</success_criteria>

<output>
After completion, create `.planning/phases/11-accessibility/11-02-SUMMARY.md`
</output>
