---
phase: 01-database
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - DB-03
must_haves:
  truths:
    - "本番環境にbgm_tracksテーブルが作成されている"
    - "Drizzle Studioでテーブル構造と2件のデータが確認できる"
  artifacts:
    - path: "drizzle/meta/_journal.json"
      provides: "マイグレーション適用履歴"
      contains: "0005"
      exports: []
  key_links:
    - from: "drizzle/0005_*.sql"
      to: "Neon PostgreSQL"
      via: "npm run db:migrate"
      pattern: "CREATE TABLE.*bgm_tracks"
---

<objective>
マイグレーションを本番環境に適用し、Drizzle Studio でテーブル構造とデータを検証する。

Purpose: Phase 1の完了条件である「本番環境でのテーブル作成」と「データの確認」を実現する。
Output: 本番DBに `bgm_tracks` テーブルと初期データ2件が存在する状態
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database/01-CONTEXT.md

<interfaces>
<!-- このプランで使用するコマンドと期待結果 -->

**マイグレーション適用コマンド:**
```bash
npm run db:migrate
```

**Drizzle Studio 起動コマンド:**
```bash
npx drizzle-kit studio
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: マイグレーションを本番環境に適用</name>
  <files></files>
  <read_first>
    - drizzle/0005_*.sql（適用するマイグレーション内容の確認）
    - drizzle/meta/_journal.json（マイグレーション履歴の確認）
  </read_first>
  <action>
    `npm run db:migrate` を実行してマイグレーションを本番DBに適用する。

    **前提条件の確認:**
    - `DATABASE_URL` 環境変数が設定されていることを確認する
      ```bash
      echo $DATABASE_URL | grep -v '^$' || echo "DATABASE_URL is not set"
      ```
    - 設定されていない場合は `.dev.vars` または Cloudflare Pages シークレットを確認

    **実行コマンド:**
    ```bash
    npm run db:migrate
    ```

    **期待される出力:**
    - `No migrations to apply` または成功メッセージ
    - エラーがないこと

    **注意事項:**
    - このコマンドは本番DB（Neon）に接続する
    - 既存テーブルには影響しない（新規テーブルのみ作成）
  </action>
  <acceptance_criteria>
    - `npm run db:migrate` がエラーなく完了する
    - コンソールにマイグレーション成功のメッセージが表示される
  </acceptance_criteria>
  <verify>
    <automated>npm run db:migrate 2>&1 | grep -v "error" || echo "Migration completed"</automated>
  </verify>
  <done>マイグレーションが本番DBに適用され、`bgm_tracks` テーブルが作成されている</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drizzle Studio でテーブル構造とデータを確認</name>
  <files></files>
  <read_first>
    - .planning/phases/01-database/01-CONTEXT.md（確認項目の詳細）
  </read_first>
  <what_built>
    - `bgm_tracks` テーブル（本番DBに作成済み）
    - 初期データ2件（Lo-Fi Study 01, Lo-Fi Study 02）
  </what_built>
  <action>
    Drizzle Studio を起動し、本番DBの `bgm_tracks` テーブルを確認する。

    **実行コマンド:**
    ```bash
    npx drizzle-kit studio
    ```

    **確認手順:**
    1. ブラウザで Drizzle Studio が開く（通常は `https://local.drizzle.studio`）
    2. 左サイドバーから `bgm_tracks` テーブルを選択
    3. 以下を確認:
       - **カラム構造**: id, title, artist, color, filename, tier, created_at, updated_at
       - **データ件数**: 2件
       - **データ内容**:
         - 1件目: title="Lo-Fi Study 01", artist="Chill Beats", color="#3b82f6", filename="bgm/lofi-01.mp3", tier="free"
         - 2件目: title="Lo-Fi Study 02", artist="Relax Sounds", color="#8b5cf6", filename="bgm/lofi-02.mp3", tier="free"
  </action>
  <how_to_verify>
    1. ターミナルで `npx drizzle-kit studio` を実行
    2. ブラウザで Drizzle Studio を開く
    3. `bgm_tracks` テーブルを選択
    4. カラム構造が以下であることを確認:
       - id (uuid, PRIMARY KEY)
       - title (text, NOT NULL)
       - artist (text, nullable)
       - color (text, nullable)
       - filename (text, NOT NULL, UNIQUE)
       - tier (text, NOT NULL, default: 'free')
       - created_at (timestamp, NOT NULL)
       - updated_at (timestamp, NOT NULL)
    5. 2件のデータが正しく登録されていることを確認
  </how_to_verify>
  <verify>
    <automated>psql $DATABASE_URL -c 'SELECT COUNT(*) FROM bgm_tracks;' 2>/dev/null | grep -E '^\s*2\s*$' || echo "Automated check skipped - manual verification required"</automated>
    <manual>User visually confirms in Drizzle Studio that bgm_tracks table exists with 8 columns and 2 rows of correct data</manual>
  </verify>
  <resume_signal>Type "approved" if table structure and data are correct, or describe any issues found</resume_signal>
  <acceptance_criteria>
    - `bgm_tracks` テーブルが8カラム（id, title, artist, color, filename, tier, created_at, updated_at）で作成されている
    - データが2件登録されている
    - 各カラムの型と制約が設計通りである
  </acceptance_criteria>
  <done>ユーザーが Drizzle Studio でテーブル構造とデータを確認し、承認した</done>
</task>

</tasks>

<verification>
**本番DB検証:**
- `npm run db:migrate` が正常に完了すること
- Drizzle Studio で `bgm_tracks` テーブルが表示されること
- テーブルに2件のレコードが存在すること
</verification>

<success_criteria>
1. マイグレーションが本番環境に適用されている
2. Drizzle Studio で `bgm_tracks` テーブルが確認できる
3. テーブルに2件の初期データが正しく登録されている
4. カラム構造が設計通りである（8カラム、制約含む）
</success_criteria>

<output>
After completion, create `.planning/phases/01-database/01-02-SUMMARY.md`
</output>
