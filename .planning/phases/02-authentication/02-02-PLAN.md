---
phase: 02-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/store/auth.ts
  - src/lib/auth.ts
  - src/hooks/useAuth.ts
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "クライアント側でユーザーのroleを取得できる"
    - "useAuthフックでisAdminを判定できる"
    - "authClientにadminClient pluginが設定されている"
  artifacts:
    - path: "src/core/store/auth.ts"
      provides: "AuthUser型 with role"
      contains: "role:"
    - path: "src/lib/auth.ts"
      provides: "authClient with adminClient plugin"
      contains: "adminClient()"
    - path: "src/hooks/useAuth.ts"
      provides: "useAuth with isAdmin"
      exports: ["isAdmin"]
  key_links:
    - from: "src/hooks/useAuth.ts"
      to: "src/core/store/auth.ts"
      via: "AuthUser type import"
      pattern: "import type { AuthUser }"
    - from: "src/hooks/useAuth.ts"
      to: "src/lib/auth.ts"
      via: "authClient.useSession()"
      pattern: "authClient\\.useSession"
---

<objective>
クライアント側でユーザーのroleを参照できるようにし、条件レンダリングで管理ボタンを表示できるようにする

Purpose: Phase 6のAdmin UIで管理者のみに管理ボタンを表示するための基盤を構築する
Output: AuthUser型拡張、authClient admin plugin、useAuth isAdmin
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/core/store/auth.ts (existing):
```typescript
export type AuthUser = {
  id: string
  email: string
  name: string
  image: string | null
  emailVerified: boolean
}
```

From src/lib/auth.ts (existing):
```typescript
import { createAuthClient } from 'better-auth/react'

export const authClient = createAuthClient({
  baseURL: window.location.origin,
})
```

From src/hooks/useAuth.ts (existing):
```typescript
import { useCallback } from 'react'
import { authClient } from '../lib/auth'
import type { AuthUser } from '../core/store/auth'

export function useAuth() {
  const { data: session, isPending } = authClient.useSession()

  const user: AuthUser | null = session?.user
    ? {
        id: session.user.id,
        email: session.user.email,
        name: session.user.name,
        image: session.user.image ?? null,
        emailVerified: session.user.emailVerified,
      }
    : null

  const login = useCallback(() => {
    authClient.signIn.social({ provider: 'google', callbackURL: '/' })
  }, [])

  const logout = useCallback(async () => {
    await authClient.signOut()
  }, [])

  return { user, loading: isPending, login, logout }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthUser型にroleを追加</name>
  <files>src/core/store/auth.ts</files>
  <read_first>
    - src/core/store/auth.ts
  </read_first>
  <action>
    src/core/store/auth.ts の AuthUser 型に role フィールドを追加する。

    変更後の型定義:
    ```typescript
    export type AuthUser = {
      id: string
      email: string
      name: string
      image: string | null
      emailVerified: boolean
      role: 'admin' | 'user'  // 追加
    }
    ```
  </action>
  <verify>
    <automated>grep -q "role: 'admin' | 'user'" src/core/store/auth.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/core/store/auth.ts の AuthUser 型に `role: 'admin' | 'user'` が存在する
  </acceptance_criteria>
  <done>AuthUser型にroleフィールドが追加されている</done>
</task>

<task type="auto">
  <name>Task 2: authClientにadminClient pluginを追加</name>
  <files>src/lib/auth.ts</files>
  <read_first>
    - src/lib/auth.ts
    - .planning/phases/02-authentication/02-RESEARCH.md
  </read_first>
  <action>
    src/lib/auth.ts で authClient に adminClient plugin を追加する。

    変更後の内容:
    ```typescript
    import { createAuthClient } from 'better-auth/react'
    import { adminClient } from 'better-auth/client/plugins'

    export const authClient = createAuthClient({
      baseURL: window.location.origin,
      plugins: [
        adminClient()  // 追加
      ]
    })
    ```

    注意: adminClient pluginはサーバー側のadmin pluginと対になるクライアント側の実装
  </action>
  <verify>
    <automated>grep -q "import { adminClient } from 'better-auth/client/plugins'" src/lib/auth.ts && grep -q "adminClient()" src/lib/auth.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/auth.ts に `import { adminClient } from 'better-auth/client/plugins'` が存在する
    - src/lib/auth.ts に `plugins: [adminClient()]` が存在する
  </acceptance_criteria>
  <done>authClientにadminClient pluginが追加されている</done>
</task>

<task type="auto">
  <name>Task 3: useAuthフックにisAdminを追加</name>
  <files>src/hooks/useAuth.ts</files>
  <read_first>
    - src/hooks/useAuth.ts
    - src/core/store/auth.ts
  </read_first>
  <action>
    src/hooks/useAuth.ts の useAuth フックを拡張して isAdmin を返すようにする。

    変更内容:
    1. user オブジェクト構築時に role を追加:
    ```typescript
    const user: AuthUser | null = session?.user
      ? {
          id: session.user.id,
          email: session.user.email,
          name: session.user.name,
          image: session.user.image ?? null,
          emailVerified: session.user.emailVerified,
          role: session.user.role ?? 'user',  // 追加
        }
      : null
    ```

    2. isAdmin 変数を追加:
    ```typescript
    const isAdmin = user?.role === 'admin'
    ```

    3. 戻り値に isAdmin を追加:
    ```typescript
    return {
      user,
      loading: isPending,
      login,
      logout,
      isAdmin,  // 追加
    }
    ```

    注意: session.user.role は Better Auth Admin Plugin が提供するフィールド
  </action>
  <verify>
    <automated>grep -q "role: session.user.role" src/hooks/useAuth.ts && grep -q "isAdmin" src/hooks/useAuth.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useAuth.ts の user オブジェクト構築に `role: session.user.role ?? 'user'` が存在する
    - src/hooks/useAuth.ts に `const isAdmin = user?.role === 'admin'` が存在する
    - src/hooks/useAuth.ts の return に `isAdmin` が含まれている
  </acceptance_criteria>
  <done>useAuthフックがisAdminを返すようになっている</done>
</task>

</tasks>

<verification>
Phase 2 Plan 2の完了条件:
- [ ] AuthUser型にroleフィールドが追加されている
- [ ] authClientにadminClient pluginが追加されている
- [ ] useAuthフックがisAdminを返すようになっている
</verification>

<success_criteria>
1. grep -q "role: 'admin' | 'user'" src/core/store/auth.ts が成功する
2. grep -q "adminClient()" src/lib/auth.ts が成功する
3. grep -q "isAdmin" src/hooks/useAuth.ts が成功する
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
