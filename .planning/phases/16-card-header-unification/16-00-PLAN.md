---
phase: 16-card-header-unification
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/timer/__tests__/card-header.test.ts
  - src/components/bgm/__tests__/card-header.test.ts
  - src/components/todos/__tests__/card-header.test.ts
autonomous: true
requirements:
  - HEADER-01
  - HEADER-02
  - HEADER-03
must_haves:
  truths:
    - "3つのテストファイルが存在し、vitestで認識される"
    - "各テストファイルにヘッダー統一を検証するテストケースが定義されている"
  artifacts:
    - path: "src/components/timer/__tests__/card-header.test.ts"
      provides: "TimerWidgetヘッダーのテストスケルトン"
      exports: ["describe", "it", "expect"]
    - path: "src/components/bgm/__tests__/card-header.test.ts"
      provides: "BgmPlayerヘッダーのテストスケルトン"
      exports: ["describe", "it", "expect"]
    - path: "src/components/todos/__tests__/card-header.test.ts"
      provides: "TodoListヘッダーのテストスケルトン"
      exports: ["describe", "it", "expect"]
  key_links:
    - from: "npm test -- --run"
      to: "テストファイル群"
      pattern: "PASS.*card-header.test.ts"
---

<objective>
Phase 16の実装前に、ヘッダー統一を検証するためのテストスケルトンを作成する。

Purpose: TDDのRED→GREEN→REFACTORサイクルに従い、先にテストを定義することで実装の完了基準を明確にする。
Output: 3つのコンポーネント（Timer, BGM, Todos）のカードヘッダーテストファイル。
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-card-header-unification/16-CONTEXT.md

<interfaces>
<!-- 統一ヘッダースタイル仕様（CONTEXT.md決定） -->
```tsx
// 全カード共通のヘッダーテキストスタイル
className="text-xs uppercase tracking-widest font-bold text-cf-text"

// ヘッダーコンテナ（左右配置）
className="flex items-center justify-between mb-4"
```

<!-- テストで検証すべき項目 -->
- ヘッダーテキストが存在する
- ヘッダーテキストのスタイルが統一されている
- flexレイアウトが適切である
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimerWidgetヘッダーテストスケルトン作成</name>
  <files>src/components/timer/__tests__/card-header.test.ts</files>
  <action>
TimerWidgetのヘッダー統一を検証するテストスケルトンを作成する。

1. `src/components/timer/__tests__/` ディレクトリが存在しない場合は作成
2. 以下の内容でテストファイルを作成:

```typescript
import { describe, it, expect } from 'vitest'

/**
 * Phase 16: TimerWidgetカードヘッダー統一テスト
 *
 * 検証項目:
 * - HEADER-01: ヘッダーに「Pomodoro」テキストが表示される
 * - ヘッダーテキストスタイルが統一されている
 * - flexレイアウトが適切である
 */

describe('TimerWidget Card Header', () => {
  it.todo('should display "Pomodoro" text in the header')
  it.todo('should have unified header text style: text-xs uppercase tracking-widest font-bold text-cf-text')
  it.todo('should have flex items-center justify-between mb-4 layout')
})
```

※ 現段階ではテストは `it.todo` でスキップ状態とし、Wave 1の実装後にGREENになるよう調整する。
</action>
  <verify>
    <automated>npm test -- --run src/components/timer/__tests__/card-header.test.ts 2>&1 | grep -E "(PASS|FAIL|TODO|tests)"</automated>
  </verify>
  <done>
    テストファイルが存在し、`npm test`で認識される。3つの`it.todo`テストケースが定義されている。
  </done>
</task>

<task type="auto">
  <name>Task 2: BgmPlayerヘッダーテストスケルトン作成</name>
  <files>src/components/bgm/__tests__/card-header.test.ts</files>
  <action>
BgmPlayerのヘッダー統一を検証するテストスケルトンを作成する。

1. `src/components/bgm/__tests__/` ディレクトリが存在しない場合は作成
2. 以下の内容でテストファイルを作成:

```typescript
import { describe, it, expect } from 'vitest'

/**
 * Phase 16: BgmPlayerカードヘッダー統一テスト
 *
 * 検証項目:
 * - HEADER-02: ヘッダーに「BGM」テキストが左上に表示される
 * - Listボタンが右上に配置される
 * - スペーサー要素が削除されている
 * - ヘッダーテキストスタイルが統一されている
 */

describe('BgmPlayer Card Header', () => {
  it.todo('should display "BGM" text in the header on the left')
  it.todo('should have List button on the right')
  it.todo('should not have spacer element (className="w-5")')
  it.todo('should have unified header text style: text-xs uppercase tracking-widest font-bold text-cf-text')
  it.todo('should have flex items-center justify-between mb-4 layout')
})
```
</action>
  <verify>
    <automated>npm test -- --run src/components/bgm/__tests__/card-header.test.ts 2>&1 | grep -E "(PASS|FAIL|TODO|tests)"</automated>
  </verify>
  <done>
    テストファイルが存在し、`npm test`で認識される。5つの`it.todo`テストケースが定義されている。
  </done>
</task>

<task type="auto">
  <name>Task 3: TodoListヘッダーテストスケルトン作成</name>
  <files>src/components/todos/__tests__/card-header.test.ts</files>
  <action>
TodoListのヘッダー統一を検証するテストスケルトンを作成する。

1. `src/components/todos/__tests__/` ディレクトリが存在しない場合は作成
2. 以下の内容でテストファイルを作成:

```typescript
import { describe, it, expect } from 'vitest'

/**
 * Phase 16: TodoListカードヘッダー統一テスト
 *
 * 検証項目:
 * - HEADER-03: ヘッダーに「Tasks」テキストが表示される
 * - CheckSquareアイコンが削除されている
 * - ヘッダーテキストスタイルが統一されている
 * - バッジが右上に維持されている
 */

describe('TodoList Card Header', () => {
  it.todo('should display "Tasks" text in the header')
  it.todo('should not have CheckSquare icon')
  it.todo('should have unified header text style: text-xs uppercase tracking-widest font-bold text-cf-text')
  it.todo('should have remaining todos badge on the right')
  it.todo('should have flex items-center justify-between mb-4 layout')
})
```
</action>
  <verify>
    <automated>npm test -- --run src/components/todos/__tests__/card-header.test.ts 2>&1 | grep -E "(PASS|FAIL|TODO|tests)"</automated>
  </verify>
  <done>
    テストファイルが存在し、`npm test`で認識される。5つの`it.todo`テストケースが定義されている。
  </done>
</task>

</tasks>

<verification>
## 自動テスト

```bash
# 全テストファイルの認識確認
npm test -- --run 2>&1 | grep -E "card-header"

# テストファイルの存在確認
ls -la src/components/timer/__tests__/card-header.test.ts
ls -la src/components/bgm/__tests__/card-header.test.ts
ls -la src/components/todos/__tests__/card-header.test.ts
```
</verification>

<success_criteria>
1. 3つのテストファイルがそれぞれのディレクトリに存在する
2. `npm test -- --run` でテストが実行され、すべてのテストケースが`it.todo`として認識される
3. テストケース数: Timer 3件、BGM 5件、Todos 5件
</success_criteria>

<output>
After completion, create `.planning/phases/16-card-header-unification/16-00-SUMMARY.md`
</output>
