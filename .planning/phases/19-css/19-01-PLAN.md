---
phase: 19-css
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.css]
autonomous: true
requirements: [BGMAN-01, BGMAN-02]

must_haves:
  truths:
    - "index.cssに@keyframes blinkが定義され、opacity 0.6-1.0、scale 1-1.02で2s周期のアニメーションが動作する"
    - "index.cssに@keyframes pulse-glowが定義され、3層box-shadowでglowエフェクトが広がる"
    - "rotate @keyframesと.album-art-spinningクラスが削除されている"
    - ".album-art-pausedクラスは維持されている"
  artifacts:
    - path: "src/index.css"
      provides: "BGMアニメーション用CSS keyframes"
      contains: "@keyframes blink"
      contains: "@keyframes pulse-glow"
      not_contains: "@keyframes rotate"
      not_contains: ".album-art-spinning"
  key_links:
    - from: "@keyframes blink"
      to: "AlbumArtコンポーネント"
      via: "album-art-blinkingクラス（Phase 20で適用）"
      pattern: "animation: blink 2s ease-in-out infinite"
    - from: "@keyframes pulse-glow"
      to: "AlbumArtコンポーネント"
      via: "album-art-pulsingクラス（Phase 20で適用）"
      pattern: "animation: pulse-glow 2s ease-in-out infinite"
---

<objective>
index.cssにblinkとpulse-glowの@keyframesを定義し、不要なrotateアニメーションを削除する。

Purpose: BGM再生中の「鼓動」を表現するアニメーション基盤を提供。光感受性エピレプシー対策を考慮した緩やかな点滅とパルスエフェクト。
Output: index.cssに2つの新規@keyframes定義、1つの削除
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-css/19-CONTEXT.md

<interfaces>
<!-- 既存のindex.css構造（確認済み） -->

現在のアニメーション定義位置: 211-226行目付近
```css
/* Rotating album art */
@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.album-art-spinning {
  animation: rotate 8s linear infinite;
}

.album-art-paused {
  animation-play-state: paused;
}
```

このブロック全体を新しい定義に置換する。

参考パターン（既存の@keyframes bounce）:
```css
@keyframes bounce {
  0%, 100% { transform: scaleY(0.3); }
  50% { transform: scaleY(1); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: blink @keyframes定義</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css（現在の@keyframes定義位置を確認）
  </read_first>
  <action>
    index.cssの`/* Rotating album art */`セクション（211-226行目付近）を以下の内容に置換する:

    ```css
    /* Album art breathing animation - Phase 19 */
    @keyframes blink {
      0%, 100% {
        opacity: 0.6;
        transform: scale(1);
      }
      50% {
        opacity: 1.0;
        transform: scale(1.02);
      }
    }

    .album-art-blinking {
      animation: blink 2s ease-in-out infinite;
    }

    .album-art-paused {
      animation-play-state: paused;
    }
    ```

    **重要な値（CONTEXT.mdの決定事項）:**
    - 3点構造: 0%, 50%, 100%
    - opacity: 0.6（最小）→ 1.0（最大）
    - scale: 1 → 1.02
    - duration: 2s
    - easing: ease-in-out
    - iteration: infinite

    **削除対象:**
    - `@keyframes rotate`ブロック全体
    - `.album-art-spinning`クラス定義

    **維持対象:**
    - `.album-art-paused`クラス（新アニメーションでも使用）
  </action>
  <verify>
    <automated>grep -c "@keyframes blink" src/index.css | xargs -I {} test {} -eq 1 && echo "PASS: blink keyframes exists" || echo "FAIL: blink keyframes missing"</automated>
    <automated>grep -c "@keyframes rotate" src/index.css | xargs -I {} test {} -eq 0 && echo "PASS: rotate removed" || echo "FAIL: rotate still exists"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.cssに`@keyframes blink`が存在する
    - src/index.cssに`opacity: 0.6`と`opacity: 1.0`が含まれる
    - src/index.cssに`transform: scale(1)`と`transform: scale(1.02)`が含まれる
    - src/index.cssに`animation: blink 2s ease-in-out infinite`が含まれる
    - src/index.cssに`@keyframes rotate`が存在しない
    - src/index.cssに`.album-art-spinning`が存在しない
    - src/index.cssに`.album-art-paused`が存在する
  </acceptance_criteria>
  <done>blink @keyframesが定義され、rotate @keyframesとalbum-art-spinningが削除されている</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: pulse-glow @keyframes定義</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css（Task 1で追加したblink定義の直後を確認）
  </read_first>
  <action>
    Task 1で追加した`.album-art-paused`クラスの直後に、pulse-glow定義を追加する:

    ```css
    /* Album art glow pulse animation - Phase 19 */
    @keyframes pulse-glow {
      0%, 100% {
        box-shadow:
          0 0 0px currentColor,
          0 0 8px currentColor,
          0 0 16px currentColor;
      }
      50% {
        box-shadow:
          0 0 4px currentColor,
          0 0 12px currentColor,
          0 0 24px currentColor;
      }
    }

    .album-art-pulsing {
      animation: pulse-glow 2s ease-in-out infinite;
    }
    ```

    **重要な値（CONTEXT.mdの決定事項）:**
    - 3層構造のbox-shadow:
      - 内層: 0px（0%時）/ 4px（50%時）
      - 中層: 8px（0%時）/ 12px（50%時）
      - 外層: 16px（0%時）/ 24px（50%時）
    - blur-radius: 0（固定、spread-radiusのみ変化）
    - 色: `currentColor`（動的なトラック色を継承）
    - duration: 2s（blinkと同期）
    - easing: ease-in-out
    - iteration: infinite

    **注意:**
    - `currentColor`を使用することで、コンポーネント側で設定されたcolorプロパティが自動的に適用される
    - Phase 20でAlbumArtコンポーネントにcolorスタイルを設定する必要がある
  </action>
  <verify>
    <automated>grep -c "@keyframes pulse-glow" src/index.css | xargs -I {} test {} -eq 1 && echo "PASS: pulse-glow keyframes exists" || echo "FAIL: pulse-glow keyframes missing"</automated>
    <automated>grep -c "album-art-pulsing" src/index.css | xargs -I {} test {} -eq 1 && echo "PASS: album-art-pulsing class exists" || echo "FAIL: album-art-pulsing class missing"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.cssに`@keyframes pulse-glow`が存在する
    - src/index.cssに`box-shadow:`が3回含まれる（0%, 50%の各keyframe内）
    - src/index.cssに`currentColor`が含まれる
    - src/index.cssに`animation: pulse-glow 2s ease-in-out infinite`が含まれる
    - src/index.cssに`.album-art-pulsing`クラスが存在する
  </acceptance_criteria>
  <done>pulse-glow @keyframesが定義され、3層box-shadow構造でglowエフェクトが実装されている</done>
</task>

</tasks>

<verification>
## Phase 19 Verification

1. **blink @keyframes確認:**
   ```bash
   grep -A 12 "@keyframes blink" src/index.css
   ```
   期待値: 0%/50%/100%キーフレーム、opacity 0.6-1.0、scale 1-1.02

2. **pulse-glow @keyframes確認:**
   ```bash
   grep -A 16 "@keyframes pulse-glow" src/index.css
   ```
   期待値: 3層box-shadow、currentColor使用、2s duration

3. **削除確認:**
   ```bash
   grep -c "@keyframes rotate" src/index.css  # 期待値: 0
   grep -c ".album-art-spinning" src/index.css  # 期待値: 0
   ```

4. **維持確認:**
   ```bash
   grep -c ".album-art-paused" src/index.css  # 期待値: 1
   ```
</verification>

<success_criteria>
1. index.cssに`@keyframes blink`が定義され、`opacity: 0.6`/`1.0`、`scale(1)`/`scale(1.02)`、`2s`周期で変化する
2. index.cssに`@keyframes pulse-glow`が定義され、3層`box-shadow`（0px/8px/16px → 4px/12px/24px）でglowエフェクトが広がる
3. `@keyframes rotate`と`.album-art-spinning`が削除されている
4. `.album-art-paused`クラスが維持されている
</success_criteria>

<output>
After completion, create `.planning/phases/19-css/19-01-SUMMARY.md`
</output>
