---
phase: 06-admin-ui
plan: 03
type: execute
wave: 2
depends_on: ["06-00", "06-01", "06-02"]
files_modified:
  - src/components/bgm/EditTrackDialog.tsx
  - src/components/bgm/DeleteConfirmDialog.tsx
  - src/components/bgm/AdminModal.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
must_haves:
  truths:
    - 編集ダイアログが開き、既存値が表示される
    - フォーム入力でトラック情報を更新できる
    - 更新成功時にトースト通知が表示され、リストが更新される
    - 削除確認ダイアログが開き、確認メッセージが表示される
    - 削除実行時にトースト通知が表示され、リストから削除される
    - TrackItemから編集・削除ダイアログが開く
  artifacts:
    - path: src/components/bgm/EditTrackDialog.tsx
      provides: トラック編集ダイアログ
      min_lines: 90
      exports: ["EditTrackDialog"]
    - path: src/components/bgm/DeleteConfirmDialog.tsx
      provides: 削除確認ダイアログ
      min_lines: 60
      exports: ["DeleteConfirmDialog"]
    - path: src/components/bgm/AdminModal.tsx
      provides: AdminModal（TrackList/EditTrackDialog/DeleteConfirmDialog統合）
      min_lines: 100
      exports: ["AdminModal"]
  key_links:
    - from: src/components/bgm/EditTrackDialog.tsx
      to: src/app/routers/bgm.ts
      via: trpc.bgm.update.useMutation
      pattern: "trpc\.bgm\.update\.useMutation"
    - from: src/components/bgm/DeleteConfirmDialog.tsx
      to: src/app/routers/bgm.ts
      via: trpc.bgm.delete.useMutation
      pattern: "trpc\.bgm\.delete\.useMutation"
    - from: src/components/bgm/TrackItem.tsx
      to: src/components/bgm/EditTrackDialog.tsx
      via: 編集ボタンクリックでshowEditDialog stateをtrue
      pattern: "setShowEditDialog\(true\)"
    - from: src/components/bgm/TrackItem.tsx
      to: src/components/bgm/DeleteConfirmDialog.tsx
      via: 削除ボタンクリックでshowDeleteConfirm stateをtrue
      pattern: "setShowDeleteConfirm\(true\)"
---

<objective>
トラック編集（EditTrackDialog）と削除確認（DeleteConfirmDialog）を実装し、AdminModalを完成させる

Purpose: 管理者がトラック情報を編集・削除できるUIを実装
Output: EditTrackDialog、DeleteConfirmDialog、完成したAdminModal
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-ui/06-CONTEXT.md
@.planning/phases/06-admin-ui/06-RESEARCH.md
@.planning/phases/06-admin-ui/06-UI-SPEC.md
@src/components/dialogs/LoginDialog.tsx
@src/components/dialogs/MigrateDialog.tsx
@src/components/bgm/AddTrackForm.tsx
@src/app/routers/bgm.ts
@src/core/store/ui.ts
@src/lib/animation.ts
@src/test/setup.ts
@src/components/bgm/EditTrackDialog.test.tsx
@src/components/bgm/DeleteConfirmDialog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditTrackDialogコンポーネントを実装</name>
  <files>src/components/bgm/EditTrackDialog.tsx</files>
  <read_first>
    - src/components/dialogs/LoginDialog.tsx
    - src/components/bgm/AddTrackForm.tsx
    - src/app/routers/bgm.ts
  </read_first>
  <behavior>
    - Test 1: 既存値がフォームに表示される
    - Test 2: フォーム入力が正しく動作する
    - Test 3: 送信時にupdate mutationが呼ばれる
    - Test 4: 送信成功時にトースト通知が表示され、onCloseが呼ばれる
    - Test 5: エラー時にエラートーストが表示される
    - Test 6: キャンセルボタンでonCloseが呼ばれる
  </behavior>
  <action>
  EditTrackDialog.tsxファイルを作成し、以下の実装を行う：

  1. **ファイル作成とインポート**:
     ```typescript
     import { useState, useEffect } from 'react'
     import { createPortal } from 'react-dom'
     import { motion, AnimatePresence } from 'framer-motion'
     import { X } from 'lucide-react'
     import { trpc } from '@/lib/trpc'
     import { useUiStore } from '@/core/store/ui'
     import { tapAnimation } from '@/lib/animation'
     import type { BgmTrack, UpdateBgmTrack } from '@/app/routers/_shared'

     interface EditTrackDialogProps {
       track: BgmTrack
       onClose: () => void
     }
     ```

  2. **コンポーネント実装（stateとmutation）**:
     ```typescript
     export function EditTrackDialog({ track, onClose }: EditTrackDialogProps) {
       const addToast = useUiStore(state => state.addToast)
       const utils = trpc.useUtils()

       const [title, setTitle] = useState(track.title)
       const [artist, setArtist] = useState(track.artist || '')
       const [color, setColor] = useState(track.color || '#3b82f6')
       const [tier, setTier] = useState<'free' | 'premium'>(track.tier)

       // track変更時にフォームをリセット
       useEffect(() => {
         setTitle(track.title)
         setArtist(track.artist || '')
         setColor(track.color || '#3b82f6')
         setTier(track.tier)
       }, [track])

       const updateMutation = trpc.bgm.update.useMutation({
         onSuccess: () => {
           addToast('トラックを更新しました', 'success')
           utils.bgm.getAll.invalidate()
           onClose()
         },
         onError: (error) => {
           addToast(error.message, 'error')
         }
       })
     ```

  3. **送信ハンドラー実装**:
     ```typescript
       const handleSubmit = async (e: React.FormEvent) => {
         e.preventDefault()

         const data: UpdateBgmTrack = {
           id: track.id,
           title,
           artist,
           color,
           tier,
         }

         await updateMutation.mutateAsync(data)
       }
     ```

  4. **JSX実装（createPortal + モーダル）**:
     ```typescript
       return createPortal(
         <AnimatePresence>
           {track && (
             <div className="fixed inset-0 z-widget-modal flex items-center justify-center bg-black/50 backdrop-blur-sm">
               <motion.div
                 initial={{ opacity: 0, scale: 0.9, y: 20 }}
                 animate={{ opacity: 1, scale: 1, y: 0 }}
                 exit={{ opacity: 0, scale: 0.9, y: 20 }}
                 transition={{ duration: 0.2, ease: 'easeOut' }}
                 className="widget p-6 w-full max-w-md m-4"
               >
                 {/* Header */}
                 <div className="flex items-center justify-between mb-6">
                   <h3 className="text-lg font-bold text-cf-text">トラック編集</h3>
                   <motion.button
                     {...tapAnimation}
                     onClick={onClose}
                     className="text-cf-subtext hover:text-cf-text transition-colors cursor-pointer"
                   >
                     <X size={20} />
                   </motion.button>
                 </div>

                 {/* Form */}
                 <form onSubmit={handleSubmit} className="flex flex-col gap-4">
                   {/* Title */}
                   <div className="flex flex-col gap-1.5">
                     <label className="text-xs text-cf-subtext">曲名</label>
                     <input
                       type="text"
                       required
                       value={title}
                       onChange={(e) => setTitle(e.target.value)}
                       className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
                     />
                   </div>

                   {/* Artist */}
                   <div className="flex flex-col gap-1.5">
                     <label className="text-xs text-cf-subtext">アーティスト</label>
                     <input
                       type="text"
                       value={artist}
                       onChange={(e) => setArtist(e.target.value)}
                       className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
                     />
                   </div>

                   {/* Color */}
                   <div className="flex flex-col gap-1.5">
                     <label className="text-xs text-cf-subtext">色</label>
                     <div className="flex items-center gap-2">
                       <input
                         type="color"
                         value={color}
                         onChange={(e) => setColor(e.target.value)}
                         className="w-10 h-10 rounded cursor-pointer"
                       />
                       <span className="text-sm text-cf-text">{color}</span>
                     </div>
                   </div>

                   {/* Tier */}
                   <div className="flex flex-col gap-1.5">
                     <label className="text-xs text-cf-subtext">Tier</label>
                     <select
                       value={tier}
                       onChange={(e) => setTier(e.target.value as 'free' | 'premium')}
                       className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
                     >
                       <option value="free">Free</option>
                       <option value="premium">Premium</option>
                     </select>
                   </div>

                   {/* Buttons */}
                   <div className="flex gap-2 mt-2">
                     <motion.button
                       {...tapAnimation}
                       type="button"
                       onClick={onClose}
                       disabled={updateMutation.isPending}
                       className="flex-1 bg-cf-surface hover:bg-cf-surface1 text-cf-text px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
                     >
                       キャンセル
                     </motion.button>
                     <motion.button
                       {...tapAnimation}
                       type="submit"
                       disabled={updateMutation.isPending}
                       className="flex-1 bg-cf-primary hover:bg-cf-primary/90 text-white px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
                     >
                       {updateMutation.isPending ? '更新中...' : '更新'}
                     </motion.button>
                   </div>
                 </form>
               </motion.div>
             </div>
           )}
         </AnimatePresence>,
         document.body
       )
     }
     ```

  **AddTrackFormパターンの再利用**:
  - フォーム入力スタイルはAddTrackFormと同じ
  - `trpc.bgm.update.useMutation`を使用
  - 成功時に`utils.bgm.getAll.invalidate()`でリスト更新

  **確認事項**:
  - `useEffect`でtrack変更時にフォーム値をリセット
  - `trpc.bgm.update.useMutation`を使用
  - ファイルアップロードなし（編集不可項目）
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/EditTrackDialog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/EditTrackDialog.tsxが存在する
    - EditTrackDialogコンポーネントがexportされている
    - `trpc.bgm.update.useMutation`を使用している
    - `useEffect`でtrack変更時にフォーム値をリセットしている
    - ファイルアップロードフィールドがない（編集不可）
    - 成功時に`utils.bgm.getAll.invalidate()`を呼んでいる
    - テスト: `npm test -- src/components/bgm/EditTrackDialog.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - 既存値が正しく表示される
  - フォーム更新が正しく動作する
  - 更新成功時にトースト通知が表示され、リストが更新される
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeleteConfirmDialogコンポーネントを実装</name>
  <files>src/components/bgm/DeleteConfirmDialog.tsx</files>
  <read_first>
    - src/components/dialogs/MigrateDialog.tsx
    - src/app/routers/bgm.ts
  </read_first>
  <behavior>
    - Test 1: 削除確認メッセージが表示される（「{title}」を削除しますか？）
    - Test 2: 削除ボタンクリックでdelete mutationが呼ばれる
    - Test 3: 削除成功時にトースト通知が表示され、onCloseが呼ばれる
    - Test 4: エラー時にエラートーストが表示される
    - Test 5: キャンセルボタンでonCloseが呼ばれる
  </behavior>
  <action>
  DeleteConfirmDialog.tsxファイルを作成し、以下の実装を行う：

  1. **ファイル作成とインポート**:
     ```typescript
     import { createPortal } from 'react-dom'
     import { motion, AnimatePresence } from 'framer-motion'
     import { trpc } from '@/lib/trpc'
     import { useUiStore } from '@/core/store/ui'
     import { tapAnimation } from '@/lib/animation'
     import type { BgmTrack } from '@/app/routers/_shared'

     interface DeleteConfirmDialogProps {
       track: BgmTrack
       onClose: () => void
     }
     ```

  2. **コンポーネント実装**:
     ```typescript
     export function DeleteConfirmDialog({ track, onClose }: DeleteConfirmDialogProps) {
       const addToast = useUiStore(state => state.addToast)
       const utils = trpc.useUtils()

       const deleteMutation = trpc.bgm.delete.useMutation({
         onSuccess: () => {
           addToast('トラックを削除しました', 'success')
           utils.bgm.getAll.invalidate()
           onClose()
         },
         onError: (error) => {
           addToast(error.message, 'error')
         }
       })

       const handleDelete = async () => {
         await deleteMutation.mutateAsync({ id: track.id })
       }

       return createPortal(
         <AnimatePresence>
           <div className="fixed inset-0 z-widget-modal flex items-center justify-center bg-black/50 backdrop-blur-sm">
             <motion.div
               initial={{ opacity: 0, scale: 0.9, y: 20 }}
               animate={{ opacity: 1, scale: 1, y: 0 }}
               exit={{ opacity: 0, scale: 0.9, y: 20 }}
               transition={{ duration: 0.2, ease: 'easeOut' }}
               className="widget p-6 w-full max-w-md m-4"
             >
               <h3 className="text-lg font-bold text-cf-text mb-2">確認</h3>
               <p className="text-cf-subtext mb-6">
                 「{track.title}」を削除しますか？
               </p>

               <div className="flex gap-2">
                 <motion.button
                   {...tapAnimation}
                   onClick={onClose}
                   disabled={deleteMutation.isPending}
                   className="flex-1 bg-cf-surface hover:bg-cf-surface1 text-cf-text px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
                 >
                   キャンセル
                 </motion.button>
                 <motion.button
                   {...tapAnimation}
                   onClick={handleDelete}
                   disabled={deleteMutation.isPending}
                   className="flex-1 bg-cf-danger hover:bg-cf-danger/90 text-white px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
                 >
                   {deleteMutation.isPending ? '削除中...' : '削除'}
                 </motion.button>
               </div>
             </motion.div>
           </div>
         </AnimatePresence>,
         document.body
       )
     }
     ```

  **MigrateDialogパターンの再利用**:
  - 確認ダイアログの構造
  - `createPortal` + `AnimatePresence` + `motion.div`
  - 破壊的アクションのボタンスタイル（`bg-cf-danger`）

  **UI-SPEC準拠**:
  - 確認メッセージ: 「{title}」を削除しますか？
  - 削除ボタン: `bg-cf-danger hover:bg-cf-danger/90`

  **確認事項**:
  - `trpc.bgm.delete.useMutation`を使用
  - 成功時に`utils.bgm.getAll.invalidate()`でリスト更新
  - 削除ボタンは破壊的アクションスタイル（赤色）
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/DeleteConfirmDialog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/DeleteConfirmDialog.tsxが存在する
    - DeleteConfirmDialogコンポーネントがexportされている
    - `trpc.bgm.delete.useMutation`を使用している
    - 確認メッセージ「{title}」を削除しますか？」が含まれる
    - 削除ボタンに`bg-cf-danger`クラスが含まれる
    - 成功時に`utils.bgm.getAll.invalidate()`を呼んでいる
    - テスト: `npm test -- src/components/bgm/DeleteConfirmDialog.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - 確認メッセージが正しく表示される
  - 削除実行が正しく動作する
  - 削除成功時にトースト通知が表示され、リストが更新される
  </done>
</task>

<task type="auto">
  <name>Task 3: AdminModalにTrackList/AddTrackFormを統合</name>
  <files>src/components/bgm/AdminModal.tsx</files>
  <read_first>
    - src/components/bgm/AdminModal.tsx
    - src/components/bgm/TrackList.tsx
    - src/components/bgm/AddTrackForm.tsx
  </read_first>
  <behavior>
    - Test 1: mode='list'時にTrackListが表示される
    - Test 2: mode='add'時にAddTrackFormが表示される
    - Test 3: mode='edit'時にEditTrackDialogが表示される（TrackItemから開く）
    - Test 4: TrackListの追加ボタンクリックでmodeが'add'に切り替わる
    - Test 5: AddTrackFormのonBackでmodeが'list'に戻る
  </behavior>
  <action>
  AdminModal.tsxを更新し、TrackListとAddTrackFormを統合：

  1. **インポート追加**:
     ```typescript
     import { TrackList } from './TrackList'
     import { AddTrackForm } from './AddTrackForm'
     ```

  2. **コンテンツ部分を更新**:
     ```typescript
     export function AdminModal({ isOpen, onClose }: AdminModalProps) {
       const [mode, setMode] = useState<Mode>('list')

       // 既存のuseEffectはそのまま

       return createPortal(
         <AnimatePresence>
           {isOpen && (
             <div
               className="fixed inset-0 z-widget-modal flex items-center justify-center bg-black/50 backdrop-blur-sm"
               onClick={onClose}
             >
               <motion.div
                 initial={{ opacity: 0, scale: 0.9, y: 20 }}
                 animate={{ opacity: 1, scale: 1, y: 0 }}
                 exit={{ opacity: 0, scale: 0.9, y: 20 }}
                 transition={{ duration: 0.2, ease: 'easeOut' }}
                 className="widget p-6 w-full max-w-2xl m-4"
                 onClick={(e) => e.stopPropagation()}
               >
                 {/* Header */}
                 <div className="flex items-center justify-between mb-6">
                   <h3 className="text-lg font-bold text-cf-text">BGM管理</h3>
                   <motion.button
                     {...tapAnimation}
                     onClick={onClose}
                     className="text-cf-subtext hover:text-cf-text transition-colors cursor-pointer"
                   >
                     <X size={20} />
                   </motion.button>
                 </div>

                 {/* Content - modeによる切り替え */}
                 {mode === 'list' && (
                   <TrackList onAdd={() => setMode('add')} />
                 )}
                 {mode === 'add' && (
                   <AddTrackForm onBack={() => setMode('list')} />
                 )}
                 {mode === 'edit' && (
                   <div className="text-cf-subtext text-center py-8">
                     編集ダイアログはTrackItemから直接開きます
                   </div>
                 )}
               </motion.div>
             </div>
           )}
         </AnimatePresence>,
         document.body
       )
     }
     ```

  **mode切り替えロジック**:
  - `'list'` → `'add'`: TrackListの追加ボタンクリック
  - `'add'` → `'list'`: AddTrackFormの戻るボタンクリック
  - `'edit'`モードはTrackItemからEditTrackDialogが直接開くため、AdminModal側ではプレースホルダー表示

  **確認事項**:
  - TrackListを`onAdd={() => setMode('add')}`で統合
  - AddTrackFormを`onBack={() => setMode('list')}`で統合
  - mode切り替えでコンテンツが正しく切り替わる
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/AdminModal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AdminModal.tsxにTrackList、AddTrackFormのインポートがある
    - mode='list'時にTrackListが表示される
    - mode='add'時にAddTrackFormが表示される
    - TrackListのonAddコールバックで`setMode('add')`が呼ばれる
    - AddTrackFormのonBackコールバックで`setMode('list')`が呼ばれる
    - テスト: `npm test -- src/components/bgm/AdminModal.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - AdminModalでTrackListが表示される
  - 追加ボタンクリックでAddTrackFormが表示される
  - 戻るボタンクリックでTrackListに戻る
  </done>
</task>

</tasks>

<verification>
1. TrackItemの編集ボタンクリックでEditTrackDialogが開く
2. EditTrackDialogでトラック情報を更新できる
3. 更新成功後、リストが更新される
4. TrackItemの削除ボタンクリックでDeleteConfirmDialogが開く
5. 削除実行後、リストから削除される
6. すべてのテストがパスする
</verification>

<success_criteria>
- トラック編集が機能する（UI-05）
- 削除確認ダイアログが機能する（UI-06）
- テストがすべてパスする
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui/06-03-SUMMARY.md`
</output>
