---
phase: 09-stats
plan: 03
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - src/components/stats/StatsCard.tsx
autonomous: true
requirements:
  - STAT-05
user_setup: []

must_haves:
  truths:
    - "Weekタブで累積集中時間の折れ線グラフが表示される"
    - "棒グラフと折れ線グラフが複合表示される（ComposedChart）"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "ComposedChartによる複合グラフ"
      contains: "ComposedChart"
      exports: ["default"]
  key_links:
    - from: "src/components/stats/StatsCard.tsx"
      to: "recharts"
      via: "import of ComposedChart, Line, YAxis (yAxisId)"
      pattern: "import.*Line.*from 'recharts'"
---

## Objective

週次統計に折れ線グラフを追加し、棒グラフと折れ線グラフを複合表示する。

## Purpose

ユーザーが日別セッション数（棒グラフ）と累積集中時間の推移（折れ線グラフ）を同時に確認できるようにする。RechartsのComposedChartを使用して2つのY軸を持つ複合グラフを実装する。

## Output

- WeekタブにComposedChartによる複合グラフが表示される
- 棒グラフ（左Y軸）: 日別セッション数
- 折れ線グラフ（右Y軸）: 累積集中時間

## Execution Context

@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-stats/09-CONTEXT.md
@.planning/phases/09-stats/09-RESEARCH.md
@.planning/phases/09-stats/09-UI-SPEC.md
@src/components/stats/StatsCard.tsx

## Interfaces

From src/components/stats/StatsCard.tsx (after Plan 02):
```typescript
interface WeeklyData {
  date: string
  sessions: number
}
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: 累積集中時間の計算ロジックを実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Plan 02のweeklyData集計ロジックを確認するため）
  </read_first>
  <behavior>
    STAT-05: 各日付までの累積集中時間が計算される
    累積計算: その日を含むそれ以前の日の合計
  </behavior>
  <action>
    weeklyDataのデータ構造を拡張し、累積集中時間を計算するロジックを追加する。

    実装内容：
    1. WeeklyDataインターフェースにcumulativeMinutesフィールドを追加
    2. データ変換時に累積計算を実行
    3. reduce関数を使用して各日付までの合計を計算

    データ変換ロジック:
    ```tsx
    interface WeeklyData {
      date: string
      sessions: number
      cumulativeMinutes: number
    }

    const weeklyData: WeeklyData[] = []
    let cumulativeTotal = 0

    for (let i = 6; i >= 0; i--) {
      const date = new Date()
      date.setDate(date.getDate() - i)
      const dateStr = date.toDateString()

      const daySessions = sessions.filter(
        s => s.type === 'work' && s.completedAt !== null && new Date(s.startedAt).toDateString() === dateStr
      )

      const dayMinutes = daySessions.reduce((sum, s) => sum + Math.floor(s.durationSecs / 60), 0)
      cumulativeTotal += dayMinutes

      weeklyData.push({
        date: getDayLabel(dateStr),
        sessions: daySessions.length,
        cumulativeMinutes: cumulativeTotal
      })
    }
    ```

    注: 累積計算は「その日までの合計」を計算する。各日で累積合計を更新する。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-05" --run</automated>
  </verify>
  <done>
    - WeeklyDataインターフェースにcumulativeMinutesフィールドが含まれている
    - weeklyData配列の各要素に正しい累積集中時間が含まれている
    - 累積計算が単調増加している（各日の値が前日以上である）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComposedChartで複合グラフを実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Task 1の変更を確認するため）
    - .planning/phases/09-stats/09-RESEARCH.md（ComposedChartパターンを確認するため）
  </read_first>
  <behavior>
    STAT-05: 折れ線グラフが累積集中時間を表示する
    STAT-04: 棒グラフが日別セッション数を表示する
    ComposedChart: 2つのY軸（左: セッション数、右: 累積時間）
  </behavior>
  <action>
    BarChartをComposedChartに置き換え、棒グラフと折れ線グラフを複合表示する。

    実装内容：
    1. BarChartをComposedChartに置き換え
    2. 2つのYAxisを定義（左: セッション数、右: 累積時間）
    3. BarコンポーネントにyAxisId="left"を設定
    4. Lineコンポーネントを追加し、yAxisId="right"を設定

    ComposedChart描画:
    ```tsx
    import { ComposedChart, Bar, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'

    <ResponsiveContainer width="100%" height={200}>
      <ComposedChart data={weeklyData}>
        <XAxis dataKey="date" />
        <YAxis yAxisId="left" />
        <YAxis yAxisId="right" orientation="right" />
        <Tooltip />
        <Bar yAxisId="left" dataKey="sessions" fill="#22c55e" name="セッション数" />
        <Line yAxisId="right" type="monotone" dataKey="cumulativeMinutes" stroke="#22c55e" strokeWidth={2} name="累積時間(分)" />
      </ComposedChart>
    </ResponsiveContainer>
    ```

    注: type="monotone"は滑らかな曲線を描画するオプション。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-05" --run</automated>
  </verify>
  <done>
    - WeekタブにComposedChartが表示される
    - 棒グラフ（左Y軸）で日別セッション数が表示される
    - 折れ線グラフ（右Y軸）で累積集中時間が表示される
    - 2つのY軸が正しく表示される
    - src/components/stats/StatsCard.test.tsのSTAT-05テストがパスする
  </done>
</task>

## Verification

1. 複合グラフが正しく表示される
   - npm run devでグラフが描画されることを目視確認
   - 棒グラフと折れ線グラフが同時に表示されている

2. 累積計算が正しい
   - 折れ線グラフが単調増加している
   - 各日の累積値が「その日までの合計」になっている

## Success Criteria

1. WeekタブにComposedChartが表示される
2. 棒グラフと折れ線グラフが複合表示されている
3. 2つのY軸が正しく表示されている
4. 累積集中時間が正しく計算されている

## Output

完了後、`.planning/phases/09-stats/09-03-SUMMARY.md`を作成する
