---
phase: 22-graph-ui-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/stats/StatsCard.tsx
autonomous: false
requirements:
  - STAT-02
  - STAT-04
user_setup: []
must_haves:
  truths:
    - "グラフサイズと余白が適切に調整され、ラベルが途切れず表示される"
    - "軸ラベルの色、サイズ、太さがshadcn/uiのmutedスタイルに準拠している"
    - "モバイルとデスクトップ両方でグラフが適切に表示される"
    - "キーボードナビゲーションとスクリーンリーダー対応が有効化されている"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "改善されたグラフUI（サイズ、余白、軸スタイル）"
      contains: "ComposedChart with accessibilityLayer, XAxis/YAxis with tickLine/axisLine false"
  key_links:
    - from: "src/components/stats/StatsCard.tsx"
      to: "Recharts ComposedChart"
      via: "accessibilityLayer prop"
      pattern: "accessibilityLayer={true}"
    - from: "src/components/stats/StatsCard.tsx"
      to: "XAxis/YAxis components"
      via: "tickLine and axisLine props"
      pattern: "tickLine={false}.*axisLine={false}"
---

<objective>
グラフの視認性を向上させ、shadcn/uiデザインシステムに準拠したスタイルを適用する

Purpose: ユーザーが統計データをより読みやすく、アクセシビリティの高いグラフで確認できるようにする
Output: 改善されたWeekタブのグラフUI（適切なサイズ・余白・ミニマルな軸スタイル・アクセシビリティ対応）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-graph-ui-improvements/22-CONTEXT.md
@.planning/phases/22-graph-ui-improvements/22-RESEARCH.md
@.planning/phases/22-graph-ui-improvements/22-UI-SPEC.md

# Phase 09 Summary（Rechartsパターン）
@.planning/phases/09-stats/09-03-SUMMARY.md

# Phase 21 Summary（曜日表示修正）
@.planning/phases/21-stats-week-start/021-01-SUMMARY.md
</context>

<interfaces>
<!-- 既存コードからのインターフェース情報 -->

From src/components/stats/StatsCard.tsx:
```typescript
// 既存のComposedChart実装（現在は191行目付近）
<ResponsiveContainer width="100%" height={200}>
  <ComposedChart data={weeklyData}>
    <XAxis dataKey="date" />
    <YAxis yAxisId="left" />
    <YAxis yAxisId="right" orientation="right" />
    <Tooltip />
    <Bar yAxisId="left" dataKey="sessions" fill="#22c55e" name="セッション数" />
    <Line yAxisId="right" type="monotone" dataKey="cumulativeMinutes" stroke="#22c55e" strokeWidth={2} name="累積時間(分)" />
  </ComposedChart>
</ResponsiveContainer>

// 週データ構造
interface WeeklyData {
  date: string  // 曜日ラベル（Sun, Mon, Tue...）
  sessions: number
  cumulativeMinutes: number
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: グラフサイズと余白の調整（STAT-02）</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（ComposedChart周辺のコード）
    - .planning/phases/22-graph-ui-improvements/22-CONTEXT.md（サイズ・余白の決定事項）
    - .planning/phases/22-graph-ui-improvements/22-UI-SPEC.md（Component Specifications - StatsCard グラフエリア）
  </read_first>
  <action>
    **目的:** グラフの高さを適切に設定し、ラベルが途切れず表示されるようにする

**実装内容:**
1. ResponsiveContainerの高さを固定値200から動的高さに変更:
   - `height={Math.max(200, parentWidth * 0.4)}` で親コンテナの幅に基づいてaspect比を維持
   - 親コンテナの幅を取得するため、useStateとuseEffectを追加してResizeObserverまたはwindow.innerWidthを監視
   - 最小高さ200pxを維持して、小さい画面でもグラフが潰れないようにする

2. XAxisのtickFormatterを維持:
   - 現在の `tickFormatter={(value) => value.slice(0, 3)}` を維持（3文字表記: Sun, Mon, Tue...）
   - 「Today」表示はgetDayLabel()関数で既に処理されているため、変更不要

3. XAxisにtickMarginを追加（ラベルとグラフの間隔）:
   - `tickMargin={10}` を追加して、X軸ラベルがグラフと重ならないようにする

**実装パターン:**
```tsx
// 親コンテナの幅を取得
const [containerWidth, setContainerWidth] = useState(0)
const containerRef = useRef<HTMLDivElement>(null)

useEffect(() => {
  const updateWidth = () => {
    if (containerRef.current) {
      setContainerWidth(containerRef.current.clientWidth)
    }
  }

  updateWidth()
  window.addEventListener('resize', updateWidth)
  return () => window.removeEventListener('resize', updateWidth)
}, [])

// ResponsiveContainerの高さを動的に計算
<ResponsiveContainer width="100%" height={Math.max(200, containerWidth * 0.4)}>
  <ComposedChart data={weeklyData}>
    <XAxis
      dataKey="date"
      tickFormatter={(value) => value.slice(0, 3)}
      tickMargin={10}
    />
    {/* ... */}
  </ComposedChart>
</ResponsiveContainer>
```

**参考 CONTEXT.md 決定事項:**
- 高さはUXベストプラクティスに基づいてClaudeが裁量で決定（aspectベースの動的高さ推奨）
- X軸ラベルは3文字表記を維持（Sun, Mon, Tue...）
- レスポンシブ対応はResponsiveContainerを維持（デスクトップと同じ高さで幅に応じて縮小）

**実装上の注意:**
- Weekタブのグラフエリア（`<div className="flex-1 flex flex-col min-h-0">`）にrefを付与
- containerWidthが0の場合のフォールバック処理を追加（初期レンダリング時）
- useMemoを使用して高さ計算を最適化（再計算を防ぐ）
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    - グラフの高さが親コンテナの幅に基づいて動的に計算され、aspect比が維持されている
    - X軸ラベル（Sun, Mon, Tue...）が途切れず表示されている
    - デスクトップとモバイル両方でグラフが適切に表示されている
    - コンテナサイズ変更時にグラフが正しくリサイズされている
  </done>
  <acceptance_criteria>
    - src/components/stats/StatsCard.tsx に `containerWidth` stateが存在する
    - src/components/stats/StatsCard.tsx に `containerRef` refが存在する
    - ResponsiveContainer の height prop に `Math.max(200, containerWidth * 0.4)` が指定されている
    - XAxis に `tickMargin={10}` が指定されている
    - XAxis の `tickFormatter={(value) => value.slice(0, 3)}` が維持されている
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 軸スタイルの改善（STAT-04）</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（ComposedChart周辺のコード）
    - .planning/phases/22-graph-ui-improvements/22-CONTEXT.md（軸スタイルの決定事項）
    - .planning/phases/22-graph-ui-improvements/22-UI-SPEC.md（Component Specifications - スタイル仕様）
    - .planning/phases/22-graph-ui-improvements/22-RESEARCH.md（Pattern 1: shadcn/ui準拠のミニマル軸スタイル）
  </read_first>
  <action>
    **目的:** 軸スタイルをshadcn/ui準拠のミニマルなデザインに変更し、アクセシビリティを向上させる

**実装内容:**
1. ComposedChartにaccessibilityLayerを追加:
   - `accessibilityLayer={true}` を追加して、キーボードナビゲーションとARIA属性を有効化
   - Recharts 3.8.0ではデフォルトでtrueだが、明示的に指定

2. XAxisにミニマルスタイルを適用:
   - `tickLine={false}` を追加（目盛り線を非表示）
   - `axisLine={false}` を追加（軸線を非表示）
   - 現在の `tickFormatter={(value) => value.slice(0, 3)}` と `tickMargin={10}` を維持

3. YAxis（左）にミニマルスタイルを適用:
   - `tickLine={false}` を追加
   - `axisLine={false}` を追加

4. YAxis（右）にミニマルスタイルを適用:
   - `tickLine={false}` を追加
   - `axisLine={false}` を追加

5. 軸ラベルの色を指定:
   - 現状はRechartsデフォルトを使用（`stroke`プロップ未指定）
   - 必要に応じて `stroke="#9ca3af"` を追加（text-cf-subtextと同等）
   - ただし、CONTEXT.mdでは「軸ラベルの色はtext-cf-subtextを使用」と決定されているため、実装

**実装パターン:**
```tsx
<ComposedChart data={weeklyData} accessibilityLayer={true}>
  <XAxis
    dataKey="date"
    tickLine={false}
    axisLine={false}
    tickFormatter={(value) => value.slice(0, 3)}
    tickMargin={10}
    stroke="#9ca3af"
  />
  <YAxis
    yAxisId="left"
    tickLine={false}
    axisLine={false}
    stroke="#9ca3af"
  />
  <YAxis
    yAxisId="right"
    orientation="right"
    tickLine={false}
    axisLine={false}
    stroke="#9ca3af"
  />
  {/* Bar, Line, Tooltip は変更なし */}
</ComposedChart>
```

**参考 CONTEXT.md 決定事項:**
- XAxis/YAxisに `tickLine={false}` を適用（目盛り線を非表示）
- XAxis/YAxisに `axisLine={false}` を適用（軸線を非表示）
- 軸ラベルの色は `text-cf-subtext`（#9ca3af）を使用
- ComposedChartに `accessibilityLayer` プロップを追加（スクリーンリーダー対応）

**参考 RESEARCH.md Pattern 1:**
- shadcn/ui準拠のミニマル軸スタイル: `tickLine={false}`, `axisLine={false}`
- `accessibilityLayer` でアクセシビリティ向上

**実装上の注意:**
- `stroke="#9ca3af"` はCSS変数 `text-cf-subtext` と同等の色
- Tooltip, Bar, Line コンポーネントは変更しない
- レスポンシブ対応はTask 1で実装済みのため、ここでは軸スタイルのみに集中
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    - XAxis/YAxisの目盛り線（tickLine）が非表示になっている
    - XAxis/YAxisの軸線（axisLine）が非表示になっている
    - 軸ラベルの色がtext-cf-subtext（#9ca3af）になっている
    - ComposedChartにaccessibilityLayerが追加され、スクリーンリーダー対応が有効化されている
    - グラフがミニマルで読みやすい見た目になっている
  </done>
  <acceptance_criteria>
    - ComposedChart に `accessibilityLayer={true}` が指定されている
    - XAxis に `tickLine={false}` が指定されている
    - XAxis に `axisLine={false}` が指定されている
    - YAxis（左）に `tickLine={false}` が指定されている
    - YAxis（左）に `axisLine={false}` が指定されている
    - YAxis（右）に `tickLine={false}` が指定されている
    - YAxis（右）に `axisLine={false}` が指定されている
    - XAxis/YAxis に `stroke="#9ca3af"` が指定されている
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: グラフUIの視覚的検証</name>
  <what-built>
    Task 1と2で実装したグラフUI改善:
    - グラフサイズと余白の調整（動的高さ、tickMargin）
    - 軸スタイルの改善（tickLine/axisLine非表示、軸ラベルの色変更）
    - アクセシビリティ対応（accessibilityLayer）
  </what-built>
  <how-to-verify>
    1. 開発サーバーを起動: `npm run dev`
    2. ブラウザで http://localhost:5173 にアクセス
    3. StatsカードのWeekタブをクリック
    4. 以下を確認:
       - グラフの高さが適切で、ラベルが途切れていない（デスクトップとモバイル両方）
       - X軸・Y軸の線と目盛り線が非表示になっている
       - 軸ラベルの色がグレー（#9ca3af）になっている
       - グラフ全体がミニマルで読みやすい
       - キーボードでTabキーを押してグラフにフォーカスが当たるか確認（accessibilityLayerの効果）
    5. 問題がなければ「approved」と入力
  </how-to-verify>
  <resume-signal>「approved」または問題の詳細を入力してください</resume-signal>
</task>

</tasks>

<verification>
1. 既存テストがパスすること: `npm test`（Regression確認）
2. グラフがブラウザで正しく表示されること（視覚的検証）
3. デスクトップとモバイル両方でグラフが適切に表示されること（レスポンシブ確認）
4. 軸スタイルがshadcn/ui準拠のミニマルなデザインになっていること（視覚的検証）
</verification>

<success_criteria>
- グラフサイズと余白が適切に調整され、ラベルが途切れず表示される（STAT-02）
- 軸ラベルの色、サイズ、太さがshadcn/uiのmutedスタイルに準拠している（STAT-04）
- モバイルとデスクトップ両方でグラフが適切に表示される（STAT-02, STAT-04）
- キーボードナビゲーションとスクリーンリーダー対応が有効化されている（STAT-04）
</success_criteria>

<output>
After completion, create `.planning/phases/22-graph-ui-improvements/22-01-SUMMARY.md`
</output>
