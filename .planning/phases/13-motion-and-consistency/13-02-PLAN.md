---
phase: 13-motion-and-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/todos/TodoItem.tsx
  - src/components/bgm/BgmPlayer.tsx
  - src/components/timer/TimerControls.tsx
autonomous: true
requirements:
  - CONS-02
user_setup: []

must_haves:
  truths:
    - "ユーザーは全ての対話要素でFramer Motionのscaleアニメーション（scale 1.02）を確認できる"
    - "ユーザーは色変化ホバー効果（hover:text-cf-danger等）を確認できる"
    - "ユーザーはCSSのhover:bg-white/10が削除されたことを確認できる（Framer Motionとの重複解消）"
  artifacts:
    - path: src/components/todos/TodoItem.tsx
      provides: "削除ボタンのホバー効果統一"
      contains_not: 'hover:bg-white/10'
      exports: ["{...hoverAnimation}"]
    - path: src/components/bgm/BgmPlayer.tsx
      provides: "小ボタンのホバー効果統一"
      contains_not: 'hover:bg-white/10'
      exports: ["{...hoverAnimation}"]
    - path: src/components/timer/TimerControls.tsx
      provides: "リセット/スキップボタンのホバー効果統一"
      contains_not: 'hover:bg-white/10'
      exports: ["{...hoverAnimation}"]
  key_links:
    - from: "src/lib/animation.ts"
      to: "src/components/todos/TodoItem.tsx"
      via: "{...hoverAnimation} spread"
      pattern: '\.\.\.hoverAnimation'
    - from: "src/lib/animation.ts"
      to: "src/components/bgm/BgmPlayer.tsx"
      via: "{...hoverAnimation} spread"
      pattern: '\.\.\.hoverAnimation'
    - from: "src/lib/animation.ts"
      to: "src/components/timer/TimerControls.tsx"
      via: "{...hoverAnimation} spread"
      pattern: '\.\.\.hoverAnimation'
---

<objective>
ホバー効果をFramer Motionのscaleアニメーションに統一し、CSSのhover:bg-white/10を削除して実装の重複を解消する

Purpose: Framer Motion（scale 1.02）とCSS（hover:bg-white/10）の重複を解消し、モダンなアニメーションライブラリを活用した統一されたホバー効果を実現する。色変化はCSSで維持する。
Output: 全ての対話要素でFramer Motionの{...hoverAnimation}が適用され、CSSのhover:bg-white/10が削除された状態
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-motion-and-consistency/13-CONTEXT.md

@src/lib/animation.ts
@src/components/todos/TodoItem.tsx
@src/components/bgm/BgmPlayer.tsx
@src/components/timer/TimerControls.tsx
</context>

<interfaces>
<!-- 既存のアニメーション定義 -->

From src/lib/animation.ts:
```typescript
export const hoverAnimation = {
  whileHover: { scale: 1.02 },
}

export const tapAnimation = {
  whileTap: { scale: 0.95 },
}
```

<!-- 現在のホバー効果実装（重複あり）-->

From src/components/todos/TodoItem.tsx (line 95-105):
```typescript
<motion.button
  {...tapAnimation}
  onClick={(e) => {
    e.stopPropagation()
    onDelete(id)
  }}
  className="p-3 opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger hover:bg-white/10"
  aria-label="削除"
>
```

From src/components/bgm/BgmPlayer.tsx:
```typescript
// プレイリストボタン (line 81-91) - hoverAnimationなし、hover:bg-white/10あり
<motion.button
  {...tapAnimation}
  onClick={() => setIsExpanded(!isExpanded)}
  className="p-3 text-cf-subtext hover:text-cf-primary hover:bg-white/10 transition-colors"
  ...
>

// 前/次ボタン (line 117-148) - hoverAnimationあり、hover:bg-white/10あり
<motion.button
  {...hoverAnimation}
  {...tapAnimation}
  className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors ..."
  ...
>
```

From src/components/timer/TimerControls.tsx (line 22-49):
```typescript
// リセットボタン (line 22-30)
<motion.button
  {...hoverAnimation}
  {...tapAnimation}
  onClick={onReset}
  className="size-14 rounded-full glass flex items-center justify-center text-cf-subtext hover:text-cf-text transition-colors hover:bg-white/10"
  ...
>

// スキップボタン (line 41-49)
<motion.button
  {...hoverAnimation}
  {...tapAnimation}
  onClick={onSkip}
  className="size-14 rounded-full glass flex items-center justify-center text-cf-subtext hover:text-cf-text transition-colors hover:bg-white/10"
  ...
>

// STARTボタン (line 32-39) - 独自のwhileHover設定、hover:bg-white/10なし
<motion.button
  whileHover={{ scale: 1.05 }}
  whileTap={{ scale: 0.95 }}
  ...
>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TodoItem削除ボタンのホバー効果を統一</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>src/components/todos/TodoItem.tsx</read_first>
  <action>
    TodoItem削除ボタンのホバー効果をFramer Motionに統一する。

    変更内容:
    1. line 95-105: 削除ボタンのmotion.buttonに{...hoverAnimation}を追加
    2. classNameから`hover:bg-white/10`を削除
    3. 色変化（`hover:text-cf-danger`）は維持

    変更後の構造:
    ```typescript
    <motion.button
      {...hoverAnimation}
      {...tapAnimation}
      onClick={(e) => {
        e.stopPropagation()
        onDelete(id)
      }}
      className="p-3 rounded-xl opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
      aria-label="削除"
    >
    ```

    注意:
    - rounded-xlはPlan 01で追加済みのため維持
    - hover:text-cf-danger（色変化）は維持
    - tapAnimationは既にあるため維持
  </action>
  <verify>
    <automated>grep -n 'hover:bg-white/10' src/components/todos/TodoItem.tsx || echo "No hover:bg-white/10 found (expected)"</automated>
  </verify>
  <done>TodoItem削除ボタンに{...hoverAnimation}が含まれ、hover:bg-white/10が含まれず、npm run buildが成功する</done>
  <acceptance_criteria>
    - src/components/todos/TodoItem.tsx contains '{...hoverAnimation}'
    - src/components/todos/TodoItem.tsx not contains 'hover:bg-white/10'
    - src/components/todos/TodoItem.tsx contains 'hover:text-cf-danger' (色変化維持)
    - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BgmPlayer小ボタンのホバー効果を統一</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>src/components/bgm/BgmPlayer.tsx</read_first>
  <action>
    BgmPlayerの小ボタン3つ（プレイリスト、前、次）のホバー効果をFramer Motionに統一する。

    変更内容:
    1. line 81-91: プレイリストボタンに{...hoverAnimation}を追加、hover:bg-white/10を削除
    2. line 117-126: 前の曲ボタンのhover:bg-white/10を削除（{...hoverAnimation}は既にある）
    3. line 139-148: 次の曲ボタンのhover:bg-white/10を削除（{...hoverAnimation}は既にある）

    変更後の構造:
    ```typescript
    // プレイリストボタン
    <motion.button
      {...hoverAnimation}
      {...tapAnimation}
      onClick={() => setIsExpanded(!isExpanded)}
      className="p-3 rounded-xl text-cf-subtext hover:text-cf-primary transition-colors"
      ...
    >

    // 前/次ボタン
    <motion.button
      {...hoverAnimation}
      {...tapAnimation}
      onClick={handlePrevTrack/handleNextTrack}
      className="p-3 rounded-xl text-cf-text hover:text-cf-primary transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
      ...
    >
    ```

    注意:
    - rounded-xlはPlan 01で追加済みのため維持
    - 色変化（hover:text-cf-primary等）は維持
    - STARTボタン（line 128-137）の独自whileHover={{ scale: 1.1 }}は維持
  </action>
  <verify>
    <automated>grep -n 'hover:bg-white/10' src/components/bgm/BgmPlayer.tsx || echo "No hover:bg-white/10 found (expected)"</automated>
  </verify>
  <done>BgmPlayer小ボタンに{...hoverAnimation}が含まれ、hover:bg-white/10が含まれず、npm run buildが成功する</done>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsx contains '{...hoverAnimation}' 3回以上
    - src/components/bgm/BgmPlayer.tsx not contains 'hover:bg-white/10'
    - src/components/bgm/BgmPlayer.tsx contains 'hover:text-cf-primary' (色変化維持)
    - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: TimerControlsリセット/スキップボタンのホバー効果を統一</name>
  <files>src/components/timer/TimerControls.tsx</files>
  <read_first>src/components/timer/TimerControls.tsx</read_first>
  <action>
    TimerControlsのリセット/スキップボタンのホバー効果からCSSのhover:bg-white/10を削除する。

    変更内容:
    1. line 22-30: リセットボタンのclassNameから`hover:bg-white/10`を削除
    2. line 41-49: スキップボタンのclassNameから`hover:bg-white/10`を削除

    変更後の構造:
    ```typescript
    // リセットボタン
    <motion.button
      {...hoverAnimation}
      {...tapAnimation}
      onClick={onReset}
      className="size-14 rounded-full glass flex items-center justify-center text-cf-subtext hover:text-cf-text transition-colors"
      aria-label="リセット"
    >

    // スキップボタン
    <motion.button
      {...hoverAnimation}
      {...tapAnimation}
      onClick={onSkip}
      className="size-14 rounded-full glass flex items-center justify-center text-cf-subtext hover:text-cf-text transition-colors"
      aria-label="スキップ"
    >
    ```

    注意:
    - {...hoverAnimation}は既にあるため維持
    - 色変化（hover:text-cf-text）は維持
    - STARTボタン（line 32-39）は変更なし
  </action>
  <verify>
    <automated>grep -n 'hover:bg-white/10' src/components/timer/TimerControls.tsx || echo "No hover:bg-white/10 found (expected)"</automated>
  </verify>
  <done>TimerControlsのリセット/スキップボタンにhover:bg-white/10が含まれず、npm run buildが成功する</done>
  <acceptance_criteria>
    - src/components/timer/TimerControls.tsx not contains 'hover:bg-white/10'
    - src/components/timer/TimerControls.tsx contains '{...hoverAnimation}' 2回（リセット/スキップ）
    - src/components/timer/TimerControls.tsx contains 'hover:text-cf-text' (色変化維持)
    - npm run build exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. npm run buildが成功すること
2. 全ての対話要素に{...hoverAnimation}が含まれること
3. CSSのhover:bg-white/10が全て削除されていること
4. 色変化（hover:text-*）は維持されていること
</verification>

<success_criteria>
1. TodoItem削除ボタン、BgmPlayer小ボタン3つ、TimerControlsリセット/スキップボタンの全てに{...hoverAnimation}が適用されている
2. 全てのファイルからhover:bg-white/10が削除されている
3. 色変化ホバー効果（hover:text-cf-danger等）は維持されている
4. npm run buildが成功する
5. ビルド結果に警告やエラーがない
</success_criteria>

<output>
After completion, create `.planning/phases/13-motion-and-consistency/13-02-SUMMARY.md`
</output>
