---
phase: 13-motion-and-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/todos/TodoItem.tsx
  - src/components/bgm/BgmPlayer.tsx
autonomous: true
requirements:
  - CONS-01
user_setup: []

must_haves:
  truths:
    - "ユーザーはTodoItem削除ボタンで統一された角丸スタイル（rounded-xl）を確認できる"
    - "ユーザーはBgmPlayer小ボタン（前/次/プレイリスト）で統一された角丸スタイル（rounded-xl）を確認できる"
    - "ユーザーはSTARTボタンのrounded-fullスタイルが維持されていることを確認できる"
  artifacts:
    - path: src/components/todos/TodoItem.tsx
      provides: "削除ボタンの角丸統一"
      contains: 'rounded-xl'
    - path: src/components/bgm/BgmPlayer.tsx
      provides: "小ボタンの角丸統一"
      contains: 'rounded-xl' 2回以上
  key_links:
    - from: "src/components/todos/TodoItem.tsx"
      to: "src/components/todos/TodoItem.tsx (削除ボタン)"
      via: "classNameにrounded-xl追加"
      pattern: 'className="p-3.*rounded-xl'
    - from: "src/components/bgm/BgmPlayer.tsx"
      to: "src/components/bgm/BgmPlayer.tsx (小ボタン群)"
      via: "classNameにrounded-xl追加"
      pattern: 'className="p-3.*rounded-xl'
---

<objective>
小ボタンの角丸スタイルをrounded-xlに統一し、ボタンスタイルの一貫性を実現する

Purpose: 小ボタンの角丸が不統一である問題を解決し、視覚的な一貫性を向上する。主要ボタン（START）のrounded-fullは維持する。
Output: TodoItem削除ボタンとBgmPlayer小ボタンにrounded-xlが適用された状態
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-motion-and-consistency/13-CONTEXT.md

@src/lib/animation.ts
@src/components/todos/TodoItem.tsx
@src/components/bgm/BgmPlayer.tsx
</context>

<interfaces>
<!-- 既存のボタンスタイルパターン -->

From src/components/todos/TodoItem.tsx (line 101):
```typescript
<motion.button
  {...tapAnimation}
  onClick={(e) => {
    e.stopPropagation()
    onDelete(id)
  }}
  className="p-3 opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger hover:bg-white/10"
  aria-label="削除"
>
```

From src/components/bgm/BgmPlayer.tsx (line 117-126):
```typescript
<motion.button
  {...hoverAnimation}
  {...tapAnimation}
  onClick={handlePrevTrack}
  disabled={hasError}
  className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
  title="前の曲"
>
```

From src/components/bgm/BgmPlayer.tsx (line 81-91):
```typescript
<motion.button
  {...tapAnimation}
  onClick={() => setIsExpanded(!isExpanded)}
  className="p-3 text-cf-subtext hover:text-cf-primary hover:bg-white/10 transition-colors"
  title={isExpanded ? '縮小' : 'プレイリスト'}
  aria-label={isExpanded ? '縮小' : 'プレイリスト'}
>
```

From src/components/bgm/BgmPlayer.tsx (line 128-137):
<!-- STARTボタン（主要ボタン） - rounded-fullを維持 -->
```typescript
<motion.button
  whileHover={{ scale: 1.1 }}
  whileTap={{ scale: 0.9 }}
  onClick={toggle}
  disabled={hasError}
  className="w-14 h-14 rounded-full bg-cf-primary hover:bg-cf-primary/80 text-white flex items-center justify-center transition-colors disabled:bg-cf-subtext/30 disabled:text-cf-subtext disabled:cursor-not-allowed shadow-lg shadow-cf-primary/30"
  title={isPlaying ? '一時停止' : '再生'}
>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TodoItem削除ボタンにrounded-xlを追加</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>src/components/todos/TodoItem.tsx</read_first>
  <action>
    削除ボタンのclassNameに`rounded-xl`を追加する。

    変更対象:
    - line 101: `className="p-3 opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger hover:bg-white/10"`
    - 変更後: `className="p-3 rounded-xl opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger hover:bg-white/10"`

    注意:
    - p-3（12px padding）は維持する（タッチターゲット44px確保）
    - その他のクラス（opacity-0, group-hover:opacity-100等）はそのまま維持
  </action>
  <verify>
    <automated>grep -n 'rounded-xl' src/components/todos/TodoItem.tsx</automated>
  </verify>
  <done>TodoItemの削除ボタンにrounded-xlが含まれ、npm run buildが成功する</done>
  <acceptance_criteria>
    - src/components/todos/TodoItem.tsx contains 'rounded-xl'
    - grepで確認: 削除ボタンのmotion.buttonにclassName=".*rounded-xl.*"が含まれる
    - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BgmPlayer小ボタンにrounded-xlを追加</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>src/components/bgm/BgmPlayer.tsx</read_first>
  <action>
    BgmPlayerの小ボタン3つ（プレイリストボタン、前の曲ボタン、次の曲ボタン）のclassNameに`rounded-xl`を追加する。

    変更対象:
    1. line 84: プレイリストボタン（Listアイコン）
       - 変更前: `className="p-3 text-cf-subtext hover:text-cf-primary hover:bg-white/10 transition-colors"`
       - 変更後: `className="p-3 rounded-xl text-cf-subtext hover:text-cf-primary hover:bg-white/10 transition-colors"`

    2. line 122: 前の曲ボタン（SkipBackアイコン）
       - 変更前: `className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"`
       - 変更後: `className="p-3 rounded-xl text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"`

    3. line 144: 次の曲ボタン（SkipForwardアイコン）
       - 変更前: `className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"`
       - 変更後: `className="p-3 rounded-xl text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"`

    注意:
    - STARTボタン（line 133）の`rounded-full`は変更しない（主要ボタンスタイルを維持）
    - p-3（12px padding）は維持する（タッチターゲット44px確保）
    - その他のクラス（disabled関連等）はそのまま維持
  </action>
  <verify>
    <automated>grep -c 'rounded-xl' src/components/bgm/BgmPlayer.tsx</automated>
  </verify>
  <done>BgmPlayerにrounded-xlが3回以上出現し、STARTボタンのrounded-fullが維持され、npm run buildが成功する</done>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsx contains 'rounded-xl' 3回以上
    - grepで確認: STARTボタンのmotion.buttonにclassName=".*rounded-full.*"が含まれる（維持）
    - npm run build exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. npm run buildが成功すること
2. TodoItem削除ボタンにrounded-xlが含まれること
3. BgmPlayer小ボタン3つにrounded-xlが含まれること
4. STARTボタンのrounded-fullが維持されていること
</verification>

<success_criteria>
1. 全ての小ボタン（TodoItem削除、BgmPlayer小ボタン3つ）にrounded-xlが適用されている
2. STARTボタンのrounded-fullが維持されている
3. npm run buildが成功する
4. ビルド結果に警告やエラーがない
</success_criteria>

<output>
After completion, create `.planning/phases/13-motion-and-consistency/13-01-SUMMARY.md`
</output>
