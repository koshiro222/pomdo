---
phase: 17-layout-animation-improvements
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/components/todos/TodoList.tsx
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
user_setup: []
must_haves:
  truths:
    - "ヘッダー（Tasks/Current Task）とTodoリストの間に仕切り線が表示される"
    - "「Add a new task」入力がTodoリストの一番下に配置されている"
    - "TodoInputがリストと一緒にスクロールする"
  artifacts:
    - path: "src/components/todos/TodoList.tsx"
      provides: "新レイアウト構造"
      contains: "border-b border-white/10"
  key_links:
    - from: "TodoList.tsx ブロールエリア"
      to: "TodoInput"
      via: "スクロールエリア内の最後の子要素として配置"
      pattern: "TodoInput.*スクロールエリア"
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodoListレイアウト構造変更</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（現在のレイアウト構造を確認）
    - src/components/todos/TodoInput.tsx（移動対象のコンポーネント）
    - .planning/phases/17-layout-animation-improvements/17-UI-SPEC.md（Interaction Contract）
    - .planning/phases/17-layout-animation-improvements/17-CONTEXT.md（ユーザー決定事項）
  </read_first>
  <action>
    TodoList.tsxのレイアウト構造を変更する。

    **現在の構造:**
    ```tsx
    .bento-card
      └── div.border-b.pb-4.mb-4  // ヘッダー全体
          ├── "Tasks" + badge
          ├── Current Task highlight
          ├── フィルタータブ
          └── TodoInput ← ここから移動
      └── div.flex-1.overflow-y-auto  // Todoリスト
    ```

    **変更後の構造:**
    ```tsx
    .bento-card (p-4 sm:p-6)
      ├── div (ヘッダー — border-bなし)
      │   ├── "Tasks" + badge
      │   └── Current Task highlight (AnimatePresence)
      ├── div.border-b.border-white/10.my-3  // 仕切り線（独立要素）
      ├── フィルタータブ
      └── div.flex-1.overflow-y-auto.min-h-0.flex.flex-col.gap-3 (スクロールエリア)
          ├── AnimatePresence > TodoItem*
          └── TodoInput ← 最下部に移動
    ```

    **具体的な変更:**

    1. **ヘッダーdivの構造変更:**
       - 現在の `div.border-b.border-white/10.pb-4.mb-4` を以下のように分割:
         - 外側div: border-bなし
         - Current Taskハイライトセクション（AnimatePresence部分）
       - 仕切り線: `<div className="border-b border-white/10 my-3" />`
       - フィルタータブ: 現在の位置から移動

    2. **TodoInputの移動:**
       - `TodoInput` をヘッダー内からスクロールエリア内の最後に移動
       - スクロールエリアの構造: `flex flex-col gap-3`
       - TodoItem一覧の後にTodoInputを配置

    3. **フィルタータブのマージン調整:**
       - `mb-4` → `mb-3` に変更（仕切り線との間隔を調整）

    **実装詳細:**
    ```tsx
    // 変更前のヘッダー:
    // <div className="border-b border-white/10 pb-4 mb-4">
    //   <div className="flex items-center justify-between mb-4">...</div>
    //   <AnimatePresence mode="popLayout">...</AnimatePresence> // Current Task
    //   <div className="flex gap-2 mb-4">...</div>  // フィルタータブ
    //   <TodoInput onAdd={handleAddTodo} />
    // </div>

    // 変更後の構造:
    // <div className="pb-4 sm:pb-6">
    //   {/* Tasks ヘッダー */}
    //   <div className="flex items-center justify-between mb-4">...</div>
    //   {/* Current Taskハイライト（変更なし） */}
    //   <AnimatePresence mode="popLayout">...</AnimatePresence>
    // </div>

    // {/* 仕切り線 */}
    // <div className="border-b border-white/10 my-3" />

    // {/* フィルタータブ */}
    // <div className="flex gap-2 mb-3">...</div>

    // {/* スクロールエリア（TodoList + TodoInput） */}
    // <div className="flex-1 overflow-y-auto min-h-0 flex flex-col gap-3 -mx-4 sm:-mx-6 px-4 sm:px-6">
    //   {filteredTodos.length === 0 ? (
    //     <div className="text-cf-subtext text-center py-8">No tasks yet</div>
    //   ) : (
    //     <AnimatePresence mode="popLayout">
    //       {filteredTodos.map(...)}
    //     </AnimatePresence>
    //   )}

    //   {/* TodoInputを最後に移動 */}
    //   <TodoInput onAdd={handleAddTodo} />
    // </div>
    ```

    **注意点:**
    - TodoInputはTodoList.tsxからimport済み、ため、配置場所の変更のみで内部実装は変更不要
    - `slideInVariants` のimport は `expandInVariants` に変更（Plan 01で実装済み）
    - スクロールエリアのpadding: `-mx-4 sm:-mx-6 px-4 sm:px-6` で負のマージンを相殺

  </action>
  <acceptance_criteria>
    - TodoList.tsx に `border-b border-white/10 my-3` という独立した仕切り線要素が存在する
    - 仕切り線がCurrent Taskセクションとフィルタータブの間にある
    - TodoInputがスクロールエリア内の最後の子要素として配置されている
    - ヘッダーdivに `border-b` クラスが存在しない（仕切り線は独立要素に移動）
    - フィルタータブの `mb-4` が `mb-3` に変更されている
    - スクロールエリアに `-mx-4 sm:-mx-6 px-4 sm:px-6` クラスが存在する
  </acceptance_criteria>
  <verify>
    <automated>grep -n "border-b border-white/10 my-3" src/components/todos/TodoList.tsx | head -1</automated>
  </verify>
  <done>
    TodoListのレイアウト構造が変更され、仕切り線が正しい位置に表示され、TodoInputがリスト最下部に移動している
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TodoListのレイアウト変更（仕切り線の位置変更、TodoInputの配置変更）
  </what-built>
  <how-to-verify>
    1. `npm run dev` で開発サーバーを起動（http://localhost:5173 でアプリを開く）
    2. TodoListカードを表示し、以下のレイアウトを確認:
       - Current Taskセクションの下に仕切り線（薄い灰色の線）があること
       - 仕切り線の下にフィルタータブ（All/Active/Done）があること
       - フィルタータブの下にTodoリストがあること
       - Todoリストの一番下に「+ Add a new task...」ボタンがあること
    3. Todoリストをスクロールした際、TodoInputも一緒にスクロールすること
    4. 新タスクを追加し、以下のアニメーションを確認:
       - 既存タスクがスムーズに下方向へスライドしてスペースを確保すること
       - 新タスクが上から展開するように出現すること（高さ展開 + フェードイン）
    5. ページをリロードし、既存タスクがアニメーションなしで表示されること
  </how-to-verify>
  <resume-signal>Type "approved" または不具合を説明</resume-signal>
</task>

</tasks>

<verification>
**LAYOUT-01 検証:**
- 仕切り線がCurrent Taskセクションの下、フィルタータブの上に表示される

**LAYOUT-02 検証:**
- TodoInputがスクロールエリア内の最後に配置されている
- TodoInputがリストと一緒にスクロールする

**視覚的確認手順:**
1. `npm run dev` でアプリを起動
2. TodoListカードのレイアウト構造を目視確認
</verification>

<success_criteria>
- [ ] 仕切り線がCurrent Task下、フィルタータブ上に正しく表示される
- [ ] TodoInputがTodoリストの一番下に配置されている
- [ ] TodoInputがリストと一緒にスクロールする
- [ ] ヘッダーとTodoリストが視覚的に明確に分離されている
</success_criteria>

<output>
After completion, create `.planning/phases/17-layout-animation-improvements/17-02-SUMMARY.md`
</output>
