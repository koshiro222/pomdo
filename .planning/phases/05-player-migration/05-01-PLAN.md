---
phase: 05-player-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useBgm.ts
  - src/components/bgm/BgmPlayer.tsx
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03
  - FE-04
user_setup: []
must_haves:
  truths:
    - "プレイヤーがDBからトラックを取得して再生できる"
    - "ハードコードされたTRACKS定数が削除されている"
    - "ローディング中はフォールバックトラックが表示される"
    - "エラー時はフォールバックトラックが表示され続ける"
    - "エラー時にエラーメッセージ（トースト）が表示される"
    - "既存のBGM再生機能が問題なく動作している"
  artifacts:
    - path: "src/hooks/useBgm.ts"
      provides: "tRPC連携されたBGMフック（ローディング・エラー時のフォールバック、エラー状態返却）"
      min_lines: 100
      exports: ["useBgm", "type Track"]
    - path: "src/components/bgm/BgmPlayer.tsx"
      provides: "プレイヤー（APIエラー時のトースト表示追加）"
  key_links:
    - from: "src/hooks/useBgm.ts"
      to: "trpc.bgm.getAll"
      via: "useQuery hook"
      pattern: "trpc\\.bgm\\.getAll\\.useQuery"
    - from: "src/hooks/useBgm.ts"
      to: "FALLBACK_TRACKS"
      via: "isLoading || error条件"
      pattern: "\\(isLoading \\|\\| error\\).*FALLBACK_TRACKS"
    - from: "src/components/bgm/BgmPlayer.tsx"
      to: "useBgm().error"
      via: "エラー状態の監視とトースト表示"
      pattern: "useEffect.*error.*toast"
---

<objective>
既存プレイヤーをDB連携に移行し、ハードコードされたトラック定数を削除する

Purpose: Phase 3で実装したbgm.getAll APIをフロントエンドから使用し、BGMトラックの取得をDB連携に移行する

Output:
- tRPCのbgm.getAllクエリを使用するuseBgmフック
- ローディング・エラー時のフォールバックトラック機能
- エラー発生時のトースト通知（FE-04要件を満たす）
- ハードコードされたTRACKS定数の削除
- 既存プレイヤーとの互換性維持
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-player-migration/05-CONTEXT.md
@src/hooks/useBgm.ts
@src/hooks/useTodos.ts
@src/app/routers/bgm.ts
@functions/lib/schema.ts
@src/components/bgm/BgmPlayer.tsx

<interfaces>
<!-- Phase 3 API: bgmRouter.getAll -->

From src/app/routers/bgm.ts:
```typescript
getAll: publicProcedure
  .input(bgmGetAllInputSchema)
  .query(async ({ ctx, input }) => {
    // Returns: { id, title, src, artist, color, tier, createdAt, updatedAt }[]
    // src field is auto-generated: /api/bgm/{filename}
    // Optional tier filter via input?.tier
  })
```

From src/app/routers/_shared.ts:
```typescript
export const bgmGetAllInputSchema = z.object({
  tier: z.enum(['free', 'premium']).optional()
})
```

<!-- Existing Track type (must maintain compatibility) -->

From src/hooks/useBgm.ts (current):
```typescript
export type Track = {
  id: string
  title: string
  src: string
  artist?: string
  color?: string
}

export const TRACKS: Track[] = [
  { id: '1', title: 'Lo-Fi Study 01', src: '/api/bgm/lofi-01.mp3', artist: 'Chill Beats', color: '#3b82f6' },
  { id: '2', title: 'Lo-Fi Study 02', src: '/api/bgm/lofi-02.mp3', artist: 'Relax Sounds', color: '#8b5cf6' },
]
```

<!-- tRPC useQuery pattern reference (from useTodos.ts) -->
```typescript
const todosQuery = trpc.todos.getAll.useQuery(undefined, {
  enabled: !!user,
  refetchOnWindowFocus: false,
})
const todos = todosQuery.data ?? []
const loading = todosQuery.isLoading
```

<!-- BgmPlayer.tsx existing pattern (for reference) -->
From src/components/bgm/BgmPlayer.tsx:
```typescript
const { tracks, currentTrack, isPlaying, toggle, selectTrack, setVolume, hasError } = useBgm()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBgmフックをtRPC連携に変更（フォールバック・エラー状態付き）</name>
  <files>src/hooks/useBgm.ts</files>
  <read_first>
    - src/hooks/useBgm.ts (既存実装全体)
    - src/hooks/useTodos.ts (tRPC useQueryパターン参考)
    - src/app/routers/bgm.ts (getAllクエリの戻り値型)
  </read_first>
  <action>
    tRPCのbgm.getAllクエリを使用して、ハードコードされたTRACKS定数を置き換える

    **1. tRPCクライアントインポート追加**
    ```typescript
    import { trpc } from '../lib/trpc'
    ```

    **2. フォールバックトラック定数をファイル先頭に追加（TRACKS定数の前に）**
    ```typescript
    const FALLBACK_ENABLED = import.meta.env.VITE_BGM_FALLBACK !== 'false'
    const FALLBACK_TRACKS: Track[] = [
      { id: '1', title: 'Lo-Fi Study 01', src: '/api/bgm/lofi-01.mp3', artist: 'Chill Beats', color: '#3b82f6' },
      { id: '2', title: 'Lo-Fi Study 02', src: '/api/bgm/lofi-02.mp3', artist: 'Relax Sounds', color: '#8b5cf6' },
    ]
    ```
    ※ TRACKS定数は既存コードと同じ内容を使用（Task 2で削除予定）

    **3. useBgm関数内でtRPCクエリを使用（関数の先頭、audioRef定義の前に）**
    ```typescript
    export function useBgm(): BgmState {
      // tRPCクエリでトラック取得
      const bgmQuery = trpc.bgm.getAll.useQuery(undefined, {
        enabled: true,
        staleTime: 60 * 60 * 1000, // 1時間
        refetchOnWindowFocus: false,
      })

      const { data: apiTracks, isLoading, error } = bgmQuery

      // DBスキーマからTrack型にマッピング（tier, createdAt, updatedAtは破棄）
      const dbTracks = apiTracks?.map(({ id, title, src, artist, color }) =>
        ({ id, title, src, artist: artist ?? undefined, color: color ?? undefined })
      ) ?? []

      // フォールバックロジック: ローディング中またはエラー時にフォールバック使用
      const tracks = ((isLoading || error) && FALLBACK_ENABLED) ? FALLBACK_TRACKS : dbTracks

      // エラー状態を抽出（BgmPlayerでトースト表示に使用）
      const apiError = error

      // 以降は既存コード（audioRef定義以降）をそのまま使用
      const audioRef = useRef<HTMLAudioElement | null>(null)
      // ... 既存の実装を維持
    ```

    **4. BgmState型拡張（戻り値にloading, error追加）**
    ```typescript
    export type BgmState = {
      tracks: Track[]
      currentTrack: Track
      currentIndex: number
      isPlaying: boolean
      volume: number
      hasError: boolean      // Audio要素の再生エラー用（既存）
      loading: boolean       // 追加：APIローディング状態
      error: Error | null    // 追加：APIエラー情報（トースト表示用）
      toggle: () => void
      selectTrack: (index: number) => void
      setVolume: (vol: number) => void
    }
    ```

    **5. 戻り値にloading, error追加**
    ```typescript
    return {
      tracks,
      currentTrack: tracks[currentIndex],
      currentIndex,
      isPlaying,
      volume,
      hasError,
      loading,
      error: apiError ?? null,
      toggle,
      selectTrack,
      setVolume,
    }
    ```

    **6. 既存のtracks参照を置き換え**
    - `const audio = new Audio()` の後ろにある `TRACKS[currentIndex]` を `tracks[currentIndex]` に変更
    - toggle関数内の `TRACKS[currentIndex]` を `tracks[currentIndex]` に変更

    **重要:** 既存の`TRACKS`定数はTask 2で削除するため、この時点では残したまま
  </action>
  <verify>
    <automated>grep -n "trpc.bgm.getAll.useQuery" src/hooks/useBgm.ts</automated>
    <automated>grep -n "const tracks = " src/hooks/useBgm.ts | grep -E "(isLoading.*error|error.*isLoading)"</automated>
    <automated>grep -n "loading: " src/hooks/useBgm.ts | grep -v "//"</automated>
    <automated>grep -n "error: " src/hooks/useBgm.ts | grep -v "//"</automated>
    <automated>grep -n "FALLBACK_TRACKS" src/hooks/useBgm.ts</automated>
    <automated>grep -n "FALLBACK_ENABLED" src/hooks/useBgm.ts</automated>
    <automated>! grep -n "tracks: TRACKS" src/hooks/useBgm.ts</automated>
  </verify>
  <done>
    - tRPCのbgm.getAll.useQueryが使用されている
    - フォールバックトラック定数が追加されている
    - ローディング中またはエラー時にフォールバックが使用される
    - 戻り値にloading, errorが追加されている
    - errorプロパティはErrorオブジェクトまたはnull
    - 既存のTRACKS参照がtracks変数に置き換わっている
  </done>
</task>

<task type="auto">
  <name>Task 2: BgmPlayerにAPIエラー時のトースト表示を追加</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx (既存実装全体)
  </read_first>
  <action>
    APIエラー発生時にトースト通知を表示し、FE-04要件を満たす

    **1. 必要なインポートを追加**
    ```typescript
    import { useEffect } from 'react'
    import { toast } from 'sonner'
    ```

    **2. useBgmフックからerror, loadingを取得**
    ```typescript
    const { tracks, currentTrack, isPlaying, toggle, selectTrack, setVolume, hasError, loading, error } = useBgm()
    ```

    **3. エラー時のトースト表示を追加（コンポーネント内のuseEffectとして）**
    ```typescript
    // APIエラー時のトースト表示（FE-04: BGM取得失敗時にエラーメッセージ表示）
    useEffect(() => {
      if (error && !loading) {
        // エラー発生時に1度だけトースト表示
        toast.error('BGMの読み込みに失敗しました。代替トラックを再生します。', {
          id: 'bgm-api-error', // 重複表示を防止
          duration: 5000,
        })
      }
    }, [error, loading])
    ```

    **重要:**
    - `hasError` は Audio 要素の再生エラー用として既存の表示パターン（「音源なし」表示）を維持
    - `error` は API エラー用で、トースト表示のみに使用（フォールバックトラックは自動表示される）
    - トーストに `id` を指定して、同じエラーで複数回トーストが表示されるのを防止
  </action>
  <verify>
    <automated>grep -n "import.*toast.*sonner" src/components/bgm/BgmPlayer.tsx</automated>
    <automated>grep -n "const.*=.*useBgm()" src/components/bgm/BgmPlayer.tsx | grep -E "(error|loading)"</automated>
    <automated>grep -n "useEffect" src/components/bgm/BgmPlayer.tsx | grep -A5 "error"</automated>
    <automated>grep -n "toast.error" src/components/bgm/BgmPlayer.tsx</automated>
    <automated>grep -n "bgm-api-error" src/components/bgm/BgmPlayer.tsx</automated>
  </verify>
  <done>
    - sonnerからtoastがインポートされている
    - useBgmからerror, loadingが取得されている
    - error時かつloading完了時にトーストが表示される
    - トーストにidが指定され、重複表示が防止されている
    - hasErrorは既存のAudioエラー表示として維持されている
  </done>
</task>

<task type="auto">
  <name>Task 3: ハードコードされたTRACKS定数を削除</name>
  <files>src/hooks/useBgm.ts</files>
  <read_first>
    - src/hooks/useBgm.ts (Task 1変更後の状態)
  </read_first>
  <action>
    ハードコードされたTRACKS定数を削除し、コードを整理する

    **1. TRACKS定数を削除**
    ```typescript
    // 削除: export const TRACKS: Track[] = [...]
    ```

    **2. Track型のエクスポートを維持**
    ```typescript
    export type Track = {
      id: string
      title: string
      src: string
      artist?: string
      color?: string
    }
    ```
    ※ Track型はBgmPlayer.tsxで使用されているため、エクスポートは維持

    **3. インポートの確認**
    - trpcインポートが存在することを確認
    - 他の未使用インポートがないことを確認

    **4. コードの整合性確認**
    - ファイル内に "TRACKS" の文字列が、FALLBACK_TRACKSと型名以外で残っていないことを確認
  </action>
  <verify>
    <automated>! grep -n "^export const TRACKS" src/hooks/useBgm.ts</automated>
    <automated>grep -n "export type Track" src/hooks/useBgm.ts</automated>
    <automated>grep -c "TRACKS" src/hooks/useBgm.ts | grep -E "^[2-3]$"</automated> <!-- FALLBACK_TRACKS(2箇所) + Track型(1箇所) = 2-3 -->
    <automated>grep -n "import.*trpc" src/hooks/useBgm.ts</automated>
  </verify>
  <done>
    - TRACKS定数が削除されている
    - Track型はエクスポートされている
    - tRPCインポートが存在する
    - ハードコードされたトラックデータが存在しない
  </done>
</task>

</tasks>

<verification>
## 全体検証

**1. 型チェック**
```bash
npm run build
```
TypeScriptコンパイルエラーがないこと

**2. 動作確認（ローカル）**
```bash
npm run dev
```
- プレイヤーがトラックを表示すること
- 再生ボタンで音楽が再生されること
- トラック選択で切り替わること

**3. エラー時の動作確認（FE-04検証）**
- tRPCサーバーを停止した状態でアプリを開く
- フォールバックトラックが表示されること
- エラートーストが1度だけ表示されること

**4. E2E確認**
- 既存のBGM再生機能（再生・一時停止・トラック切り替え・音量調整）が問題なく動作していること
  - hasErrorはAudio要素の再生エラー用として維持
  - APIローディング時はフォールバックトラックが表示される
  - APIエラー時はフォールバックトラックが表示され続ける
  - APIエラー時はトースト通知が表示される
</verification>

<success_criteria>
## 成功基準

1. **DB連携完了**: プレイヤーがDBからトラックを取得して再生できる
2. **ハードコード削除**: `TRACKS` 定数が削除されている
3. **フォールバック機能**: ローディング中またはエラー時にフォールバックトラックが表示される
4. **エラー通知（FE-04）**: APIエラー時にトーストでエラーメッセージが表示される
5. **互換性維持**: 既存のBGM再生機能が問題なく動作している
6. **型安全**: TypeScriptコンパイルエラーがない
</success_criteria>

<output>
After completion, create `.planning/phases/05-player-migration/05-01-SUMMARY.md`
</output>
