---
phase: 04-bgm-api-write
plan: 03
type: execute
wave: 2
depends_on: [04-00, 04-01, 04-02]
files_modified: [src/app/routers/bgm.ts, src/app/routers/_shared.ts]
autonomous: false
requirements: [API-05, API-07]
must_haves:
  truths:
    - "管理者がdelete mutationを実行するとDBからレコードが削除される"
    - "R2バケットからもファイルが削除される"
    - "非管理者がdelete mutationを実行すると403エラーが返される"
    - "存在しないトラックIDを指定すると404エラーが返される"
    - "R2削除失敗時でもDB削除は確定する（整合性優先）"
  artifacts:
    - path: "src/app/routers/bgm.ts"
      provides: "delete mutation実装"
      exports: ["bgmRouter.delete"]
      contains: "adminProcedure.input(deleteBgmTrackSchema).mutation"
    - path: "src/app/routers/_shared.ts"
      provides: "deleteBgmTrackSchema Zodスキーマ"
      exports: ["deleteBgmTrackSchema"]
      contains: "id: z.string().uuid()"
  key_links:
    - from: "bgmRouter.delete"
      to: "schema.bgmTracks"
      via: "db.delete().where()"
      pattern: "db\\.delete\\(schema\\.bgmTracks\\)"
    - from: "bgmRouter.delete"
      to: "BGM_BUCKET"
      via: "R2 delete操作"
      pattern: "ctx\\.env\\.BGM_BUCKET\\.delete"
    - from: "db.delete()"
      to: "BGM_BUCKET.delete()"
      via: "順序保証"
      pattern: "await db\\.delete.*await ctx\\.env\\.BGM_BUCKET\\.delete"
---

<objective>
delete mutation実装：BGMトラック削除機能

管理者がトラックを削除できるtRPC mutationを実装する。DBレコード削除後にR2ファイル削除を試行し、R2削除失敗時でもDB削除を確定させる（CONTEXT.md決定）。

Purpose: 不要トラックの削除とR2ストレージ解放を両立、整合性を維持
Output: bgmRouter.delete mutation、deleteBgmTrackSchema
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bgm-api-write/04-CONTEXT.md
@.planning/phases/04-bgm-api-write/04-RESEARCH.md
@.planning/phases/04-bgm-api-write/04-01-SUMMARY.md
@.planning/phases/04-bgm-api-write/04-02-SUMMARY.md
@src/app/routers/bgm.ts
@src/app/routers/todos.ts
@functions/lib/schema.ts
</context>

<interfaces>
<!-- todosRouter deleteパターン（参考） -->
From src/app/routers/todos.ts:
```typescript
delete: protectedProcedure
  .input(z.object({ id: z.string().uuid() }))
  .mutation(async ({ ctx, input }) => {
    // 存在チェック
    const existing = await db
      .select()
      .from(ctx.schema.todos)
      .where(eq(ctx.schema.todos.id, input.id))
      .limit(1)

    if (!existing || existing.length === 0) {
      throw new TRPCError({ code: 'NOT_FOUND', message: 'Todo not found' })
    }

    // DB削除
    await db
      .delete(ctx.schema.todos)
      .where(eq(ctx.schema.todos.id, input.id))

    return { id: input.id }
  })
```

<!-- 既存bgmRouter構造（Plan 01, 02で更新済み） -->
From src/app/routers/bgm.ts（Plan 02後の想定）:
```typescript
export const bgmRouter = router({
  getAll: publicProcedure /* ... */,
  create: adminProcedure /* ... */,
  update: adminProcedure /* ... */,
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: deleteBgmTrackSchema Zodスキーマ定義</name>
  <files>src/app/routers/_shared.ts</files>
  <read_first>
  - src/app/routers/_shared.ts（既存スキーマ定義）
  - functions/lib/schema.ts（bgmTracksカラム制約）
  </read_first>
  <action>
_shared.tsにBGM削除用のZodスキーマを追加：

```typescript
// BGM削除用スキーマ
export const deleteBgmTrackSchema = z.object({
  id: z.string().uuid('UUID形式で指定してください'),
})
```

配置場所：
- updateBgmTrackSchema定義の直後

型エイリアスも追加：
```typescript
export type DeleteBgmTrack = z.infer<typeof deleteBgmTrackSchema>
```

バリデーションルール：
- id: 必須、UUID形式

スキーマはシンプルに、IDのみ受け取る。filenameはDBから取得して使用する。
  </action>
  <verify>
    <automated>grep -q "export const deleteBgmTrackSchema" src/app/routers/_shared.ts && grep -q "id: z.string().uuid()" src/app/routers/_shared.ts && grep -q "export type DeleteBgmTrack" src/app/routers/_shared.ts</automated>
  </verify>
  <done>
  - deleteBgmTrackSchemaがエクスポートされている
  - idフィールドが必須でUUIDバリデーションされている
  - DeleteBgmTrack型がエクスポートされている
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bgmRouterにdelete mutationを追加</name>
  <files>src/app/routers/bgm.ts</files>
  <read_first>
  - src/app/routers/bgm.ts（create, update mutation実装済み）
  - src/app/routers/todos.ts（delete mutationパターン）
  </read_first>
  <behavior>
    - Test 1: 管理者がdelete mutationを実行すると、DBからレコードが削除される
    - Test 2: DB削除後にR2からもファイルが削除される
    - Test 3: 非管理者が実行するとFORBIDDENエラー（adminProcedureにより）
    - Test 4: 存在しないIDを指定するとNOT_FOUNDエラー
    - Test 5: R2削除失敗時（throw）でもDB削除は確定する
    - Test 6: 削除成功時、削除されたトラックのIDが返される
  </behavior>
  <action>
bgm.tsのbgmRouterにdelete mutationを追加：

1. インポート追加（まだの場合）：
```typescript
import { deleteBgmTrackSchema } from './_shared'
```

2. bgmRouterにdelete mutation追加（updateの後）：
```typescript
delete: adminProcedure
  .input(deleteBgmTrackSchema)
  .mutation(async ({ ctx, input }) => {
    const { db, schema } = ctx

    // 存在チェック（filename取得のため）
    const existing = await db
      .select()
      .from(schema.bgmTracks)
      .where(eq(schema.bgmTracks.id, input.id))
      .limit(1)

    if (!existing || existing.length === 0) {
      throw new TRPCError({
        code: 'NOT_FOUND',
        message: 'トラックが見つかりません',
      })
    }

    const track = existing[0]

    // DB削除（先に実行）
    await db
      .delete(schema.bgmTracks)
      .where(eq(schema.bgmTracks.id, input.id))

    // R2削除（失敗してもDB削除は確定）
    try {
      await ctx.env.BGM_BUCKET.delete(track.filename)
    } catch (error) {
      // ログ記録のみ、DB削除はロールバックしない
      console.error(`R2 delete failed for ${track.filename}:`, error)
    }

    return { id: input.id }
  })
```

実装の注意点：
- todos.tsのdeleteパターンを参考に、存在チェック→DB削除→R2削除の手順を実装
- **重要**: DB削除を先に実行し、その後R2削除を試行（CONTEXT.md決定）
- R2削除失敗時はtry-catchで捕捉し、console.errorでログ記録のみ
- エラーハンドリングはNOT_FOUNDのみ実装（R2削除失敗はエラーを返さない）
- エラーメッセージは日本語

CONTEXT.mdの決定事項を遵守：
> 削除対象: DBレコード + R2ファイル両方
> 順序: DB削除 → R2削除
> R2削除失敗時: DB削除は進める（整合性優先、ゴミは手動で対処）

RESEARCH.mdのPitfall 2に注意：R2削除失敗時にDB削除をロールバックしない。
  </action>
  <verify>
    <automated>grep -q "delete: adminProcedure" src/app/routers/bgm.ts && grep -q "deleteBgmTrackSchema" src/app/routers/bgm.ts && grep -q "BGM_BUCKET.delete" src/app/routers/bgm.ts && grep -q "console.error" src/app/routers/bgm.ts</automated>
  </verify>
  <done>
  - delete mutationがadminProcedureで保護されている
  - deleteBgmTrackSchemaでinputバリデーションされている
  - 存在チェックが実装されている
  - DB削除が先に実行されている
  - R2削除がtry-catchで保護されている
  - NOT_FOUNDエラーハンドリングが実装されている
  - R2削除失敗時のエラーログが実装されている
  - エラーメッセージが日本語
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>delete mutation実装完了：Zodスキーマ、tRPC mutation、DB→R2削除順序</what-built>
  <how-to-verify>
1. 管理者アカウントでログイン
2. ブラウザコンソールで以下を実行：
```javascript
// テスト用トラック作成
const created = await trpc.bgm.create.mutate({
  fileBase64: "//uQxAAAAAAAAAAAAAAAAAAAA...", // 小さいMP3 Base64
  title: "Delete Test Track",
  tier: "free"
})
console.log("Created:", created)

// 削除実行
const deleted = await trpc.bgm.delete.mutate({ id: created.id })
console.log("Deleted:", deleted)

// 削除確認
const tracks = await trpc.bgm.getAll.query()
console.log("Remaining tracks:", tracks.filter(t => t.id === created.id).length === 0)
```
3. 期待される結果：
   - deleted.idが作成したトラックのIDと一致
   - getAll結果に削除トラックが含まれない

4. R2確認（Wrangler CLI）：
```bash
wrangler r2 object pomdo-bgm <filename>
# → "404 Not Found" になるはず
```

5. エラーケース確認：
   - 存在しないID → NOT_FOUNDエラー
   - 一般ユーザー → FORBIDDENエラー

6. R2削除失敗シミュレーション（開発環境）：
   - 一時的にctx.env.BGM_BUCKET.deleteをモックでthrowするように変更
   - DB削除は成功し、R2削除エラーがコンソールに出力されることを確認
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
検証項目：
- [ ] deleteBgmTrackSchemaが定義されている
- [ ] bgmRouter.deleteがadminProcedureで保護されている
- [ ] 存在チェックが実装されている
- [ ] DB削除がR2削除より先に実行されている
- [ ] R2削除がtry-catchで保護されている
- [ ] NOT_FOUNDエラーハンドリングが実装されている
- [ ] 非管理者アクセスで403エラー
- [ ] 存在しないIDで404エラー
- [ ] R2削除失敗時でもDB削除が確定する
</verification>

<success_criteria>
- [ ] トラックを削除するとR2からもファイルが削除される
- [ ] 非管理者が書き込みAPIを呼ぶと403エラーになる
- [ ] 存在しないトラックIDを指定すると404エラーが返される
- [ ] R2削除失敗時でもDB削除は進める（整合性優先）
</success_criteria>

<output>
After completion, create `.planning/phases/04-bgm-api-write/04-03-SUMMARY.md`
</output>
