---
phase: 10-grid-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/DESIGN.md
autonomous: true
requirements:
  - GRID-02
  - GRID-04
must_haves:
  truths:
    - "ユーザーは全領域で一貫したガターサイズ（16px）を確認できる"
    - "ユーザーは各ブレイクポイントでグリッドが正しく表示されるのを確認できる"
    - "開発者はデザインシステムのルールを参照できる"
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "デザインシステム文書"
      contains: "Spacing scale"
  key_links:
    - from: "src/App.tsx"
      to: "Tailwind CSS"
      via: "gap-4クラス"
      pattern: "className=\".*gap-4"
---

<objective>
グリッドシステムの検証とデザインシステムの文書化を行う。

Purpose: Phase 08で修正されたグリッドシステムが正しく機能していることを確認し、開発者がデザインルールを参照できるようにDESIGN.mdを作成する。これにより、今後の開発で一貫したデザインを維持できるようになる。

Output:
- グリッドcol-span合計の検証（sm: 6列、lg: 12列）
- DESIGN.mdの作成（Spacing scale、Animation、Border radius、Z-index、Colors、Typography）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-grid-unification/10-CONTEXT.md
@.planning/phases/10-grid-unification/10-RESEARCH.md
@src/App.tsx
@src/index.css
@.planning/phases/08-responsive-fix/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from codebase -->

From src/App.tsx (lines 167-237):
```tsx
// Bento Grid: 12列システム
<div className="h-full grid grid-cols-1 sm:grid-cols-6 lg:grid-cols-12 gap-4">
  {/* Timer: sm:col-span-4, lg:col-span-8 */}
  {/* CurrentTask: sm:col-span-2, lg:col-span-2 */}
  {/* BGM: sm:col-span-2, lg:col-span-2 */}
  {/* Stats: sm:col-span-2, lg:col-span-4 */}
  {/* Todo: sm:col-span-6, lg:col-span-12 */}
</div>
```

From src/index.css (lines 268-281):
```css
.bento-card {
  background: color-mix(in srgb, var(--df-bg-elevated), transparent 40%);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--df-border-subtle);
  border-radius: 1.5rem;
  overflow: hidden;
  transition: all 0.3s ease;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: グリッドcol-span合計の検証</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lines 167-237)
  </read_first>
  <action>
    App.tsxのグリッド定義を検証：

    1. smブレイクポイント（6列）:
       - Timer: col-span-4
       - CurrentTask + BGM + Stats: col-span-2 + col-span-2 + col-span-2 = 6
       - Todo: col-span-6
       - 合計: 6列 ✓

    2. lgブレイクポイント（12列）:
       - Timer: col-span-8
       - CurrentTask + BGM + Stats: col-span-2 + col-span-2 + col-span-4 = 8
       - Todo: col-span-12
       - 合計: 12列 ✓

    検証結果:
    - Phase 08で修正済みであり、正しく実装されていることを確認
    - 問題がない場合は、grepコマンドでcol-spanの合計を確認

    確認コマンド:
    ```bash
    # smブレイクポイントのcol-span合計
    grep -o "sm:col-span-[0-9]" src/App.tsx | grep "sm:" | sed 's/sm:col-span-//' | awk '{sum += $1} END {print "sm合計:", sum}'

    # lgブレイクポイントのcol-span合計
    grep -o "lg:col-span-[0-9]" src/App.tsx | grep "lg:" | sed 's/lg:col-span-//' | awk '{sum += $1} END {print "lg合計:", sum}'
    ```
  </action>
  <verify>
    <automated>grep "gap-4" src/App.tsx | grep "grid" | head -1</automated>
  </verify>
  <done>
    - gap-4がグリッドに設定されていることを確認
    - グリッドcol-span合計が正しいことを確認（sm: 6列、lg: 12列）
  </done>
</task>

<task type="auto">
  <name>Task 2: DESIGN.mdの作成</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - src/index.css (lines 100-300)
    - .planning/phases/10-grid-unification/10-CONTEXT.md
  </read_first>
  <action>
    `.planning/DESIGN.md`を作成し、以下のセクションを含める：

    1. **Overview**:
       - Pomdoのデザインシステムの目的と原則
       - Deep Forestカラーパレットの採用

    2. **Spacing Scale**（Tailwind CSS v4ベース）:
       - 4px基数のspacing scale
       - gap: gap-4（16px）で統一
       - padding: 大きなカード（p-6: 24px）、小さなカード（p-4: 16px）
       - margin: 原則使用せず、gap/flex/grid spacingを使用

    3. **Animation**:
       - Duration: 0.2s（初期表示）、0.3s（hoverトランジション）
       - Easing: ease-out（初期表示）、ease（hover）
       - Framer Motion: layout="position"でレイアウトシフトを最適化

    4. **Border Radius**:
       - rounded-xl: 0.75rem（12px） - 小さな要素
       - rounded-3xl: 1.5rem（24px） - カード（.bento-card）
       - rounded-2xl: 1rem（16px） - 使用停止（bento-cardに統一）

    5. **Z-index Scale**:
       - .z-widget-bg: 10
       - .z-widget: 20
       - .z-widget-overlay: 30
       - .z-widget-modal: 9999

    6. **Colors**（Deep Forestカラーパレット）:
       - Primary: #22c55e（green）
       - Background: var(--df-background)
       - Elevated: var(--df-bg-elevated)
       - Border: var(--df-border-subtle)
       - Text: var(--df-text-primary), var(--df-text-secondary)
       - Subtext: var(--df-text-subtext)

    7. **Typography**:
       - Font Family: system-ui, -apple-system, sans-serif（font-displayクラス）
       - Font Sizes: text-xs（12px）、text-sm（14px）、text-base（16px）、text-lg（18px）、text-xl（20px）、text-2xl（24px）
       - Line Height: leading-tight（1.25）、leading-normal（1.5）

    8. **Card Design**:
       - 全カードで`.bento-card`クラスを使用
       - glassmorphism効果（backdrop-filter: blur(12px)）
       - hover時のアニメーション（border-colorとbox-shadow）

    9. **Grid System**:
       - 12列グリッドシステム（lg: grid-cols-12）
       - 6列グリッドシステム（sm: grid-cols-6）
       - 1列グリッドシステム（base: grid-cols-1）
       - gap-4（16px）で統一

    10. **Component Examples**:
        - TimerWidget: bento-card + p-6（大きなカード）
        - CurrentTaskCard: bento-card + p-6（大きなカード）
        - BgmPlayer: bento-card + p-4（小さなカード）
        - StatsCard: bento-card + p-6（大きなカード）
        - TodoList: bento-card + ルートにpaddingなし（子要素で制御）

    ファイルパス: `.planning/DESIGN.md`
  </action>
  <verify>
    <automated>test -f .planning/DESIGN.md && grep -q "Spacing Scale" .planning/DESIGN.md && grep -q "Animation" .planning/DESIGN.md && grep -q "Border Radius" .planning/DESIGN.md && grep -q "Z-index" .planning/DESIGN.md && grep -q "Colors" .planning/DESIGN.md && grep -q "Typography" .planning/DESIGN.md</automated>
  </verify>
  <done>
    - DESIGN.mdが作成されている
    - 全セクション（Overview、Spacing Scale、Animation、Border Radius、Z-index、Colors、Typography、Card Design、Grid System、Component Examples）が含まれている
    - 各セクションに具体的な値と使用例が記載されている
  </done>
</task>

</tasks>

<verification>
全タスク完了後の検証:
1. `npm run dev`でグリッドレイアウトが正しく表示されること（各ブレイクポイントで確認）
2. DESIGN.mdの全セクションが完成していること
3. DESIGN.mdの内容が実際のコードと一致していること
</verification>

<success_criteria>
- [ ] グリッドcol-span合計が正しいことを確認（sm: 6列、lg: 12列）
- [ ] gap-4が全グリッドで統一されていることを確認
- [ ] DESIGN.mdが作成されている
- [ ] DESIGN.mdに全セクションが含まれている
- [ ] DESIGN.mdの内容がコードと一致している
</success_criteria>

<output>
After completion, create `.planning/phases/10-grid-unification/10-02-SUMMARY.md`
</output>
