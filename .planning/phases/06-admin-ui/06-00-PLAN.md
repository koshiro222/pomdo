---
phase: 06-admin-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/layout/Header.test.tsx
  - src/components/bgm/AdminModal.test.tsx
  - src/components/bgm/TrackList.test.tsx
  - src/components/bgm/TrackItem.test.tsx
  - src/components/bgm/AddTrackForm.test.tsx
  - src/components/bgm/EditTrackDialog.test.tsx
  - src/components/bgm/DeleteConfirmDialog.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - 各コンポーネントのテストファイルが存在する
    - Testing Libraryのセットアップが正しく行われている
    - tRPCモックが適切に設定されている
  artifacts:
    - path: src/components/layout/Header.test.tsx
      provides: Headerコンポーネントの管理ボタン表示テスト
      min_lines: 20
    - path: src/components/bgm/AdminModal.test.tsx
      provides: AdminModalコンポーネントのテスト
      min_lines: 30
    - path: src/components/bgm/TrackList.test.tsx
      provides: TrackListコンポーネントのテスト
      min_lines: 30
    - path: src/components/bgm/TrackItem.test.tsx
      provides: TrackItemコンポーネントのテスト
      min_lines: 30
    - path: src/components/bgm/AddTrackForm.test.tsx
      provides: AddTrackFormコンポーネントのテスト
      min_lines: 40
    - path: src/components/bgm/EditTrackDialog.test.tsx
      provides: EditTrackDialogコンポーネントのテスト
      min_lines: 30
    - path: src/components/bgm/DeleteConfirmDialog.test.tsx
      provides: DeleteConfirmDialogコンポーネントのテスト
      min_lines: 25
  key_links:
    - from: src/test/setup.ts
      to: すべてのテストファイル
      via: Testing Library設定のインポート
      pattern: "@testing-library/react|vitest"
---

<objective>
各BGM管理UIコンポーネントのテストファイルを作成し、TDDサイクルの基盤を整える

Purpose: Wave 0でテストインフラを構築し、各実装タスクでRED→GREEN→REFACTORサイクルを回せるようにする
Output: 7つのテストファイル（Header, AdminModal, TrackList, TrackItem, AddTrackForm, EditTrackDialog, DeleteConfirmDialog）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-ui/06-CONTEXT.md
@.planning/phases/06-admin-ui/06-RESEARCH.md
@.planning/phases/06-admin-ui/06-VALIDATION.md
@src/test/setup.ts
@src/components/layout/Header.tsx
@src/components/dialogs/LoginDialog.tsx
@src/components/todos/TodoList.tsx
@src/hooks/useAuth.ts
@src/lib/animation.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Headerコンポーネントの管理ボタン表示テストを作成</name>
  <files>src/components/layout/Header.test.tsx</files>
  <behavior>
    - Test 1: 管理者ユーザー時に管理ボタン（Settingsアイコン）が表示される
    - Test 2: 非管理者ユーザー時に管理ボタンが表示されない
    - Test 3: 管理ボタンクリック時にAdminModalが開く
  </behavior>
  <action>
  Header.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `useAuth`フックをモックし、isAdminを制御可能にする
     - `useState`をモックし、showAdminModal状態を追跡可能にする

  2. **テストケース実装**:
     ```typescript
     describe('Header - Admin Button', () => {
       it('should show admin button when user is admin', () => {
         // useAuthモックでisAdmin=trueを設定
         // Headerをレンダリング
         // Settingsアイコン（管理ボタン）が存在することを確認
       })

       it('should not show admin button when user is not admin', () => {
         // useAuthモックでisAdmin=falseを設定
         // Headerをレンダリング
         // Settingsアイコンが存在しないことを確認
       })

       it('should open AdminModal when admin button is clicked', () => {
         // useAuthモックでisAdmin=trueを設定
         // useStateモックでshowAdminModalのsetterを捕捉
         // 管理ボタンをクリック
         // AdminModalのisOpenがtrueに設定されることを確認
       })
     })
     ```

  **モック設定**: `vi.mock('@/hooks/useAuth')`を使用して、isAdminを制御
  **レンダリング**: Testing Libraryの`render`を使用
  **クエリ**: `getByRole`、`getByTitle`、`queryByRole`を使用
  </action>
  <verify>
    <automated>npm test -- src/components/layout/Header.test.ts</automated>
  </verify>
  <done>
  - 管理者時に管理ボタンが表示されるテストがパスする
  - 非管理者時に管理ボタンが表示されないテストがパスする
  - 管理ボタンクリックでAdminModalが開くテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminModalコンポーネントのテストを作成</name>
  <files>src/components/bgm/AdminModal.test.tsx</files>
  <behavior>
    - Test 1: isOpen=true時にモーダルが表示される
    - Test 2: isOpen=false時にモーダルが表示されない
    - Test 3: 背景クリックでonCloseが呼ばれる
    - Test 4: EscapeキーでonCloseが呼ばれる
    - Test 5: 閉じるボタン（X）でonCloseが呼ばれる
    - Test 6: mode='list'時にTrackListが表示される
  </behavior>
  <action>
  AdminModal.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `createPortal`をモックし、ポータル内のDOMをテスト可能にする
     - Framer Motionの`AnimatePresence`、`motion.div`をモック

  2. **テストケース実装**:
     ```typescript
     describe('AdminModal', () => {
       it('should render modal when isOpen is true', () => {
         // AdminModalをisOpen=trueでレンダリング
         // "BGM管理"というテキストが存在することを確認
       })

       it('should not render modal when isOpen is false', () => {
         // AdminModalをisOpen=falseでレンダリング
         // モーダルコンテンツが存在しないことを確認
       })

       it('should call onClose when backdrop is clicked', () => {
         // onCloseモック関数を作成
         // AdminModalをレンダリング
         // 背景要素をクリック
         // onCloseが呼ばれることを確認
       })

       it('should call onClose when Escape key is pressed', () => {
         // fireEvent.keyDown(document, { key: 'Escape' })
         // onCloseが呼ばれることを確認
       })

       it('should call onClose when close button (X) is clicked', () => {
         // 閉じるボタンをクリック
         // onCloseが呼ばれることを確認
       })

       it('should render TrackList in list mode', () => {
         // mode='list'でレンダリング
         // TrackListコンポーネントが表示されることを確認
       })
     })
     ```

  **モック設定**: `vi.mock('react-dom', () => ({ createPortal: vi.fn((children) => children) }))`
  **イベント発火**: `fireEvent.click`、`fireEvent.keyDown`を使用
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/AdminModal.test.ts</automated>
  </verify>
  <done>
  - isOpen状態に応じてモーダルが表示/非表示になるテストがパスする
  - 背景クリック、Escapeキー、閉じるボタンでonCloseが呼ばれるテストがパスする
  - modeに応じて適切なコンテンツが表示されるテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TrackListコンポーネントのテストを作成</name>
  <files>src/components/bgm/TrackList.test.tsx</files>
  <behavior>
    - Test 1: トラック一覧が表示される
    - Test 2: 空状態時にメッセージが表示される
    - Test 3: ローディング時に"Loading..."が表示される
    - Test 4: 追加ボタンクリックでonAddが呼ばれる
  </behavior>
  <action>
  TrackList.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `trpc.bgm.getAll.useQuery`をモックし、tracksデータを制御可能にする

  2. **テストケース実装**:
     ```typescript
     describe('TrackList', () => {
       it('should render track list', () => {
         // tRPCモックでtracksデータを設定
         // TrackListをレンダリング
         // トラックアイテムが表示されることを確認
       })

       it('should render empty state when no tracks', () => {
         // tRPCモックで空配列を設定
         // TrackListをレンダリング
         // "トラックがありません"が表示されることを確認
       })

       it('should render loading state', () => {
         // tRPCモックでisLoading=trueを設定
         // TrackListをレンダリング
         // "Loading..."が表示されることを確認
       })

       it('should call onAdd when add button is clicked', () => {
         // onAddモック関数を作成
         // 追加ボタンをクリック
         // onAddが呼ばれることを確認
       })
     })
     ```

  **モック設定**: `vi.mock('@/lib/trpc')`を使用してtRPCクエリをモック
  **アサーション**: `getByText`、`queryAllByText`を使用
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/TrackList.test.ts</automated>
  </verify>
  <done>
  - トラック一覧が正しく表示されるテストがパスする
  - 空状態とローディング状態のテストがパスする
  - 追加ボタンでonAddが呼ばれるテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: TrackItemコンポーネントのテストを作成</name>
  <files>src/components/bgm/TrackItem.test.tsx</files>
  <behavior>
    - Test 1: トラック情報（タイトル、アーティスト、色、tier）が表示される
    - Test 2: 編集ボタンクリックで編集ダイアログが開く
    - Test 3: 削除ボタンクリックで削除確認ダイアログが開く
    - Test 4: ホバー時にアクションボタンが表示される
  </behavior>
  <action>
  TrackItem.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - Framer Motionの`motion.div`をモック

  2. **テストケース実装**:
     ```typescript
     describe('TrackItem', () => {
       const mockTrack = {
         id: '1',
         title: 'Test Track',
         artist: 'Test Artist',
         color: '#3b82f6',
         tier: 'free',
         createdAt: new Date(),
         updatedAt: new Date(),
       }

       it('should render track information', () => {
         // TrackItemをレンダリング
         // タイトル、アーティスト、tierバッジが表示されることを確認
         // 色プレビューのbackgroundColorが正しいことを確認
       })

       it('should show edit dialog when edit button is clicked', () => {
         // useStateモックでshowEditDialogのsetterを捕捉
         // 編集ボタンをクリック
         // showEditDialogがtrueに設定されることを確認
       })

       it('should show delete confirm dialog when delete button is clicked', () => {
         // useStateモックでshowDeleteConfirmのsetterを捕捉
         // 削除ボタンをクリック
         // showDeleteConfirmがtrueに設定されることを確認
       })

       it('should show action buttons on hover', () => {
         // TrackItemをレンダリング
         // デフォルトでアクションボタンが非表示であることを確認（opacity-0）
         // ホバー時にボタンが表示されることを確認
       })
     })
     ```

  **スタイル検証**: `toHaveStyle`を使用して色プレビューのbackgroundColorを確認
  **ホバーシミュレーション**: `fireEvent.mouseEnter`、`fireEvent.mouseLeave`を使用
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/TrackItem.test.ts</automated>
  </verify>
  <done>
  - トラック情報が正しく表示されるテストがパスする
  - 編集・削除ボタンでダイアログが開くテストがパスする
  - ホバー時にアクションボタンが表示されるテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: AddTrackFormコンポーネントのテストを作成</name>
  <files>src/components/bgm/AddTrackForm.test.tsx</files>
  <behavior>
    - Test 1: フォーム入力が正しく動作する
    - Test 2: ファイル選択時のバリデーションが動作する（サイズ、タイプ）
    - Test 3: 送信時にcreate mutationが呼ばれる
    - Test 4: 送信成功時にトースト通知が表示され、onBackが呼ばれる
    - Test 5: エラー時にエラートーストが表示される
    - Test 6: ローディング時にボタンが無効化される
  </behavior>
  <action>
  AddTrackForm.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `trpc.bgm.create.useMutation`をモック
     - `useUiStore`をモックし、トースト通知を追跡可能にする
     - `FileReader`をモックし、Base64変換をシミュレート

  2. **テストケース実装**:
     ```typescript
     describe('AddTrackForm', () => {
       it('should update form fields on input', () => {
         // タイトル入力フィールドに値を入力
         // アーティスト入力フィールドに値を入力
         // 色選択inputで色を変更
         // tier選択selectで値を変更
         // 各フィールドの値が正しく更新されることを確認
       })

       it('should validate file size (max 10MB)', () => {
         // 10MBを超えるファイルを選択
         // ファイルエラーが表示されることを確認
       })

       it('should validate file type (MP3 only)', () => {
         // MP3以外のファイルを選択
         // ファイルエラーが表示されることを確認
       })

       it('should call create mutation on submit', () => {
         // フォームを入力
         // 送信ボタンをクリック
         // create mutationが呼ばれることを確認
       })

       it('should show success toast and call onBack when successful', () => {
         // create mutationを成功させる
         // トースト通知が表示されることを確認
         // onBackが呼ばれることを確認
       })

       it('should show error toast when mutation fails', () => {
         // create mutationを失敗させる
         // エラートーストが表示されることを確認
       })

       it('should disable button while loading', () => {
         // create mutationのisLoadingをtrueに設定
         // 送信ボタンがdisabledであることを確認
       })
     })
     ```

  **ファイルモック**: `new File(['content'], 'test.mp3', { type: 'audio/mpeg' })`
  **Base64モック**: `vi.spyOn(FileReader.prototype, 'readAsDataURL')`
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/AddTrackForm.test.ts</automated>
  </verify>
  <done>
  - フォーム入力とバリデーションのテストがパスする
  - 送信成功・失敗時のテストがパスする
  - ローディング状態のテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: EditTrackDialogコンポーネントのテストを作成</name>
  <files>src/components/bgm/EditTrackDialog.test.tsx</files>
  <behavior>
    - Test 1: 既存値がフォームに表示される
    - Test 2: フォーム入力が正しく動作する
    - Test 3: 送信時にupdate mutationが呼ばれる
    - Test 4: 送信成功時にトースト通知が表示され、onCloseが呼ばれる
    - Test 5: キャンセルボタンでonCloseが呼ばれる
  </behavior>
  <action>
  EditTrackDialog.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `trpc.bgm.update.useMutation`をモック
     - `useUiStore`をモック

  2. **テストケース実装**:
     ```typescript
     describe('EditTrackDialog', () => {
       const mockTrack = {
         id: '1',
         title: 'Original Title',
         artist: 'Original Artist',
         color: '#3b82f6',
         tier: 'free',
         createdAt: new Date(),
         updatedAt: new Date(),
       }

       it('should render with existing values', () => {
         // EditTrackDialogをレンダリング
         // 各フィールドに既存値が表示されることを確認
       })

       it('should update form fields on input', () => {
         // 各フィールドの値を変更
         // 値が正しく更新されることを確認
       })

       it('should call update mutation on submit', () => {
         // フォームを更新
         // 送信ボタンをクリック
         // update mutationが正しいデータで呼ばれることを確認
       })

       it('should show success toast and call onClose when successful', () => {
         // update mutationを成功させる
         // トースト通知が表示されることを確認
         // onCloseが呼ばれることを確認
       })

       it('should call onClose when cancel button is clicked', () => {
         // キャンセルボタンをクリック
         // onCloseが呼ばれることを確認
       })
     })
     ```

  **初期値検証**: `getByDisplayValue`を使用してinputの初期値を確認
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/EditTrackDialog.test.ts</automated>
  </verify>
  <done>
  - 既存値が正しく表示されるテストがパスする
  - フォーム更新と送信のテストがパスする
  - 成功・キャンセル時のテストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: DeleteConfirmDialogコンポーネントのテストを作成</name>
  <files>src/components/bgm/DeleteConfirmDialog.test.tsx</files>
  <behavior>
    - Test 1: 削除確認メッセージが表示される
    - Test 2: 削除ボタンクリックでdelete mutationが呼ばれる
    - Test 3: 削除成功時にトースト通知が表示され、onCloseが呼ばれる
    - Test 4: キャンセルボタンでonCloseが呼ばれる
  </behavior>
  <action>
  DeleteConfirmDialog.test.tsxファイルを作成し、以下のテストを実装：

  1. **前提条件のセットアップ**:
     - `trpc.bgm.delete.useMutation`をモック
     - `useUiStore`をモック

  2. **テストケース実装**:
     ```typescript
     describe('DeleteConfirmDialog', () => {
       const mockTrack = {
         id: '1',
         title: 'Test Track',
         artist: 'Test Artist',
         color: '#3b82f6',
         tier: 'free',
         createdAt: new Date(),
         updatedAt: new Date(),
       }

       it('should render confirmation message', () => {
         // DeleteConfirmDialogをレンダリング
         // 「「{track.title}」を削除しますか？」が表示されることを確認
       })

       it('should call delete mutation when delete button is clicked', () => {
         // 削除ボタンをクリック
         // delete mutationが正しいIDで呼ばれることを確認
       })

       it('should show success toast and call onClose when successful', () => {
         // delete mutationを成功させる
         // トースト通知が表示されることを確認
         // onCloseが呼ばれることを確認
       })

       it('should call onClose when cancel button is clicked', () => {
         // キャンセルボタンをクリック
         // onCloseが呼ばれることを確認
       })
     })
     ```

  **メッセージ検証**: `getByText`を使用して確認メッセージを確認
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/DeleteConfirmDialog.test.ts</automated>
  </verify>
  <done>
  - 確認メッセージが正しく表示されるテストがパスする
  - 削除実行のテストがパスする
  - 成功・キャンセル時のテストがパスする
  </done>
</task>

</tasks>

<verification>
各テストファイルが正しく作成され、Vitestで実行可能であることを確認
</verification>

<success_criteria>
- 7つのテストファイルが作成されている
- 各テストファイルがVitestで実行可能である
- テストがRED状態（実装前）で正しく失敗する
- モックが正しく設定されている
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui/06-00-SUMMARY.md`
</output>
