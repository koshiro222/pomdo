---
phase: 21-stats-week-start
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/stats/StatsCard.tsx, src/components/stats/StatsCard.test.tsx]
autonomous: false
requirements: [STAT-01]
user_setup: []

must_haves:
  truths:
    - "グラフのX軸に日曜始まりで「Sun Mon Tue Wed Thu Fri Sat」と表示される"
    - "日曜日のデータがグラフの最初（左端）に表示される"
    - "Today表示の場合、正しく'Today'と表示され曜日がずれない"
    - "Weekタブ切り替え時に曜日順序が維持される"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "getDayLabel()関数（日曜始まり実装）"
      contains: "const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']"
      min_lines: 1
    - path: "src/components/stats/StatsCard.test.tsx"
      provides: "曜日表示検証テスト"
      contains: "曜日配列または日曜始まりの検証ロジック"
      min_lines: 1
  key_links:
    - from: "getDayLabel()"
      to: "weeklyData[].date"
      via: "関数呼び出し"
      pattern: "date: getDayLabel\\(dateStr\\)"
    - from: "weeklyData[].date"
      to: "XAxis"
      via: "dataKey=\"date\""
      pattern: "dataKey=\"date\""
---

<objective>
曜日表示を月曜始まりから日曜始まりに変更し、日本のカレンダー文化に合わせる

Purpose: ユーザーが直感的に週の始まりを理解できるように、グラフのX軸を日曜始まりで表示する
Output: 日曜始まりの曜日配列と、これを使用したグラフ表示
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/021-stats-week-start/021-RESEARCH.md
@.planning/phases/021-stats-week-start/021-VALIDATION.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 既存の getDayLabel() 実装（月曜始まり） -->
From src/components/stats/StatsCard.tsx (line 92-102):
```typescript
function getDayLabel(dateStr: string): string {
  const date = new Date(dateStr)
  const today = new Date()
  const isToday = date.toDateString() === today.toDateString()

  if (isToday) return 'Today'

  const days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
  const dayIndex = (date.getDay() + 6) % 7
  return days[dayIndex]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getDayLabel()関数を日曜始まりに修正</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（line 92-102の既存実装を確認）
  </read_first>
  <behavior>
    - テスト1: 日曜日（getDay()=0）のdateStrを渡すと「Sun」が返る
    - テスト2: 月曜日（getDay()=1）のdateStrを渡すと「Mon」が返る
    - テスト3: 土曜日（getDay()=6）のdateStrを渡すと「Sat」が返る
    - テスト4: 今日のdateStrを渡すと「Today」が返る（曜日計算より優先）
  </behavior>
  <action>
    getDayLabel()関数内の曜日配列を以下のように変更：

    変更前:
    ```typescript
    const days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
    const dayIndex = (date.getDay() + 6) % 7
    ```

    変更後:
    ```typescript
    const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
    const dayIndex = date.getDay()
    ```

    「今日」判定ロジック（isToday）は変更せず、曜日配列とインデックス計算のみ修正する。
    関数シグネチャと返り値の型は変更しない。
  </action>
  <verify>
    <automated>npm test -- --run StatsCard.test.tsx</automated>
  </verify>
  <done>
    - getDayLabel()関数が日曜始まりの曜日を返す
    - JavaScript標準のDate.getDay()（日曜=0...土曜=6）を直接使用
    - 「Today」表示ロジックが維持されている
  </done>
  <acceptance_criteria>
    - src/components/stats/StatsCard.tsx contains "const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']"
    - src/components/stats/StatsCard.tsx contains "const dayIndex = date.getDay()"
    - src/components/stats/StatsCard.tsx does NOT contain "(date.getDay() + 6) % 7"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: テスト内の曜日ハードコードを更新</name>
  <files>src/components/stats/StatsCard.test.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.test.tsx（line 152付近の曜日ハードコードを確認）
  </read_first>
  <action>
    StatsCard.test.tsx内の曜日ハードコードを更新：

    1. テストデータ構造内の曜日文字列を確認（line 152付近）
    2. 'Mon'等の曜日ハードコードがある場合、日曜始まりに合わせて調整
    3. ただし、このテストは「データ構造の検証」であるため、曜日文字列自体はテストデータとして任意の値でよい
    4. 重要なのは、getDayLabel()関数が正しく曜日を返すこと（Task 1で検証済み）

    注: テストデータ内の曜日文字列は、累積計算ロジックの検証用であり、
    実際のグラフ表示には影響しないため、変更は任意とする。
    必要に応じてコメントを追加し、日曜始まり対応を明記する。
  </action>
  <verify>
    <automated>npm test -- --run StatsCard.test.tsx</automated>
  </verify>
  <done>
    - テストが全てパスする
    -曜日に関するテストデータに矛盾がない
  </done>
  <acceptance_criteria>
    - `npm test -- --run StatsCard.test.tsx` exits with code 0
    - テスト内の曜日文字列に一貫性がある（'Mon'等のハードコードがあれば、コメント等で意図を明記）
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: グラフ表示の手動検証</name>
  <files></files>
  <action></action>
  <verify></verify>
  <done>
    - グラフのX軸が日曜始まりで表示されている
    - Today表示が正しく動作している
    - タブ切り替え時に曜日順序が維持されている
  </done>
  <what-built>
    - getDayLabel()関数の日曜始まり実装
    - 更新されたテストコード
  </what-built>
  <how-to-verify>
    1. アプリケーションを起動: `npm run dev`
    2. Statsカードの「Week」タブをクリック
    3. グラフのX軸ラベルを確認
    4. 期待される表示: 左から「Sun, Mon, Tue, Wed, Thu, Fri, Sat」の順
    5. 今日が日曜日の場合、「Today」と表示され、それ以外は曜日が正しく表示されることを確認
    6. 「Today」「Week」「Month」タブを切り替え、曜日順序が維持されていることを確認
  </how-to-verify>
  <resume-signal>
    グラフのX軸が日曜始まりで正しく表示されていることを確認したら「approved」と入力してください。
    問題がある場合は詳細を教えてください。
  </resume-signal>
</task>

</tasks>

<verification>
1. npm test -- --run StatsCard.test.tsx が全てパスすること
2. グラフのX軸に日曜始まりで曜日が表示されること
3. 曜日と実際の日付が正しく対応していること（手動検証）
4. Today表示が正しく動作すること（手動検証）
</verification>

<success_criteria>
1. グラフX軸の曜日が日曜始まり（Sun, Mon, Tue...）で表示される
2. 全ての自動テストがパスする
3. 手動検証で曜日順序とToday表示が正しいことを確認
</success_criteria>

<output>
After completion, create `.planning/phases/021-stats-week-start/021-01-SUMMARY.md`
</output>
