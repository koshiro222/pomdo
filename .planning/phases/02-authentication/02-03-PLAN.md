---
phase: 02-authentication
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - drizzle/meta/_journal.json
autonomous: false
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "本番DBのusersテーブルにroleカラムが存在する"
    - "開発者のユーザーがadminロールを持っている"
    - "マイグレーションが正常に適用されている"
  artifacts:
    - path: "drizzle/0006_*.sql"
      provides: "roleカラム追加 + 初期管理者登録のマイグレーション"
      contains: "ALTER TABLE \"users\" ADD COLUMN \"role\""
    - path: "drizzle/meta/_journal.json"
      provides: "マイグレーション履歴"
      contains: "0006"
  key_links:
    - from: "drizzle/0006_*.sql"
      to: "users table"
      via: "ALTER TABLE"
      pattern: "ALTER TABLE.*users.*role"
---

<objective>
マイグレーションを生成して本番DBに適用し、初期管理者ユーザーを登録する

Purpose: Better Auth Admin Pluginで使用するroleカラムを本番DBに追加し、開発者をadminとして設定する
Output: マイグレーションファイル、本番DBでのroleカラム、初期管理者登録
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From drizzle/0005_smooth_master_chief.sql (existing pattern):
```sql
CREATE TABLE "bgm_tracks" (
  "id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  "title" text NOT NULL,
  ...
);
-->statement-breakpoint
INSERT INTO "bgm_tracks" (...) VALUES (...);
```

From .planning/phases/02-authentication/02-RESEARCH.md:
```sql
-- Admin Pluginがroleカラムを追加
ALTER TABLE "users" ADD COLUMN "role" text DEFAULT 'user' NOT NULL;
-->statement-breakpoint
ALTER TABLE "users" ADD COLUMN "banned" boolean DEFAULT false NOT NULL;
-->statement-breakpoint
ALTER TABLE "users" ADD COLUMN "ban_reason" text;
-->statement-breakpoint
ALTER TABLE "users" ADD COLUMN "ban_expires" timestamp;
-->statement-breakpoint
-- 初期管理者を登録（開発者のメールアドレス）
UPDATE "users" SET "role" = 'admin' WHERE "email" = 'developer@example.com';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: マイグレーションファイルを生成</name>
  <files>drizzle/0006_*.sql, drizzle/meta/0006_snapshot.json, drizzle/meta/_journal.json</files>
  <read_first>
    - functions/lib/schema.ts
    - drizzle/meta/_journal.json
  </read_first>
  <action>
    npm run db:generate を実行して、schema.ts の変更（role関連カラム追加）をマイグレーションファイルとして生成する。

    実行コマンド:
    ```bash
    npm run db:generate
    ```

    期待される結果:
    - drizzle/0006_*.sql が生成される
    - 内容には以下のSQLが含まれる:
      - ALTER TABLE "users" ADD COLUMN "role" text DEFAULT 'user' NOT NULL
      - ALTER TABLE "users" ADD COLUMN "banned" boolean DEFAULT false NOT NULL
      - ALTER TABLE "users" ADD COLUMN "ban_reason" text
      - ALTER TABLE "users" ADD COLUMN "ban_expires" timestamp

    注意: 生成されたファイル名はランダムなサフィックスが付くため、実際のファイル名を確認すること
  </action>
  <verify>
    <automated>ls drizzle/0006_*.sql 2>/dev/null && grep -q "role" drizzle/0006_*.sql</automated>
  </verify>
  <acceptance_criteria>
    - drizzle/0006_*.sql ファイルが存在する
    - ファイル内に `ALTER TABLE "users" ADD COLUMN "role"` が存在する
    - ファイル内に `ALTER TABLE "users" ADD COLUMN "banned"` が存在する
  </acceptance_criteria>
  <done>マイグレーションファイル0006が生成されている</done>
</task>

<task type="auto">
  <name>Task 2: 初期管理者登録SQLを追記</name>
  <files>drizzle/0006_*.sql</files>
  <read_first>
    - drizzle/0006_*.sql
  </read_first>
  <action>
    生成されたマイグレーションファイルの末尾に、初期管理者を登録するUPDATE文を追記する。

    追記内容（-->statement-breakpointの後に追加）:
    ```sql
-->statement-breakpoint
-- 初期管理者を登録（開発者のメールアドレス）
UPDATE "users" SET "role" = 'admin' WHERE "email" = 'koshiro@mudef.net';
```

    注意:
    - 開発者のメールアドレスは 'koshiro@mudef.net' を使用
    - -->statement-breakpoint は Drizzle のステートメント区切りとして必須
  </action>
  <verify>
    <automated>grep -q "UPDATE.*users.*admin.*koshiro@mudef.net" drizzle/0006_*.sql</automated>
  </verify>
  <acceptance_criteria>
    - drizzle/0006_*.sql に `UPDATE "users" SET "role" = 'admin'` が存在する
    - drizzle/0006_*.sql に `WHERE "email" = 'koshiro@mudef.net'` が存在する
  </acceptance_criteria>
  <done>初期管理者登録SQLがマイグレーションファイルに追記されている</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    マイグレーションファイル（0006）が生成され、初期管理者登録SQLが追記されている
  </what-built>
  <how-to-verify>
    1. `cat drizzle/0006_*.sql` でマイグレーション内容を確認
    2. 以下が含まれていることを確認:
       - ALTER TABLE "users" ADD COLUMN "role"
       - ALTER TABLE "users" ADD COLUMN "banned"
       - UPDATE "users" SET "role" = 'admin' WHERE "email" = 'koshiro@mudef.net'
    3. 問題なければ "approved" と入力
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: マイグレーションを本番DBに適用</name>
  <files>drizzle/meta/_journal.json</files>
  <read_first>
    - drizzle/0006_*.sql
  </read_first>
  <action>
    npm run db:migrate を実行して、マイグレーションを本番DBに適用する。

    実行コマンド:
    ```bash
    npm run db:migrate
    ```

    期待される結果:
    - "Everything's fine 🚀" または類似の成功メッセージが表示される
    - users テーブルに role, banned, ban_reason, ban_expires カラムが追加される
    - koshiro@mudef.net のユーザーが admin ロールになる

    注意:
    - 本番DBに接続するため、DATABASE_URL 環境変数が正しく設定されていること
    - エラーが発生した場合は、マイグレーションファイルの内容を確認すること
  </action>
  <verify>
    <automated>npm run db:migrate 2>&1 | grep -q "fine\|success\|applied" || echo "Check migration output manually"</automated>
  </verify>
  <acceptance_criteria>
    - npm run db:migrate がエラーなしで完了する
  </acceptance_criteria>
  <done>マイグレーションが本番DBに適用されている</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    マイグレーションが本番DBに適用され、usersテーブルにroleカラムが追加されている
  </what-built>
  <how-to-verify>
    1. `npm run db:studio` で Drizzle Studio を起動
    2. users テーブルを開く
    3. 以下を確認:
       - role, banned, ban_reason, ban_expires カラムが存在する
       - koshiro@mudef.net のユーザーの role が 'admin' になっている
       - 他のユーザーの role が 'user' になっている
    4. 問題なければ "approved" と入力
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 2 Plan 3の完了条件:
- [ ] マイグレーションファイル0006が生成されている
- [ ] 初期管理者登録SQLが追記されている
- [ ] マイグレーションが本番DBに適用されている
- [ ] Drizzle Studioでroleカラムと初期管理者を確認できている
</verification>

<success_criteria>
1. drizzle/0006_*.sql が存在し、roleカラム追加と初期管理者登録のSQLが含まれている
2. npm run db:migrate が成功している
3. Drizzle Studio で users テーブルの role カラムを確認できる
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
