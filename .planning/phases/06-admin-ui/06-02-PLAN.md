---
phase: 06-admin-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-00", "06-01"]
files_modified:
  - src/components/bgm/TrackList.tsx
  - src/components/bgm/TrackItem.tsx
  - src/components/bgm/AddTrackForm.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-07
must_haves:
  truths:
    - トラック一覧が表示される
    - 空状態時に「トラックがありません」メッセージが表示される
    - ローディング時に「Loading...」が表示される
    - 追加ボタンクリックでmodeが'add'に切り替わる
    - 各トラックアイテムに編集・削除ボタンがある（ホバー時表示）
    - ファイル選択でバリデーションが動作する（10MB制限、MP3のみ）
    - 追加成功時にトースト通知が表示され、リストが更新される
  artifacts:
    - path: src/components/bgm/TrackList.tsx
      provides: トラック一覧表示
      min_lines: 60
      exports: ["TrackList"]
    - path: src/components/bgm/TrackItem.tsx
      provides: 個別トラックアイテム
      min_lines: 100
      exports: ["TrackItem"]
    - path: src/components/bgm/AddTrackForm.tsx
      provides: トラック追加フォーム
      min_lines: 160
      exports: ["AddTrackForm"]
  key_links:
    - from: src/components/bgm/TrackList.tsx
      to: src/app/routers/bgm.ts
      via: trpc.bgm.getAll.useQuery
      pattern: "trpc\.bgm\.getAll\.useQuery"
    - from: src/components/bgm/TrackItem.tsx
      to: src/lib/animation.ts
      via: slideInVariants, tapAnimationのインポート
      pattern: "slideInVariants|tapAnimation"
    - from: src/components/bgm/AddTrackForm.tsx
      to: src/app/routers/bgm.ts
      via: trpc.bgm.create.useMutation
      pattern: "trpc\.bgm\.create\.useMutation"
    - from: src/components/bgm/AddTrackForm.tsx
      to: src/core/store/ui.ts
      via: useUiStoreでトースト通知
      pattern: "useUiStore"
---

<objective>
トラック一覧表示（TrackList/TrackItem）と追加フォーム（AddTrackForm）を実装する

Purpose: 管理者がトラックを閲覧・追加できるUIを実装
Output: TrackList、TrackItem、AddTrackFormコンポーネント
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-ui/06-CONTEXT.md
@.planning/phases/06-admin-ui/06-RESEARCH.md
@.planning/phases/06-admin-ui/06-UI-SPEC.md
@src/components/todos/TodoList.tsx
@src/components/todos/TodoItem.tsx
@src/components/dialogs/LoginDialog.tsx
@src/app/routers/bgm.ts
@src/core/store/ui.ts
@src/lib/animation.ts
@src/test/setup.ts
@src/components/bgm/TrackList.test.tsx
@src/components/bgm/TrackItem.test.tsx
@src/components/bgm/AddTrackForm.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrackListコンポーネントを実装</name>
  <files>src/components/bgm/TrackList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx
    - src/app/routers/bgm.ts
  </read_first>
  <behavior>
    - Test 1: trpc.bgm.getAll.useQueryでトラックを取得
    - Test 2: トラック一覧が表示される
    - Test 3: 空状態時に「トラックがありません」が表示される
    - Test 4: ローディング時に「Loading...」が表示される
    - Test 5: 追加ボタンクリックでonAddが呼ばれる
  </behavior>
  <action>
  TrackList.tsxファイルを作成し、以下の実装を行う：

  1. **ファイル作成とインポート**:
     ```typescript
     import { AnimatePresence } from 'framer-motion'
     import { Plus } from 'lucide-react'
     import { trpc } from '@/lib/trpc'
     import { tapAnimation, hoverAnimation } from '@/lib/animation'
     import { TrackItem } from './TrackItem'

     interface TrackListProps {
       onAdd: () => void
     }
     ```

  2. **コンポーネント実装**:
     ```typescript
     export function TrackList({ onAdd }: TrackListProps) {
       const { data: tracks, isLoading } = trpc.bgm.getAll.useQuery()

       if (isLoading) {
         return <div className="text-cf-subtext">Loading...</div>
       }

       return (
         <div>
           {/* ヘッダー：タイトルと追加ボタン */}
           <div className="flex items-center justify-between mb-4">
             <h4 className="text-sm font-bold text-cf-text">トラック一覧</h4>
             <motion.button
               {...tapAnimation}
               {...hoverAnimation}
               onClick={onAdd}
               className="flex items-center gap-2 px-3 py-1.5 bg-cf-primary text-white rounded-lg text-sm"
             >
               <Plus size={16} />
               追加
             </motion.button>
           </div>

           {/* トラックリスト */}
           <div className="flex flex-col gap-2 max-h-96 overflow-y-auto">
             {tracks.length === 0 ? (
               <div className="text-cf-subtext text-center py-8">
                 トラックがありません
               </div>
             ) : (
               <AnimatePresence mode="popLayout">
                 {tracks.map((track) => (
                   <TrackItem key={track.id} track={track} />
                 ))}
               </AnimatePresence>
             )}
           </div>
         </div>
       )
     }
     ```

  **TodoListパターンの再利用**:
  - `AnimatePresence mode="popLayout"`でアニメーション
  - 空状態: `text-cf-subtext text-center py-8`
  - ローディング: シンプルなテキスト表示

  **確認事項**:
  - `trpc.bgm.getAll.useQuery()`を使用
  - 空状態メッセージは「トラックがありません」
  - 追加ボタンは`onAdd`コールバックを実行
  - max-h-96で最大高さ制限、overflow-y-autoでスクロール
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/TrackList.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/TrackList.tsxが存在する
    - TrackListコンポーネントがexportされている
    - `trpc.bgm.getAll.useQuery()`を使用している
    - 空状態メッセージ「トラックがありません」が含まれる
    - 追加ボタンにPlusアイコンがある
    - テスト: `npm test -- src/components/bgm/TrackList.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - トラック一覧が正しく表示される
  - 空状態とローディング状態が表示される
  - 追加ボタンでonAddコールバックが呼ばれる
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrackItemコンポーネントを実装</name>
  <files>src/components/bgm/TrackItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx
    - src/lib/animation.ts
  </read_first>
  <behavior>
    - Test 1: トラック情報（タイトル、アーティスト、色、tier）が表示される
    - Test 2: 編集ボタンクリックで編集ダイアログが開く
    - Test 3: 削除ボタンクリックで削除確認ダイアログが開く
    - Test 4: ホバー時にアクションボタンが表示される
  </behavior>
  <action>
  TrackItem.tsxファイルを作成し、以下の実装を行う：

  1. **ファイル作成とインポート**:
     ```typescript
     import { useState } from 'react'
     import { motion } from 'framer-motion'
     import { Edit2, Trash2 } from 'lucide-react'
     import { slideInVariants, tapAnimation } from '@/lib/animation'
     import type { BgmTrack } from '@/app/routers/_shared'
     import { EditTrackDialog } from './EditTrackDialog'
     import { DeleteConfirmDialog } from './DeleteConfirmDialog'
     ```

  2. **コンポーネント実装**:
     ```typescript
     interface TrackItemProps {
       track: BgmTrack
     }

     export function TrackItem({ track }: TrackItemProps) {
       const [showDeleteConfirm, setShowDeleteConfirm] = useState(false)
       const [showEditDialog, setShowEditDialog] = useState(false)

       return (
         <>
           <motion.div
             variants={slideInVariants}
             initial="visible"
             animate="visible"
             className="group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors"
           >
             {/* 色プレビュー */}
             <div
               className="w-4 h-4 rounded-full"
               style={{ backgroundColor: track.color || '#3b82f6' }}
             />

             {/* トラック情報 */}
             <div className="flex-1">
               <p className="text-sm font-medium text-cf-text">{track.title}</p>
               <p className="text-xs text-cf-subtext">{track.artist || 'Unknown'}</p>
             </div>

             {/* Tierバッジ */}
             <span className="text-xs bg-cf-primary/20 text-cf-primary px-2 py-0.5 rounded">
               {track.tier}
             </span>

             {/* アクションボタン */}
             <div className="flex items-center gap-2">
               <motion.button
                 {...tapAnimation}
                 onClick={() => setShowEditDialog(true)}
                 className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-text"
                 aria-label="編集"
               >
                 <Edit2 size={16} />
               </motion.button>
               <motion.button
                 {...tapAnimation}
                 onClick={() => setShowDeleteConfirm(true)}
                 className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
                 aria-label="削除"
               >
                 <Trash2 size={16} />
               </motion.button>
             </div>
           </motion.div>

           {/* ダイアログ */}
           {showDeleteConfirm && (
             <DeleteConfirmDialog
               track={track}
               onClose={() => setShowDeleteConfirm(false)}
             />
           )}
           {showEditDialog && (
             <EditTrackDialog
               track={track}
               onClose={() => setShowEditDialog(false)}
             />
           )}
         </>
       )
     }
     ```

  **TodoItemパターンの再利用**:
  - `slideInVariants`でスライドインアニメーション
  - `group` + `group-hover:opacity-100`でホバー時ボタン表示
  - useStateでダイアログ表示制御

  **UI-SPEC準拠**:
  - 色プレビュー: `w-4 h-4 rounded-full`
  - Tierバッジ: `text-xs bg-cf-primary/20 text-cf-primary px-2 py-0.5 rounded`
  - ホバー時のアクションボタン: `opacity-0 group-hover:opacity-100`
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/TrackItem.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/TrackItem.tsxが存在する
    - TrackItemコンポーネントがexportされている
    - 色プレビューが表示される（w-4 h-4 rounded-full）
    - Tierバッジが表示される
    - 編集・削除ボタンがある（opacity-0 group-hover:opacity-100）
    - EditTrackDialog、DeleteConfirmDialogをインポートしている
    - テスト: `npm test -- src/components/bgm/TrackItem.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - トラック情報が正しく表示される
  - 編集・削除ボタンでダイアログが開く
  - ホバー時にアクションボタンが表示される
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AddTrackFormコンポーネントを実装</name>
  <files>src/components/bgm/AddTrackForm.tsx</files>
  <read_first>
    - src/components/dialogs/LoginDialog.tsx
    - src/app/routers/bgm.ts
    - src/core/store/ui.ts
  </read_first>
  <behavior>
    - Test 1: フォーム入力が正しく動作する（title, artist, color, tier）
    - Test 2: ファイル選択時のバリデーションが動作する（10MB制限、MP3のみ）
    - Test 3: 送信時にcreate mutationが呼ばれる
    - Test 4: 送信成功時にトースト通知が表示され、onBackが呼ばれる
    - Test 5: エラー時にエラートーストが表示される
    - Test 6: ローディング時にボタンが無効化される
  </behavior>
  <action>
  AddTrackForm.tsxファイルを作成し、以下の実装を行う：

  1. **ファイル作成とインポート**:
     ```typescript
     import { useState } from 'react'
     import { motion } from 'framer-motion'
     import { trpc } from '@/lib/trpc'
     import { useUiStore } from '@/core/store/ui'
     import { tapAnimation } from '@/lib/animation'
     import type { CreateBgmTrack } from '@/app/routers/_shared'

     interface AddTrackFormProps {
       onBack: () => void
     }
     ```

  2. **コンポーネント実装（stateとmutation）**:
     ```typescript
     export function AddTrackForm({ onBack }: AddTrackFormProps) {
       const addToast = useUiStore(state => state.addToast)
       const utils = trpc.useUtils()

       const [title, setTitle] = useState('')
       const [artist, setArtist] = useState('')
       const [color, setColor] = useState('#3b82f6')
       const [tier, setTier] = useState<'free' | 'premium'>('free')
       const [fileBase64, setFileBase64] = useState<string | null>(null)
       const [fileError, setFileError] = useState<string | null>(null)

       const createMutation = trpc.bgm.create.useMutation({
         onSuccess: () => {
           addToast('トラックを追加しました', 'success')
           utils.bgm.getAll.invalidate()
           onBack()
         },
         onError: (error) => {
           addToast(error.message, 'error')
         }
       })
     ```

  3. **ファイルハンドラー実装**:
     ```typescript
       const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
         const file = e.target.files?.[0]
         if (!file) return

         // サイズチェック（10MB）
         if (file.size > 10 * 1024 * 1024) {
           setFileError('ファイルサイズは10MB以下にしてください')
           setFileBase64(null)
           return
         }

         // タイプチェック（MP3のみ）
         if (file.type !== 'audio/mpeg') {
           setFileError('MP3ファイルを選択してください')
           setFileBase64(null)
           return
         }

         // Base64変換
         const reader = new FileReader()
         reader.onload = () => {
           const base64 = (reader.result as string).split(',')[1]
           setFileBase64(base64)
           setFileError(null)
         }
         reader.onerror = () => {
           setFileError('ファイルの読み込みに失敗しました')
         }
         reader.readAsDataURL(file)
       }
     ```

  4. **送信ハンドラー実装**:
     ```typescript
       const handleSubmit = async (e: React.FormEvent) => {
         e.preventDefault()
         if (!fileBase64) {
           setFileError('ファイルを選択してください')
           return
         }

         const data: CreateBgmTrack = {
           fileBase64,
           title,
           artist,
           color,
           tier,
         }

         await createMutation.mutateAsync(data)
       }
     ```

  5. **JSX実装（フォーム）**:
     ```typescript
       return (
         <form onSubmit={handleSubmit} className="flex flex-col gap-4">
           {/* Title */}
           <div className="flex flex-col gap-1.5">
             <label className="text-xs text-cf-subtext">曲名</label>
             <input
               type="text"
               required
               value={title}
               onChange={(e) => setTitle(e.target.value)}
               className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
               placeholder="_focus_time"
             />
           </div>

           {/* Artist */}
           <div className="flex flex-col gap-1.5">
             <label className="text-xs text-cf-subtext">アーティスト</label>
             <input
               type="text"
               value={artist}
               onChange={(e) => setArtist(e.target.value)}
               className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
               placeholder="Lofi Records"
             />
           </div>

           {/* Color */}
           <div className="flex flex-col gap-1.5">
             <label className="text-xs text-cf-subtext">色</label>
             <div className="flex items-center gap-2">
               <input
                 type="color"
                 value={color}
                 onChange={(e) => setColor(e.target.value)}
                 className="w-10 h-10 rounded cursor-pointer"
               />
               <span className="text-sm text-cf-text">{color}</span>
             </div>
           </div>

           {/* Tier */}
           <div className="flex flex-col gap-1.5">
             <label className="text-xs text-cf-subtext">Tier</label>
             <select
               value={tier}
               onChange={(e) => setTier(e.target.value as 'free' | 'premium')}
               className="rounded-lg border border-cf-border bg-cf-surface px-3 py-2 text-sm text-cf-text outline-none focus:border-cf-primary transition-colors"
             >
               <option value="free">Free</option>
               <option value="premium">Premium</option>
             </select>
           </div>

           {/* File Upload */}
           <div className="flex flex-col gap-1.5">
             <label className="text-xs text-cf-subtext">MP3ファイル（10MB以下）</label>
             <input
               type="file"
               accept="audio/mpeg"
               onChange={handleFileChange}
               className="text-sm text-cf-text file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:bg-cf-primary file:text-white hover:file:bg-cf-primary/90 cursor-pointer"
             />
             {fileError && (
               <p className="text-red-400 text-sm">{fileError}</p>
             )}
             {fileBase64 && !fileError && (
               <p className="text-cf-success text-sm">ファイル選択済み</p>
             )}
           </div>

           {/* Buttons */}
           <div className="flex gap-2 mt-2">
             <motion.button
               {...tapAnimation}
               type="button"
               onClick={onBack}
               disabled={createMutation.isPending}
               className="flex-1 bg-cf-surface hover:bg-cf-surface1 text-cf-text px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
             >
               戻る
             </motion.button>
             <motion.button
               {...tapAnimation}
               type="submit"
               disabled={createMutation.isPending || !fileBase64}
               className="flex-1 bg-cf-primary hover:bg-cf-primary/90 text-white px-4 py-2 rounded-lg transition-colors cursor-pointer disabled:opacity-50"
             >
               {createMutation.isPending ? '追加中...' : '追加'}
             </motion.button>
           </div>
         </form>
       )
     }
     ```

  **LoginDialogパターンの再利用**:
  - フォーム入力スタイル: `rounded-lg border border-cf-border bg-cf-surface px-3 py-2`
  - ローディング表示: ボタンテキスト変更 + disabled
  - エラー表示: `text-red-400 text-sm`

  **バリデーション**:
  - ファイルサイズ: `file.size > 10 * 1024 * 1024`
  - ファイルタイプ: `file.type !== 'audio/mpeg'`
  - Base64変換: `split(',')[1]`でData URLスキーム削除

  **確認事項**:
  - `trpc.bgm.create.useMutation`を使用
  - 成功時に`utils.bgm.getAll.invalidate()`でリスト更新
  - `useUiStore`の`addToast`で通知
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/AddTrackForm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/AddTrackForm.tsxが存在する
    - AddTrackFormコンポーネントがexportされている
    - `trpc.bgm.create.useMutation`を使用している
    - `handleFileChange`でファイルサイズチェック（10MB）を実装している
    - `handleFileChange`でファイルタイプチェック（audio/mpeg）を実装している
    - FileReaderでBase64変換を実装している（split(',')[1]でData URL削除）
    - 成功時に`utils.bgm.getAll.invalidate()`を呼んでいる
    - ローディング時にボタンがdisabledになる
    - テスト: `npm test -- src/components/bgm/AddTrackForm.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - フォーム入力とバリデーションが正しく動作する
  - ファイルアップロードが正しく動作する
  - 送信成功時にトースト通知が表示され、リストが更新される
  - エラー時にエラートーストが表示される
  </done>
</task>

</tasks>

<verification>
1. AdminModalでTrackListが表示される（mode='list'）
2. トラック一覧が正しく表示される
3. 追加ボタンクリックでAddTrackFormが表示される（mode='add'）
4. ファイル選択でバリデーションが動作する
5. 追加成功後、リストが更新される
6. すべてのテストがパスする
</verification>

<success_criteria>
- トラック一覧が表示される（UI-03）
- ファイルアップロードが機能する（UI-04）
- ローディング・エラー状態が表示される（UI-07）
- テストがすべてパスする
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui/06-02-SUMMARY.md`
</output>
