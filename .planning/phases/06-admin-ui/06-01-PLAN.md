---
phase: 06-admin-ui
plan: 01
type: execute
wave: 1
depends_on: ["06-00"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/bgm/AdminModal.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - 管理者ユーザーのみHeaderに管理ボタンが表示される
    - 管理ボタンクリックでAdminModalが開く
    - AdminModalが背景クリック、Escapeキー、閉じるボタンで閉じる
    - AdminModalにmode状態を持ち、'list' | 'add' | 'edit'を切り替えられる
  artifacts:
    - path: src/components/layout/Header.tsx
      provides: 管理ボタン表示ロジック
      min_lines: 110
      exports: ["Header"]
    - path: src/components/bgm/AdminModal.tsx
      provides: メイン管理モーダル
      min_lines: 80
      exports: ["AdminModal"]
  key_links:
    - from: src/components/layout/Header.tsx
      to: src/components/bgm/AdminModal.tsx
      via: 管理ボタンクリックでshowAdminModal stateをtrueに設定
      pattern: "setShowAdminModal\(true\)"
    - from: src/components/bgm/AdminModal.tsx
      to: src/hooks/useAuth.ts
      via: isAdminの取得
      pattern: "useAuth\(\)"
    - from: src/components/bgm/AdminModal.tsx
      to: src/lib/animation.ts
      via: アニメーション設定のインポート
      pattern: "tapAnimation|hoverAnimation"
---

<objective>
Headerに管理者用の管理ボタンを追加し、AdminModalコンポーネントを作成する

Purpose: 管理者がBGM管理画面にアクセスするためのエントリーポイントを実装
Output: 管理ボタン付きのHeader、基本的なAdminModal構造
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-ui/06-CONTEXT.md
@.planning/phases/06-admin-ui/06-RESEARCH.md
@.planning/phases/06-admin-ui/06-UI-SPEC.md
@src/components/layout/Header.tsx
@src/components/dialogs/LoginDialog.tsx
@src/hooks/useAuth.ts
@src/lib/animation.ts
@src/test/setup.ts
@src/components/layout/Header.test.tsx
@src/components/bgm/AdminModal.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Headerに管理ボタンを追加</name>
  <files>src/components/layout/Header.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx
    - src/hooks/useAuth.ts
  </read_first>
  <behavior>
    - Test 1: useAuthからisAdminを取得
    - Test 2: 管理者ユーザー時にSettingsアイコンの管理ボタンが表示される
    - Test 3: 非管理者ユーザー時に管理ボタンが表示されない
    - Test 4: 管理ボタンクリックでshowAdminModal stateがtrueになる
  </behavior>
  <action>
  Header.tsxに以下の変更を加える：

  1. **isAdminの取得**:
     ```typescript
     const { user, logout, isAdmin } = useAuth()
     ```
     既存の`useAuth()`呼び出しに`isAdmin`を追加

  2. **showAdminModal stateの追加**:
     ```typescript
     const [showAdminModal, setShowAdminModal] = useState(false)
     ```
     `showLoginDialog`の宣言の後に追加

  3. **管理ボタン追加（Focus TimeとUser Avatarの間）**:
     ```typescript
     {/* Admin Button（adminのみ） */}
     {isAdmin && (
       <motion.button
         whileHover={{ scale: 1.05 }}
         whileTap={{ scale: 0.95 }}
         onClick={() => setShowAdminModal(true)}
         className="size-10 rounded-full border-2 border-cf-primary/30 overflow-hidden flex items-center justify-center bg-cf-surface hover:border-cf-primary/60 transition-colors cursor-pointer"
         title="BGM管理"
       >
         <Settings className="text-cf-text" size={20} />
       </motion.button>
     )}
     ```
     配置: Focus TimeのdivとUser Avatar buttonの間

  4. **AdminModalのレンダリング追加**:
     ```typescript
     {isAdmin && (
       <AdminModal
         isOpen={showAdminModal}
         onClose={() => setShowAdminModal(false)}
       />
     )}
     ```
     LoginDialogのレンダリングの後に追加

  5. **インポート追加**:
     ```typescript
     import { Settings } from 'lucide-react'
     import { AdminModal } from '@/components/bgm/AdminModal'
     ```

  **確認事項**:
  - 管理ボタンは`isAdmin &&`で条件付きレンダリング
  - ボタンスタイルは既存のUser Avatarボタンと同じパターン
  - Iconsは`lucide-react`の`Settings`を使用
  </action>
  <verify>
    <automated>npm test -- src/components/layout/Header.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsxに`isAdmin`がuseAuthから取得されている
    - Header.tsxに`showAdminModal` stateが定義されている
    - Header.tsxにSettingsアイコンの管理ボタンが追加されている（条件: `isAdmin &&`）
    - 管理ボタンのclassNameに`size-10 rounded-full border-2 border-cf-primary/30`が含まれる
    - Header.tsxにAdminModalのレンダリングが追加されている
    - テスト: `npm test -- src/components/layout/Header.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - 管理者ユーザーのみ管理ボタンが表示される
  - 管理ボタンクリックでAdminModalが開く
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminModal基本構造を作成</name>
  <files>src/components/bgm/AdminModal.tsx</files>
  <read_first>
    - src/components/dialogs/LoginDialog.tsx
    - src/lib/animation.ts
  </read_first>
  <behavior>
    - Test 1: isOpen=true時にモーダルが表示される
    - Test 2: isOpen=false時にモーダルが表示されない
    - Test 3: 背景クリックでonCloseが呼ばれる
    - Test 4: EscapeキーでonCloseが呼ばれる
    - Test 5: 閉じるボタン（X）クリックでonCloseが呼ばれる
    - Test 6: mode='list'時にTrackListが表示される
  </behavior>
  <action>
  AdminModal.tsxファイルを作成し、以下の構造を実装：

  1. **ファイル作成とインポート**:
     ```typescript
     import { useState, useEffect } from 'react'
     import { createPortal } from 'react-dom'
     import { motion, AnimatePresence } from 'framer-motion'
     import { X } from 'lucide-react'
     import { tapAnimation } from '@/lib/animation'

     interface AdminModalProps {
       isOpen: boolean
       onClose: () => void
     }

     type Mode = 'list' | 'add' | 'edit'
     ```

  2. **コンポーネント実装**:
     ```typescript
     export function AdminModal({ isOpen, onClose }: AdminModalProps) {
       const [mode, setMode] = useState<Mode>('list')

       // ダイアログが閉じたらmodeをリセット
       useEffect(() => {
         if (!isOpen) {
           setMode('list')
         }
       }, [isOpen])

       // Escapeキーで閉じる
       useEffect(() => {
         const handleEscape = (e: KeyboardEvent) => {
           if (e.key === 'Escape' && isOpen) onClose()
         }
         document.addEventListener('keydown', handleEscape)
         return () => document.removeEventListener('keydown', handleEscape)
       }, [isOpen, onClose])

       return createPortal(
         <AnimatePresence>
           {isOpen && (
             <div
               className="fixed inset-0 z-widget-modal flex items-center justify-center bg-black/50 backdrop-blur-sm"
               onClick={onClose}
             >
               <motion.div
                 initial={{ opacity: 0, scale: 0.9, y: 20 }}
                 animate={{ opacity: 1, scale: 1, y: 0 }}
                 exit={{ opacity: 0, scale: 0.9, y: 20 }}
                 transition={{ duration: 0.2, ease: 'easeOut' }}
                 className="widget p-6 w-full max-w-2xl m-4"
                 onClick={(e) => e.stopPropagation()}
               >
                 {/* Header */}
                 <div className="flex items-center justify-between mb-6">
                   <h3 className="text-lg font-bold text-cf-text">BGM管理</h3>
                   <motion.button
                     {...tapAnimation}
                     onClick={onClose}
                     className="text-cf-subtext hover:text-cf-text transition-colors cursor-pointer"
                   >
                     <X size={20} />
                   </motion.button>
                 </div>

                 {/* Content - modeによる切り替え */}
                 {mode === 'list' && (
                   <div className="text-cf-subtext text-center py-8">
                     トラック一覧（次のタスクで実装）
                   </div>
                 )}
                 {mode === 'add' && (
                   <div className="text-cf-subtext text-center py-8">
                     追加フォーム（次のタスクで実装）
                   </div>
                 )}
                 {mode === 'edit' && (
                   <div className="text-cf-subtext text-center py-8">
                     編集フォーム（次のタスクで実装）
                   </div>
                 )}
               </motion.div>
             </div>
           )}
         </AnimatePresence>,
         document.body
       )
     }
     ```

  **LoginDialogパターンの再利用**:
  - `createPortal` + `AnimatePresence` + `motion.div`
  - Escapeキーイベントリスナー
  - 背景クリックで閉じる
  - widgetクラスとmax-w-2xl幅

  **確認事項**:
  - LoginDialogと同じアニメーション設定を使用
  - mode stateは'list' | 'add' | 'edit'の3つ
  - ダイアログ閉じ時にmodeがリセットされる
  </action>
  <verify>
    <automated>npm test -- src/components/bgm/AdminModal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/AdminModal.tsxが存在する
    - AdminModalコンポーネントがexportされている
    - `createPortal`を使用している
    - `AnimatePresence`と`motion.div`でアニメーション実装
    - Escapeキーイベントリスナーが実装されている（useEffectのクリーンアップ関数を含む）
    - mode stateが定義されている（'list' | 'add' | 'edit'）
    - isOpen=false時にmodeが'list'にリセットされる
    - テスト: `npm test -- src/components/bgm/AdminModal.test.ts` がパスする
  </acceptance_criteria>
  <done>
  - AdminModalが正しく表示・非表示される
  - 背景クリック、Escapeキー、閉じるボタンでモーダルが閉じる
  - mode状態によって表示内容が切り替わる
  </done>
</task>

</tasks>

<verification>
1. 管理者ユーザーでログイン時にHeaderに管理ボタンが表示される
2. 非管理者ユーザー時に管理ボタンが表示されない
3. 管理ボタンクリックでAdminModalが開く
4. AdminModalの背景クリック、Escapeキー、閉じるボタンでモーダルが閉じる
5. すべてのテストがパスする
</verification>

<success_criteria>
- 管理者のみ管理ボタンが表示される（UI-01）
- AdminModalが正しく動作する（UI-02）
- テストがすべてパスする
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui/06-01-SUMMARY.md`
</output>
