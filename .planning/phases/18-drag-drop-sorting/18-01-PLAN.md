---
phase: 18-drag-drop-sorting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [functions/lib/schema.ts, src/app/routers/todos.ts]
autonomous: true
requirements: [DND-02]
user_setup: []

must_haves:
  truths:
    - "ログインユーザーが並び替えたタスクの順序がDBに保存される"
    - "新規タスク作成時にorder値が自動採番される（最大order + 1）"
  artifacts:
    - path: "functions/lib/schema.ts"
      provides: "todosテーブルのorderカラム"
      contains: "order: integer"
    - path: "src/app/routers/todos.ts"
      provides: "reorder mutation"
      exports: ["reorder"]
  key_links:
    - from: "src/app/routers/todos.ts reorder"
      to: "functions/lib/schema.ts todos.order"
      via: "Drizzle ORM update"
      pattern: "todos.order"
---

<objective>
DBスキーマにorderカラムを追加し、tRPCルーターにreorder mutationを実装する。

Purpose: ログインユーザーのタスク並び替え順序を永続化するためのバックエンド基盤を構築する。
Output: orderカラム付きtodosスキーマ、reorder mutation付きtodosルーター
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-drag-drop-sorting/18-CONTEXT.md
@.planning/phases/18-drag-drop-sorting/18-RESEARCH.md

<interfaces>
<!-- 既存のスキーマ定義 -->
From functions/lib/schema.ts:
```typescript
export const todos = pgTable("todos", {
  id: uuid("id").primaryKey().defaultRandom(),
  userId: uuid("user_id").references(() => users.id, { onDelete: "cascade" }),
  title: text("title").notNull(),
  completed: boolean("completed").notNull().default(false),
  completedPomodoros: integer("completed_pomodoros").notNull().default(0),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

<!-- 既存のルーター定義 -->
From src/app/routers/todos.ts:
```typescript
export const todosRouter = router({
  getAll: protectedProcedure.query(...),
  create: protectedProcedure.input(newTodoSchema).mutation(...),
  update: protectedProcedure.input(updateTodoSchema).mutation(...),
  delete: protectedProcedure.input(z.object({ id: z.string().uuid() })).mutation(...),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: todosテーブルにorderカラムを追加</name>
  <files>functions/lib/schema.ts</files>
  <read_first>
    - functions/lib/schema.ts（現在のスキーマ定義を確認）
  </read_first>
  <behavior>
    - orderカラムがinteger型でNOT NULL DEFAULT 0で追加される
    - getAllクエリでorderBy orderが使用可能になる
  </behavior>
  <action>
functions/lib/schema.tsのtodosテーブル定義にorderカラムを追加する。

追加内容:
```typescript
order: integer("order").notNull().default(0),
```

配置位置: completedPomodorosの後、createdAtの前

スキーマ変更後、マイグレーションファイルを生成:
```bash
npm run db:generate
```
  </action>
  <verify>
    <automated>grep -q "order: integer" functions/lib/schema.ts && echo "order column exists" || echo "MISSING: order column"</automated>
  </verify>
  <done>
    - functions/lib/schema.tsにorder: integer("order").notNull().default(0)が存在する
    - npm run db:generateが成功し、マイグレーションファイルが生成される
  </done>
</task>

<task type="auto">
  <name>Task 2: tRPCルーターにreorder mutationを追加</name>
  <files>src/app/routers/todos.ts</files>
  <read_first>
    - src/app/routers/todos.ts（現在のルーター構造を確認）
    - functions/lib/schema.ts（orderカラム定義を確認）
    - src/app/routers/_shared.ts（スキーマ定義パターンを確認）
  </read_first>
  <behavior>
    - reorder mutationが{id: uuid, newOrder: integer}を受け取る
    - 移動範囲内のorder値のみが更新される（効率的）
    - 認証済みユーザーのみが自分のタスクを並び替え可能
  </behavior>
  <action>
src/app/routers/todos.tsにreorder mutationを追加する。

実装内容:
1. 入力スキーマ定義:
```typescript
const reorderSchema = z.object({
  id: z.string().uuid(),
  newOrder: z.number().int().min(0),
});
```

2. reorder mutation実装（移動範囲内更新戦略）:
```typescript
reorder: protectedProcedure
  .input(reorderSchema)
  .mutation(async ({ ctx, input }) => {
    const { db, user, schema } = ctx;
    const { id, newOrder } = input;

    // 対象タスク取得
    const [target] = await db
      .select()
      .from(schema.todos)
      .where(and(eq(schema.todos.id, id), eq(schema.todos.userId, user.id)))
      .limit(1);

    if (!target) {
      throw new TRPCError({ code: 'NOT_FOUND', message: 'Todo not found' });
    }

    const oldOrder = target.order;

    // 同じ位置なら何もしない
    if (oldOrder === newOrder) {
      return { success: true };
    }

    // 移動範囲を決定
    const [minOrder, maxOrder] = oldOrder < newOrder
      ? [oldOrder + 1, newOrder]
      : [newOrder, oldOrder - 1];

    // 範囲内のorderをシフト
    const shift = oldOrder < newOrder ? -1 : 1;
    await db
      .update(schema.todos)
      .set({ order: sql`${schema.todos.order} + ${shift}` })
      .where(
        and(
          eq(schema.todos.userId, user.id),
          gte(schema.todos.order, minOrder),
          lte(schema.todos.order, maxOrder)
        )
      );

    // ターゲットを新しい位置に
    await db
      .update(schema.todos)
      .set({ order: newOrder })
      .where(eq(schema.todos.id, id));

    return { success: true };
  }),
```

3. 必要なimportを追加:
```typescript
import { gte, lte, sql } from 'drizzle-orm'
```

4. create mutationで新規タスクに最大order+1を設定（getAllの結果から計算）

5. getAllクエリにorderBy追加:
```typescript
.orderBy(schema.todos.order)
```
  </action>
  <verify>
    <automated>grep -q "reorder:" src/app/routers/todos.ts && echo "reorder mutation exists" || echo "MISSING: reorder mutation"</automated>
  </verify>
  <done>
    - src/app/routers/todos.tsにreorder mutationが存在する
    - reorderは{id: uuid, newOrder: integer}を受け取り、移動範囲内のorderを更新する
    - getAllがorderBy(schema.todos.order)でソート済みの結果を返す
    - npm run buildが成功する
  </done>
</task>

</tasks>

<verification>
- TypeScriptコンパイル: `npm run build`が成功
- スキーマ整合性: orderカラムが正しく定義されている
- ルーター整合性: reorder mutationが正しく実装されている
</verification>

<success_criteria>
- todosテーブルにorderカラム（integer, NOT NULL, DEFAULT 0）が追加されている
- reorder mutationが{id, newOrder}を受け取り、移動範囲内のorder値を更新する
- getAllクエリがorder順でソートした結果を返す
</success_criteria>

<output>
After completion, create `.planning/phases/18-drag-drop-sorting/18-01-SUMMARY.md`
</output>
