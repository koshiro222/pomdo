---
phase: 10-grid-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/stats/StatsCard.tsx
  - src/components/tasks/CurrentTaskCard.tsx
  - src/components/todos/TodoList.tsx
autonomous: true
requirements:
  - GRID-01
  - GRID-03
must_haves:
  truths:
    - "ユーザーは全カードで統一感のあるデザイン（BentoCard）を確認できる"
    - "ユーザーは全領域で一貫したスペーシングを確認できる"
  artifacts:
    - path: "src/App.tsx"
      provides: "グリッド定義とカードclassName"
      contains: "bento-card"
    - path: "src/components/stats/StatsCard.tsx"
      provides: "統計表示カード"
      contains: "bento-card"
    - path: "src/components/tasks/CurrentTaskCard.tsx"
      provides: "現在のタスク表示カード"
      contains: "bento-card"
    - path: "src/components/todos/TodoList.tsx"
      provides: "タスクリストカード"
      contains: "bento-card"
  key_links:
    - from: "src/App.tsx"
      to: "src/index.css"
      via: ".bento-cardクラス"
      pattern: "className=\"bento-card"
---

<objective>
全カードのデザインを`.bento-card`クラスに統一し、paddingをカードサイズに応じて最適化する。

Purpose: CONTEXT.mdで決定された`.bento-card`クラスへの統一により、全カードで一貫したglassmorphismデザインとアニメーションを実現する。また、コンテンツ密度に応じたpaddingの使い分けにより、視覚的なバランスを改善する。

Output:
- App.tsxの全motion.divのclassNameを`bento-card`に置換
- StatsCard.tsxのclassNameを`bento-card`に置換
- CurrentTaskCard.tsxのclassNameを`bento-card`に置換
- TodoList.tsxのclassNameを`bento-card`に置換（rounded-2xl→3xl）
- paddingをカードサイズに応じて最適化（大きなカード: p-6、小さなカード: p-4）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-grid-unification/10-CONTEXT.md
@.planning/phases/10-grid-unification/10-RESEARCH.md
@src/index.css
</context>

<interfaces>
<!-- Key contracts from codebase -->

From src/index.css (lines 268-281):
```css
.bento-card {
  background: color-mix(in srgb, var(--df-bg-elevated), transparent 40%);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--df-border-subtle);
  border-radius: 1.5rem;
  overflow: hidden;
  transition: all 0.3s ease;
}

.bento-card:hover {
  border-color: rgba(255, 255, 255, 0.15);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}
```

From src/index.css (lines 153-158):
```css
.glass {
  background: color-mix(in srgb, var(--df-bg-elevated), transparent 40%);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--df-border-subtle);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: App.tsxのカードclassNameをbento-cardに置換</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lines 167-236)
  </read_first>
  <action>
    App.tsxの全motion.div（5箇所）のclassNameを置換：

    1. タイマーカード（line 170）:
       - Before: `className="glass rounded-3xl overflow-hidden sm:col-span-4 sm:row-span-2 lg:col-span-8 lg:row-span-2"`
       - After: `className="bento-card sm:col-span-4 sm:row-span-2 lg:col-span-8 lg:row-span-2"`

    2. CurrentTaskカード（line 192）:
       - Before: `className="glass rounded-3xl overflow-hidden sm:col-span-2 lg:col-span-2 lg:row-span-1"`
       - After: `className="bento-card sm:col-span-2 lg:col-span-2 lg:row-span-1"`

    3. BGMカード（line 204）:
       - Before: `className="glass rounded-3xl overflow-hidden sm:col-span-2 lg:col-span-2 lg:row-span-1"`
       - After: `className="bento-card sm:col-span-2 lg:col-span-2 lg:row-span-1"`

    4. Statsカード（line 216）:
       - Before: `className="glass rounded-3xl overflow-hidden sm:col-span-2 lg:col-span-4 lg:row-span-1"`
       - After: `className="bento-card sm:col-span-2 lg:col-span-4 lg:row-span-1"`

    5. Todoカード（line 228）:
       - Before: `className="glass rounded-3xl overflow-hidden sm:col-span-6 lg:col-span-12 lg:row-span-1"`
       - After: `className="bento-card sm:col-span-6 lg:col-span-12 lg:row-span-1"`

    置換ルール:
    - `glass rounded-3xl overflow-hidden` → `bento-card`（.bento-cardクラスがborder-radiusとoverflowを含むため）
    - motion.divの構造とlayout="position"は維持
  </action>
  <verify>
    <automated>grep -n "className=\"bento-card" src/App.tsx | wc -l | xargs -I {} test {} -eq 5</automated>
  </verify>
  <done>
    - 5箇所のmotion.divで`bento-card`クラスが使用されている
    - ビルドが成功する
  </done>
</task>

<task type="auto">
  <name>Task 2: StatsCard.tsxのclassNameをbento-cardに置換</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx (lines 60, 128)
  </read_first>
  <action>
    StatsCard.tsxの2箇所のclassNameを置換：

    1. 空状態（line 60）:
       - Before: `className="glass rounded-3xl p-6 h-full flex flex-col"`
       - After: `className="bento-card p-6 h-full flex flex-col"`

    2. 通常状態（line 128）:
       - Before: `className="glass rounded-3xl p-6 h-full flex flex-col overflow-y-auto min-h-0 relative"`
       - After: `className="bento-card p-6 h-full flex flex-col overflow-y-auto min-h-0 relative"`

    置換ルール:
    - `glass rounded-3xl` → `bento-card`
    - paddingは`p-6`を維持（大きなカード）
  </action>
  <verify>
    <automated>grep -n "className=\"bento-card" src/components/stats/StatsCard.tsx | wc -l | xargs -I {} test {} -eq 2</automated>
  </verify>
  <done>
    - 2箇所（空状態と通常状態）で`bento-card`クラスが使用されている
    - paddingが`p-6`に維持されている
  </done>
</task>

<task type="auto">
  <name>Task 3: CurrentTaskCard.tsxのclassNameをbento-cardに置換</name>
  <files>src/components/tasks/CurrentTaskCard.tsx</files>
  <read_first>
    - src/components/tasks/CurrentTaskCard.tsx (line 42)
  </read_first>
  <action>
    CurrentTaskCard.tsxのclassNameを置換：

    ルートdiv（line 42）:
    - Before: `className="glass rounded-3xl p-6 h-full flex flex-col relative overflow-hidden"`
    - After: `className="bento-card p-6 h-full flex flex-col relative overflow-hidden"`

    置換ルール:
    - `glass rounded-3xl` → `bento-card`
    - paddingは`p-6`を維持（大きなカード）
  </action>
  <verify>
    <automated>grep -n "className=\"bento-card" src/components/tasks/CurrentTaskCard.tsx | wc -l | xargs -I {} test {} -eq 1</automated>
  </verify>
  <done>
    - `bento-card`クラスが使用されている
    - paddingが`p-6`に維持されている
  </done>
</task>

<task type="auto">
  <name>Task 4: TodoList.tsxのclassNameをbento-cardに置換</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx (lines 48, 55)
  </read_first>
  <action>
    TodoList.tsxの2箇所のclassNameを置換：

    1. ローディング状態（line 48）:
       - Before: `className="glass rounded-2xl flex flex-col min-h-64 sm:h-full overflow-hidden p-6"`
       - After: `className="bento-card flex flex-col min-h-64 sm:h-full overflow-hidden p-4"`

    2. 通常状態（line 55）:
       - Before: `className="glass rounded-2xl flex flex-col min-h-64 sm:h-full overflow-hidden"`
       - After: `className="bento-card flex flex-col min-h-64 sm:h-full overflow-hidden"`

    置換ルール:
    - `glass rounded-2xl` → `bento-card`（.bento-cardはrounded-3xlを含む）
    - ローディング状態のpaddingは`p-6`から`p-4`に変更（小さなカードへの統一）
    - ルート要素にはpaddingなし、子要素で制御（line 57の`p-6`は維持）
  </action>
  <verify>
    <automated>grep -n "className=\"bento-card" src/components/todos/TodoList.tsx | wc -l | xargs -I {} test {} -eq 2</automated>
  </verify>
  <done>
    - 2箇所（ローディング状態と通常状態）で`bento-card`クラスが使用されている
    - ルート要素のpaddingが削除されている（line 55）
    - ローディング状態のpaddingが`p-4`に変更されている（line 48）
  </done>
</task>

</tasks>

<verification>
全タスク完了後の検証:
1. `npm run build`が成功すること
2. `npm run dev`で全カードが統一されたスタイルで表示されること（目視確認）
3. 全カードでhover時のアニメーション（border-colorとbox-shadow）が機能すること
4. paddingがカードサイズに応じて適切に設定されていること（大きなカード: p-6、小さなカード: p-4）
</verification>

<success_criteria>
- [ ] App.tsxの全motion.div（5箇所）で`bento-card`クラスが使用されている
- [ ] StatsCard.tsxの全カード（2箇所）で`bento-card`クラスが使用されている
- [ ] CurrentTaskCard.tsxで`bento-card`クラスが使用されている
- [ ] TodoList.tsxの全カード（2箇所）で`bento-card`クラスが使用されている
- [ ] `glass`クラスの使用が削除されている
- [ ] ビルドが成功する
- [ ] 全カードで統一されたデザインとアニメーションが表示される
</success_criteria>

<output>
After completion, create `.planning/phases/10-grid-unification/10-01-SUMMARY.md`
</output>
