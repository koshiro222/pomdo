---
phase: 08-responsive-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/lib/animation.ts
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04
must_haves:
  truths:
    - "ユーザーはsmブレイクポイントで要素が重ならず、すべてのカードが1行に収まる"
    - "ユーザーはレイアウト変更時のアニメーションがスムーズで競合しない"
    - "ユーザーはsmブレイクポイントでも縦スクロールが機能する"
  artifacts:
    - path: "src/App.tsx"
      provides: "Bento Gridレイアウト定義"
      contains: "sm:col-span-2.*Stats"
    - path: "src/lib/animation.ts"
      provides: "アニメーション設定"
      contains: "duration: 0.2"
  key_links:
    - from: "src/App.tsx main element"
      to: "グリッドコンテナ"
      via: "overflow制御"
      pattern: "min-h-0"
---

<objective>
Bento Gridのレスポンシブ不整合を解消し、全画面サイズで正しく表示されるようにする。

Purpose: smブレイクポイントで要素が重なる問題（col-span合計16列 vs グリッド6列）を解決し、Framer Motionのレイアウトシフト競合を排除する。
Output: 修正されたApp.tsx（グリッド定義、layout prop、overflow）、animation.ts（duration短縮）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-responsive-fix/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/App.tsx:
```typescript
// motion.div props pattern (lines 169-188, 191-200, 203-212, 215-224, 227-236)
<motion.div
  className="glass rounded-3xl overflow-hidden sm:col-span-* lg:col-span-*"
  variants={fadeInUpVariants}
  initial="hidden"
  animate="visible"
  custom={N}
  layout  // <-- これを layout="position" に変更
>
```

From src/lib/animation.ts:
```typescript
// fadeInUpVariants (line 6-20)
export const fadeInUpVariants: Variants = {
  hidden: { opacity: 0, y: 20 },
  visible: (i: number = 0) => ({
    opacity: 1,
    y: 0,
    transition: {
      delay: i * 0.1,
      duration: 0.5,  // <-- これを 0.2 に変更
      ease: [0, 0, 0.2, 1],
    },
  }),
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsxグリッド定義・layout prop・overflowを修正</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx（現在のグリッド定義、layout prop、main要素のoverflowを確認）
  </read_first>
  <action>
    src/App.tsxに以下の変更を適用する:

    **1. main要素のoverflow修正（line 165付近）**
    変更前: `className="flex-1 p-4 overflow-y-auto sm:overflow-hidden"`
    変更後: `className="flex-1 p-4 min-h-0"`

    **2. Statsカードのcol-span修正（line 215-216付近）**
    変更前: `sm:col-span-2 sm:row-span-1 lg:col-span-4`
    変更後: `sm:col-span-2 lg:col-span-4`
    ※ sm:row-span-1は冗長なので削除（デフォルトが1）

    **3. 全motion.divのlayout prop修正（5箇所: lines 175, 197, 209, 221, 233付近）**
    変更前: `layout`
    変更後: `layout="position"`

    これにより:
    - smブレイクポイントのcol-span合計: Timer(4) + CurrentTask(2) = 6（1行目）, BGM(2) + Stats(2) + Todo(6) = 10（折り返して2行目以降）→ 正しく配置
    - layout="position"でサイズ変更アニメーションを無効化し、レイアウトシフト競合を解消
    - min-h-0でFlexbox内のoverflowを有効化
  </action>
  <verify>
    <automated>grep -n 'layout="position"' src/App.tsx | wc -l | xargs -I {} sh -c 'test {} -eq 5 && echo "PASS: 5 layout=\"position\" found" || echo "FAIL: expected 5, got {}"'</automated>
    <automated>grep -n 'min-h-0' src/App.tsx | head -1 | grep -q 'min-h-0' && echo "PASS: min-h-0 found in main" || echo "FAIL: min-h-0 not found"</automated>
    <automated>grep -E 'sm:col-span-2.*lg:col-span-4' src/App.tsx | grep -v 'sm:row-span' | grep -q Stats && echo "PASS: Stats col-span correct" || echo "FAIL: Stats col-span incorrect"</automated>
  </verify>
  <done>
    - main要素がmin-h-0のみを持つ（overflow-y-auto sm:overflow-hidden削除）
    - Statsカードがsm:col-span-2 lg:col-span-4（sm:row-span-1削除）
    - 全5個のmotion.divがlayout="position"を持つ
  </done>
  <acceptance_criteria>
    - `grep -c 'layout="position"' src/App.tsx` が 5 を返す
    - `grep 'className="flex-1 p-4' src/App.tsx` が `min-h-0` を含み、`sm:overflow-hidden` を含まない
    - `grep 'sm:col-span-2 lg:col-span-4' src/App.tsx` が Statsカードのmotion.divに存在する
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: animation.tsのdurationを短縮</name>
  <files>src/lib/animation.ts</files>
  <read_first>
    - src/lib/animation.ts（fadeInUpVariantsのdurationを確認）
  </read_first>
  <action>
    src/lib/animation.tsのfadeInUpVariants（line 16付近）のdurationを変更する:

    変更前: `duration: 0.5,`
    変更後: `duration: 0.2,`

    これにより初期表示アニメーションが0.5秒→0.2秒に短縮され、レイアウトシフトの視覚的影響が軽減される。
  </action>
  <verify>
    <automated>grep -n 'duration: 0.2' src/lib/animation.ts | head -1 | grep -q 'duration: 0.2' && echo "PASS: duration 0.2 found" || echo "FAIL: duration not 0.2"</automated>
  </verify>
  <done>
    - fadeInUpVariantsのdurationが0.2に変更されている
  </done>
  <acceptance_criteria>
    - `grep 'duration: 0.2' src/lib/animation.ts` が成功する
    - `grep 'duration: 0.5' src/lib/animation.ts` が失敗する（0.5が存在しない）
  </acceptance_criteria>
</task>

</tasks>

<verification>
## 検証手順

1. **型チェック**: `npm run build` が成功すること
2. **グリッド確認**: App.tsxでsm:col-span合計が6の倍数で配置されること
3. **layout prop確認**: 5箇所のmotion.divがすべて`layout="position"`を持つこと
4. **duration確認**: fadeInUpVariantsのdurationが0.2であること
</verification>

<success_criteria>
- `npm run build` がエラーなく完了する
- App.tsxに`layout="position"`が5箇所存在する
- App.tsxのmain要素が`min-h-0`のみを持つ
- animation.tsのdurationが0.2である
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-fix/08-01-SUMMARY.md`
</output>
