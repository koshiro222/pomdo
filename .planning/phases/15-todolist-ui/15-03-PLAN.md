---
phase: 15-todolist-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified: []
autonomous: false
requirements:
  - TODO-01
  - TODO-02
  - TODO-03
  - TODO-04

must_haves:
  truths:
    - "ユーザーが視覚的にハイライトセクションの表示を確認できる"
    - "ユーザーがComplete/Nextボタンの動作を確認できる"
    - "ユーザーがTodoItemの選択中スタイルを確認できる"
    - "ユーザーがアニメーションの滑らかさを確認できる"
  artifacts: []
  key_links: []
---

<objective>
Phase 15の実装が視覚的・機能的に正しく動作することをユーザーが確認する。

Purpose: 自動テストでは検証できない視覚的要素（アニメーション、モバイルレイアウト等）をユーザーが確認する。
Output: ユーザー承認
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-todolist-ui/15-CONTEXT.md
@.planning/phases/15-todolist-ui/15-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ビジュアル・機能確認</name>
  <files>N/A（チェックポイントタスク）</files>
  <read_first>
    - src/components/todos/TodoList.tsx（実装後）
    - src/components/todos/TodoItem.tsx（実装後）
  </read_first>
  <action>
    これはチェックポイントタスクです。ユーザーによる視覚的・機能的確認を待ちます。

    ## 検証手順

    ### 1. 開発サーバー起動
    ```bash
    npm run dev
    ```
    http://localhost:5173 にアクセス

    ### 2. デスクトップ表示確認（ブラウザ幅1024px以上）
    1. タスクを追加（例: 「テストタスクA」「テストタスクB」）
    2. タスクをクリックして選択
    3. 以下を確認：
       - [ ] TodoListカードのヘッダー領域にハイライトセクションが表示される
       - [ ] 「Current Task」ラベルが表示される
       - [ ] タスク名が表示される
       - [ ] 完了数「X done」が表示される
       - [ ] 「✓ Complete」ボタンが表示される
       - [ ] 「→ Next」ボタンが表示される（他に未完了タスクがある場合）
       - [ ] 選択中のTodoItemに左ボーダー（緑色）が表示される

    ### 3. Completeボタン確認
    1. 「✓ Complete」ボタンをクリック
    2. 以下を確認：
       - [ ] タスクが完了状態になる（打ち消し線）
       - [ ] ハイライトセクションがフェードアウトして消える
       - [ ] 左ボーダーが消える

    ### 4. Nextボタン確認
    1. 複数の未完了タスクを作成
    2. 1つ目をクリックして選択
    3. 「→ Next」ボタンをクリック
    4. 以下を確認：
       - [ ] 次のタスクが選択される
       - [ ] ハイライトセクションの内容が切り替わる
       - [ ] アニメーションがスムーズ

    ### 5. モバイル表示確認（ブラウザ幅375px）
    1. 開発者ツールでモバイルビューに切り替え
    2. 上記2-4と同じ確認を行う
    3. 追加確認：
       - [ ] ハイライトセクションが適切に表示される
       - [ ] ボタンがタップ可能なサイズ

    ### 6. アニメーション確認
    - [ ] タスク選択時のスライドインアニメーションがスムーズ
    - [ ] タスク完了時のフェードアウトアニメーションがスムーズ
    - [ ] レイアウトシフトが発生しない（フィルタータブがガタつかない）

    ### 7. CurrentTaskCard削除確認
    - [ ] 画面にCurrentTaskCard（独立したカード）が存在しない
  </action>
  <verify>
    <automated>N/A（チェックポイントタスク）</automated>
  </verify>
  <done>
    - ユーザーがビジュアル確認を承認
    - 全チェックリスト項目がクリア
  </done>
  <how-to-verify>
    ## 検証手順

    ### 1. 開発サーバー起動
    ```bash
    npm run dev
    ```
    http://localhost:5173 にアクセス

    ### 2. デスクトップ表示確認（ブラウザ幅1024px以上）
    1. タスクを追加（例: 「テストタスクA」「テストタスクB」）
    2. タスクをクリックして選択
    3. 以下を確認：
       - [ ] TodoListカードのヘッダー領域にハイライトセクションが表示される
       - [ ] 「Current Task」ラベルが表示される
       - [ ] タスク名が表示される
       - [ ] 完了数「X done」が表示される
       - [ ] 「✓ Complete」ボタンが表示される
       - [ ] 「→ Next」ボタンが表示される（他に未完了タスクがある場合）
       - [ ] 選択中のTodoItemに左ボーダー（緑色）が表示される

    ### 3. Completeボタン確認
    1. 「✓ Complete」ボタンをクリック
    2. 以下を確認：
       - [ ] タスクが完了状態になる（打ち消し線）
       - [ ] ハイライトセクションがフェードアウトして消える
       - [ ] 左ボーダーが消える

    ### 4. Nextボタン確認
    1. 複数の未完了タスクを作成
    2. 1つ目をクリックして選択
    3. 「→ Next」ボタンをクリック
    4. 以下を確認：
       - [ ] 次のタスクが選択される
       - [ ] ハイライトセクションの内容が切り替わる
       - [ ] アニメーションがスムーズ

    ### 5. モバイル表示確認（ブラウザ幅375px）
    1. 開発者ツールでモバイルビューに切り替え
    2. 上記2-4と同じ確認を行う
    3. 追加確認：
       - [ ] ハイライトセクションが適切に表示される
       - [ ] ボタンがタップ可能なサイズ

    ### 6. アニメーション確認
    - [ ] タスク選択時のスライドインアニメーションがスムーズ
    - [ ] タスク完了時のフェードアウトアニメーションがスムーズ
    - [ ] レイアウトシフトが発生しない（フィルタータブがガタつかない）

    ### 7. CurrentTaskCard削除確認
    - [ ] 画面にCurrentTaskCard（独立したカード）が存在しない
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
## 自動検証
- 前のプランでE2Eテストがパスしていること

## 手動検証
- 上記チェックリストの全項目がクリアされていること
</verification>

<success_criteria>
- [ ] ユーザーがビジュアル確認を承認
- [ ] 全チェックリスト項目がクリア
</success_criteria>

<output>
After completion, create `.planning/phases/15-todolist-ui/15-03-SUMMARY.md`
</output>
