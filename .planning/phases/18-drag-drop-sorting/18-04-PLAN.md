---
phase: 18-drag-drop-sorting
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified: [drizzle/schema/xxx_add_order_to_todos.sql]
autonomous: false
requirements: [DND-01, DND-02]
user_setup: []

must_haves:
  truths:
    - "既存のDBデータにorder値が設定されている"
    - "ドラッグ&ドロップ並び替えが本番環境で動作する"
    - "ページリロード後も並び替え順序が維持される"
  artifacts:
    - path: "drizzle/schema/*.sql"
      provides: "orderカラム追加マイグレーション"
      contains: "ALTER TABLE todos ADD COLUMN \"order\""
  key_links:
    - from: "マイグレーションSQL"
      to: "Neon PostgreSQL"
      via: "npm run db:migrate"
      pattern: "ALTER TABLE"
---

<objective>
DBマイグレーションを実行し、ドラッグ&ドロップ並び替え機能の動作確認を行う。

Purpose: 本番環境でD&D機能が正しく動作することを確認する。
Output: マイグレーション適用済みDB、動作確認済みD&D機能
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-drag-drop-sorting/18-CONTEXT.md
@.planning/phases/18-drag-drop-sorting/18-RESEARCH.md
@.planning/phases/18-drag-drop-sorting/18-01-SUMMARY.md
@.planning/phases/18-drag-drop-sorting/18-02-SUMMARY.md
@.planning/phases/18-drag-drop-sorting/18-03-SUMMARY.md

<interfaces>
<!-- マイグレーション確認コマンド -->
```bash
# 生成されたマイグレーションファイルを確認
ls -la drizzle/schema/

# マイグレーション適用
npm run db:migrate

# 既存データのorder初期化（必要に応じて手動実行）
# Neonコンソールまたはpsqlで実行
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: マイグレーション適用と既存データ移行</name>
  <files>drizzle/schema/*.sql</files>
  <read_first>
    - drizzle/schema/（生成されたマイグレーションファイルを確認）
    - functions/lib/schema.ts（スキーマ定義を確認）
  </read_first>
  <behavior>
    - orderカラム追加マイグレーションが適用される
    - 既存データのorderがcreatedAt順で初期化される
  </behavior>
  <action>
1. 生成されたマイグレーションファイルを確認:
```bash
ls -la drizzle/schema/
cat drizzle/schema/*_add_order_to_todos.sql  # ファイル名は生成時による
```

2. マイグレーション適用:
```bash
npm run db:migrate
```

3. 既存データのorder初期化（NeonコンソールまたはSQL実行）:
```sql
-- createdAt順でorderを割り当て（0, 1, 2...）
WITH ordered_todos AS (
  SELECT id, ROW_NUMBER() OVER (ORDER BY created_at) - 1 as row_num
  FROM todos
)
UPDATE todos
SET "order" = ordered_todos.row_num
FROM ordered_todos
WHERE todos.id = ordered_todos.id;
```

注意: Neonコンソール（https://console.neon.tech）でSQL Editorを使用するか、ローカルからpsqlで接続して実行。
  </action>
  <verify>
    <automated>ls drizzle/schema/*order*.sql 2>/dev/null && echo "migration file exists" || echo "Check migration status manually"</automated>
  </verify>
  <done>
    - マイグレーションが適用され、todosテーブルにorderカラムが存在する
    - 既存データにorder値が設定されている
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ドラッグ&ドロップ並び替え機能（DB永続化 + localStorage永続化 + UI統合）</what-built>
  <how-to-verify>
**ゲストモード確認:**
1. `npm run dev`でローカルサーバーを起動
2. 未ログイン状態でTodoリストを開く
3. タスクを2つ以上追加
4. ドラッグハンドル（左側の⋮⋮）を掴んでタスクを上下にドラッグ
5. ドロップ後、順序が変わることを確認
6. ページをリロードし、順序が維持されていることを確認

**ログインモード確認:**
1. ログイン状態でTodoリストを開く
2. タスクを2つ以上追加
3. ドラッグハンドルでタスクを並び替え
4. ページをリロードし、順序が維持されていることを確認

**フィルタリング中のD&D確認:**
1. タスクを追加（完了済みと未完了を混在）
2. 「Active」タブに切り替え
3. 未完了タスクの並び替えができることを確認
4. 「Done」タブに切り替え
5. 完了済みタスクの並び替えができることを確認
  </how-to-verify>
  <resume-signal>Type "approved" または問題点を記載してください</resume-signal>
</task>

</tasks>

<verification>
- マイグレーション: orderカラムがDBに存在
- ゲストモード: ドラッグ&ドロップ + localStorage永続化が動作
- ログインモード: ドラッグ&ドロップ + DB永続化が動作
- フィルタリング: 各タブでD&Dが有効
</verification>

<success_criteria>
- マイグレーションが適用され、todosテーブルにorderカラムが存在する
- ゲストモードでタスクの並び替えができ、リロード後も維持される
- ログインモードでタスクの並び替えができ、リロード後も維持される
- フィルタリング状態に関わらずD&Dが有効
</success_criteria>

<output>
After completion, create `.planning/phases/18-drag-drop-sorting/18-04-SUMMARY.md`
</output>
