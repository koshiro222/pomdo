---
phase: 03-bgm-api-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bgm-api.test.ts
  - tests/bgm-router.test.ts
  - src/app/routers/bgm.ts
  - src/app/routers/_shared.ts
  - src/app/routers/root.ts
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []
must_haves:
  truths:
    - "ユーザーが利用可能な全BGMトラックを閲覧できる"
    - "ブラウザでBGMを再生できる（audio要素が音源を読み込める）"
    - "tierフィルタでトラックを絞り込める"
    - "ゲストユーザーも認証なしでBGM取得可能"
    - "既存プレイヤーと互換性のあるデータ形式"
  artifacts:
    - path: "tests/bgm-api.test.ts"
      provides: "BGM API統合テストスタブ"
      min_lines: 20
    - path: "tests/bgm-router.test.ts"
      provides: "tRPCルーターユニットテストスタブ"
      min_lines: 20
    - path: "src/app/routers/bgm.ts"
      provides: "bgmRouter with getAll query"
      exports: ["bgmRouter"]
      min_lines: 30
    - path: "src/app/routers/_shared.ts"
      provides: "BGM track Zod schemas"
      contains: "bgmTrackSchema, bgmTrackInputSchema"
    - path: "src/app/routers/root.ts"
      provides: "appRouter integration"
      exports: ["appRouter", "AppRouter"]
  key_links:
    - from: "src/app/routers/bgm.ts"
      to: "functions/lib/schema.ts"
      via: "ctx.schema.bgmTracks import"
      pattern: "ctx\\.schema\\.bgmTracks"
    - from: "src/app/routers/bgm.ts"
      to: "/api/bgm"
      via: "src field generation"
      pattern: "src: `/api/bgm/\\$\\{track\\.filename\\}`"
    - from: "src/app/routers/root.ts"
      to: "src/app/routers/bgm.ts"
      via: "import and router integration"
      pattern: "bgm: bgmRouter"
---

# BGM Read API実装

<objective>
tRPCルーター `bgm` を作成し、`getAll` クエリで全BGMトラックを取得するRead APIを実装する。

Purpose: 認証不要のpublicProcedureで全ユーザー（ゲスト含む）がBGMを取得可能にし、既存プレイヤーのDB移行準備を整える

Output:
- テストスタブファイル（Wave 0）
- bgmRouter（src/app/routers/bgm.ts） with getAll query
- BGM track Zod schemas（src/app/routers/_shared.ts）
- appRouter統合（src/app/routers/root.ts）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bgm-api-read/03-CONTEXT.md
@.planning/phases/03-bgm-api-read/03-RESEARCH.md
@.planning/phases/01-database/01-01-SUMMARY.md
@.planning/phases/01-database/01-02-SUMMARY.md

# Interfaces from existing codebase

## From src/app/routers/todos.ts
```typescript
import { router, protectedProcedure } from './context'
import { eq, and } from 'drizzle-orm'

export const todosRouter = router({
  getAll: protectedProcedure.query(async ({ ctx }) => {
    const { db, user } = ctx
    const todos = await db
      .select()
      .from(ctx.schema.todos)
      .where(eq(ctx.schema.todos.userId, user.id))
    return todos
  }),
})
```

## From src/app/routers/context.ts
```typescript
export const router = t.router
export const publicProcedure = t.procedure
export const protectedProcedure = t.procedure.use(({ ctx, next }) => { ... })
export const adminProcedure = t.procedure.use(({ ctx, next }) => { ... })
```

## From src/app/routers/_shared.ts
```typescript
import { z } from 'zod'

export const todoSchema = z.object({
  id: z.string().uuid(),
  userId: z.string().uuid(),
  title: z.string(),
  completed: z.boolean(),
  completedPomodoros: z.number().default(0),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
})
```

## From functions/lib/schema.ts
```typescript
export const bgmTracks = pgTable("bgm_tracks", {
  id: uuid("id").primaryKey().defaultRandom(),
  title: text("title", { length: 32 }).notNull(),
  artist: text("artist", { length: 32 }),
  color: text("color"),
  filename: text("filename", { length: 32 }).notNull().unique(),
  tier: text("tier", { enum: ["free", "premium"] }).notNull().default("free"),
  createdAt: timestamp("created_at").notNull().defaultNow(),
  updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

## From src/hooks/useBgm.ts (existing Track type)
```typescript
export type Track = {
  id: string
  title: string
  src: string
  artist?: string
  color?: string
}
```
</context>

<tasks>

<task type="auto" wave="0">
  <name>Task 0: テストスタブファイルの作成</name>
  <files>tests/bgm-api.test.ts, tests/bgm-router.test.ts</files>
  <action>
    # Wave 0: テストファイルのスタブを作成

    以下の2つのテストファイルを作成する：

    ## 1. tests/bgm-api.test.ts（統合テストスタブ）

    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest'
    import { bgmRouter } from '../src/app/routers/bgm'
    import { createContext } from '../src/app/routers/context'

    describe('BGM API - Integration', () => {
      it('getAll returns all tracks with src field', async () => {
        // TODO: 実装後に有効化
        // const ctx = await createContext({})
        // const caller = bgmRouter.createCaller(ctx)
        // const tracks = await caller.getAll()
        // expect(tracks).toBeInstanceOf(Array)
        // expect(tracks[0]).toHaveProperty('src')
        expect(true).toBe(true) // placeholder
      })

      it('getAll filters by tier when specified', async () => {
        // TODO: 実装後に有効化
        // const ctx = await createContext({})
        // const caller = bgmRouter.createCaller(ctx)
        // const freeTracks = await caller.getAll({ tier: 'free' })
        // expect(freeTracks.every(t => t.tier === 'free')).toBe(true)
        expect(true).toBe(true) // placeholder
      })
    })
    ```

    ## 2. tests/bgm-router.test.ts（ユニットテストスタブ）

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { bgmTrackSchema, bgmGetAllInputSchema } from '../src/app/routers/_shared'

    describe('BGM Router - Unit', () => {
      describe('bgmTrackSchema', () => {
        it('validates track structure', () => {
          const validTrack = {
            id: '123e4567-e89b-12d3-a456-426614174000',
            title: 'Test Track',
            src: '/api/bgm/test.mp3',
            tier: 'free',
            createdAt: '2026-03-20T00:00:00Z',
            updatedAt: '2026-03-20T00:00:00Z',
          }
          expect(() => bgmTrackSchema.parse(validTrack)).not.toThrow()
        })

        it('requires id, title, src, tier', () => {
          const invalidTrack = { title: 'Test' }
          expect(() => bgmTrackSchema.parse(invalidTrack)).toThrow()
        })
      })

      describe('bgmGetAllInputSchema', () => {
        it('accepts tier filter', () => {
          const input = { tier: 'free' }
          expect(() => bgmGetAllInputSchema.parse(input)).not.toThrow()
        })

        it('accepts undefined (no filter)', () => {
          expect(() => bgmGetAllInputSchema.parse(undefined)).not.toThrow()
        })
      })
    })
    ```

    **重要：**
    - これらはスタブファイル。実装後に本テストを書く
    - `expect(true).toBe(true)` はプレースホルダー
    - Wave 1のタスク実装後、TODOを外して本テストにする
  </action>
  <verify>
    <automated>ls tests/bgm-api.test.ts tests/bgm-router.test.ts</automated>
  </verify>
  <done>
    - tests/bgm-api.test.tsが存在する
    - tests/bgm-router.test.tsが存在する
    - 両ファイルにdescribe, itブロックが含まれている
    - ファイルが20行以上存在する
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: BGMルーターとgetAllクエリの作成</name>
  <files>src/app/routers/bgm.ts, tests/bgm-router.test.ts</files>
  <read_first>
    - src/app/routers/todos.ts — 既存ルーターパターン（getAll query構造）
    - src/app/routers/context.ts — publicProcedure定義
    - functions/lib/schema.ts — bgmTracksテーブル定義
    - src/hooks/useBgm.ts — 既存Track型とsrcフィールド要件
  </read_first>
  <behavior>
    - Test 1: getAllクエリが実行可能である
    - Test 2: getAllが配列を返す
    - Test 3: 返却トラックにsrcフィールドが含まれる（/api/bgm/filename形式）
    - Test 4: tierフィルタが動作する（free/premium指定時）
    - Test 5: tier未指定時は全トラック返却
  </behavior>
  <action>
    # 新規ファイル src/app/routers/bgm.ts を作成

    以下の実装を行う：

    1. **インポート**
    ```typescript
    import { z } from 'zod'
    import { eq } from 'drizzle-orm'
    import { router, publicProcedure } from './context'
    import { bgmGetAllInputSchema, bgmTrackSchema } from './_shared'
    ```

    2. **bgmRouter定義**
    ```typescript
    export const bgmRouter = router({
      getAll: publicProcedure
        .input(bgmGetAllInputSchema)
        .query(async ({ ctx, input }) => {
          const { db } = ctx

          // 基本クエリ
          let query = db.select().from(ctx.schema.bgmTracks)

          // tierフィルタ（指定時のみ）
          if (input?.tier) {
            query = query.where(eq(ctx.schema.bgmTracks.tier, input.tier))
          }

          const tracks = await query

          // srcフィールドを生成（/api/bgm/filename）
          return tracks.map(track => ({
            ...track,
            src: `/api/bgm/${track.filename}`,
          }))
        }),
    })
    ```

    **重要な実装詳細:**
    - `publicProcedure`を使用（認証不要）
    - `input`は全体がオプション（`.optional()`でラップ）
    - `input?.tier`でundefinedチェック（未指定時は全トラック）
    - `map()`で`src`フィールドを動的生成（既存Track型互換）
    - `ctx.schema.bgmTracks`でテーブル参照（Phase 1で作成済み）

    **TDDサイクル後のテスト更新:**
    Task 0のスタブテストを本実装に更新し、プレースホルダーを削除
  </action>
  <verify>
    <automated>npm test -- bgm-router --run</automated>
  </verify>
  <done>
    - bgmRouterがエクスポートされている
    - getAllクエリがpublicProcedureで定義されている
    - tierフィルタロジックが実装されている（input?.tierチェック）
    - srcフィールドが正しく生成されている（/api/bgm/filename形式）
    - ファイルが30行以上存在する
    - テストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BGM用Zodスキーマ定義の追加</name>
  <files>src/app/routers/_shared.ts, tests/bgm-router.test.ts</files>
  <read_first>
    - src/app/routers/_shared.ts — 既存Zodスキーマパターン（todoSchema, pomodoroSessionSchema）
    - src/hooks/useBgm.ts — 既存Track型
    - functions/lib/schema.ts — bgmTracksテーブル構造
  </read_first>
  <behavior>
    - Test 1: bgmTrackSchemaが有効なトラックオブジェクトを検証する
    - Test 2: bgmTrackSchemaが必須フィールド（id, title, src, tier）を要求する
    - Test 3: bgmTrackSchemaがartist, colorをオプションとして扱う
    - Test 4: bgmGetAllInputSchemaがtierフィルタを受け入れる
    - Test 5: bgmGetAllInputSchemaがundefined（フィルタなし）を受け入れる
  </behavior>
  <action>
    # src/app/routers/_shared.ts にBGM用スキーマを追加

    ファイルの末尾（エラー用スキーマの後）に以下を追加：

    ```typescript
    // BGM用スキーマ
    export const bgmTrackSchema = z.object({
      id: z.string().uuid(),
      title: z.string().min(1),
      src: z.string(), // /api/bgm/filename
      artist: z.string().optional(),
      color: z.string().optional(),
      tier: z.enum(['free', 'premium']),
      createdAt: z.string().datetime(),
      updatedAt: z.string().datetime(),
    })

    export const bgmGetAllInputSchema = z.object({
      tier: z.enum(['free', 'premium']).optional(),
    }).optional() // input全体をオプション化

    export type BgmTrack = z.infer<typeof bgmTrackSchema>
    export type BgmGetAllInput = z.infer<typeof bgmGetAllInputSchema>
    ```

    **重要な実装詳細:**
    - `src`フィールドは`z.string()`（バリデーション緩和：URL形式に限らない）
    - `color`はオプション（既存Track型と互換）
    - `artist`はオプション（既存Track型と互換）
    - `bgmGetAllInputSchema`は全体を`.optional()`でラップ（未指定時も`getAll()`呼び出し可能）
    - `tier`はenum型で型安全（'free' | 'premium'のみ許容）

    **TDDサイクル後のテスト更新:**
    Task 0のスタブテストは既にこのスキーマを参照しているため、実装完了後にテストがパスする
  </action>
  <verify>
    <automated>npm test -- bgm-router --run</automated>
  </verify>
  <done>
    - bgmTrackSchemaが定義されている（id, title, src, artist?, color?, tier, createdAt, updatedAt）
    - bgmGetAllInputSchemaが定義されている（tierはオプション、全体もオプション）
    - BgmTrack型がエクスポートされている
    - BgmGetAllInput型がエクスポートされている
    - tier enumが正しく定義されている（'free', 'premium'）
    - テストがパスする
  </done>
</task>

<task type="auto">
  <name>Task 3: appRouterへのbgmRouter統合</name>
  <files>src/app/routers/root.ts</files>
  <read_first>
    - src/app/routers/root.ts — 既存ルーター統合パターン
    - src/app/routers/bgm.ts — bgmRouter定義（Task 1で作成）
  </read_first>
  <action>
    # src/app/routers/root.ts にbgmRouterを統合

    以下の変更を行う：

    1. **bgmRouterのインポートを追加**
    ```typescript
    import { router } from './context'
    import { todosRouter } from './todos'
    import { pomodoroRouter } from './pomodoro'
    import { bgmRouter } from './bgm' // 追加
    ```

    2. **appRouterにbgmを追加**
    ```typescript
    export const appRouter = router({
      todos: todosRouter,
      pomodoro: pomodoroRouter,
      bgm: bgmRouter, // 追加
    })
    ```

    **重要な実装詳細:**
    - インポートは他のルーターと同じパターン（名前付きインポート）
    - router()のオブジェクトに`bgm: bgmRouter`を追加
    - `AppRouter`型は`typeof appRouter`で自動生成（変更不要）
    - 順序は他のルーターと隣接（todos, pomodoroの後）
  </action>
  <verify>
    <automated>npm run build && grep -q "bgm:" src/app/routers/root.ts</automated>
  </verify>
  <done>
    - bgmRouterがインポートされている
    - appRouterにbgmが追加されている
    - AppRouter型が正しくエクスポートされている（typeof appRouter）
    - TypeScriptコンパイルエラーがない
    - ファイルが変更されている（diffがある）
  </done>
</task>

</tasks>

<verification>

## 全体検証

### 自動化テスト
```bash
# 全テストスイート実行
npm test -- --run

# TypeScriptコンパイルチェック
npm run build
```

### 手動検証
1. tRPCパネル（`http://localhost:5173/trpc`）で `bgm.getAll` を実行
2. 期待されるレスポンス：
   ```json
   [
     {
       "id": "uuid",
       "title": "Lo-Fi Study 01",
       "src": "/api/bgm/lofi-01.mp3",
       "artist": "Chill Beats",
       "color": "#3b82f6",
       "tier": "free",
       "createdAt": "2026-03-19T...",
       "updatedAt": "2026-03-19T..."
     }
   ]
   ```
3. tierフィルタテスト：`{ "tier": "free" }` を入力してfreeのみ返却されることを確認

</verification>

<success_criteria>
- [ ] テストスタブファイルが作成されている（Wave 0）
- [ ] bgmRouterが作成され、getAllクエリが定義されている
- [ ] getAllが全トラックを返す（Phase 1のシードデータ2件）
- [ ] tierフィルタでトラックを絞り込める（free/premium）
- [ ] レスポンスにsrcフィールドが含まれる（/api/bgm/filename）
- [ ] publicProcedureで認証なしでアクセス可能
- [ ] appRouterにbgmが統合されている
- [ ] 既存Track型と互換性のあるレスポンス形式
- [ ] TypeScriptコンパイルエラーなし
- [ ] テストがパスする
</success_criteria>

<output>
After completion, create `.planning/phases/03-bgm-api-read/03-01-SUMMARY.md`
</output>
