---
phase: 09-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/stats/StatsCard.test.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
  - STAT-07
  - STAT-08
user_setup: []

must_haves:
  truths:
    - "Rechartsパッケージがインストールされている"
    - "StatsCard用のテストファイルが存在し、全要件のスタブが定義されている"
    - "テストが実行可能で、全スタブがpending状態である"
  artifacts:
    - path: "package.json"
      provides: "Recharts依存関係"
      contains: "\"recharts\": \"^3.8.0\""
    - path: "src/components/stats/StatsCard.test.ts"
      provides: "StatsCardのユニットテスト"
      min_lines: 50
      exports: ["describe", "it", "expect"]
  key_links:
    - from: "src/components/stats/StatsCard.test.ts"
      to: "src/components/stats/StatsCard.tsx"
      via: "import of default export"
      pattern: "import.*StatsCard.*from.*StatsCard"
---

## Objective

Stats機能実装のための基盤をセットアップする。Rechartsライブラリをインストールし、全要件（STAT-01～STAT-08）に対応するテストスタブを作成する。

## Purpose

RechartsはReactネイティブなチャートライブラリで、棒グラフ・折れ線グラフ・複合グラフを宣言的に記述できる。Wave 0でテストインフラを整えることで、後続のタスクでTDDサイクルを回せるようにする。

## Output

- package.jsonにrechartsが追加されている
- StatsCard.test.tsに全要件のテストスタブが定義されている

## Execution Context

@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-stats/09-CONTEXT.md
@.planning/phases/09-stats/09-RESEARCH.md
@.planning/codebase/TESTING.md

## Tasks

<task type="auto">
  <name>Task 1: Rechartsをインストール</name>
  <files>package.json</files>
  <read_first>
    - package.json（依存関係を確認するため）
  </read_first>
  <action>
    Recharts v3.8.0をプロジェクトにインストールする。

    コマンド：
    ```bash
    npm install recharts@^3.8.0
    ```

    インストール後、package.jsonにrechartsが追加されていることを確認する。
  </action>
  <verify>
    <automated>grep -q '"recharts"' package.json && npm list recharts | grep -q '3.8.0'</automated>
  </verify>
  <done>
    - package.jsonのdependenciesに"recharts": "^3.8.0"が含まれている
    - npm list rechartsの実行でバージョン3.8.0が表示される
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatsCard用テストファイルを作成（Wave 0）</name>
  <files>src/components/stats/StatsCard.test.ts</files>
  <read_first>
    - src/test/setup.ts（テスト設定を確認するため）
    - src/hooks/useTimer.test.ts（既存のテストパターンを参照するため）
  </read_first>
  <behavior>
    STAT-01（今日の統計表示）: 今日の集中時間とセッション数が正しく集計される
    STAT-02（週次統計表示）: 直近7日間の統計が正しく集計される
    STAT-03（月次統計表示）: 今月の合計集中時間とセッション数が正しく集計される
    STAT-04（棒グラフ表示）: 日別セッション数の棒グラフが表示される
    STAT-05（折れ線グラフ表示）: 累積集中時間の折れ線グラフが表示される
    STAT-06（ローディング状態）: ローディング中にスピナーが表示される
    STAT-07（空状態）: データがない場合に空状態が表示される
    STAT-08（データ更新時の再描画）: セッション完了後に統計が自動更新される
  </behavior>
  <action>
    src/components/stats/StatsCard.test.tsを作成し、STAT-01～STAT-08に対応するテストスタブを定義する。

    既存のuseTimer.test.tsのパターンに従い、以下の構造で実装：

    ```typescript
    import { renderHook } from '@testing-library/react'
    import { describe, it, expect } from 'vitest'
    import { usePomodoro } from '../../hooks/usePomodoro'

    describe('StatsCard', () => {
      describe('STAT-01: 今日の統計表示', () => {
        it('今日の集中時間とセッション数が正しく集計される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-02: 週次統計表示', () => {
        it('直近7日間の統計が正しく集計される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-03: 月次統計表示', () => {
        it('今月の合計集中時間とセッション数が正しく集計される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-04: 棒グラフ表示', () => {
        it('日別セッション数の棒グラフが表示される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-05: 折れ線グラフ表示', () => {
        it('累積集中時間の折れ線グラフが表示される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-06: ローディング状態', () => {
        it('ローディング中にスピナーが表示される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-07: 空状態', () => {
        it('データがない場合に空状態が表示される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })

      describe('STAT-08: データ更新時の再描画', () => {
        it('セッション完了後に統計が自動更新される', () => {
          // TODO: 実装
          expect(true).toBe(true)
        })
      })
    })
    ```

    注: このファイルはテストスタブのみを含み、実際のテスト実装は後続のプランで行う。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts --run</automated>
  </verify>
  <done>
    - src/components/stats/StatsCard.test.tsが作成されている
    - STAT-01～STAT-08に対応する8つのdescribeブロックが定義されている
    - npm testの実行で全テストがpassing状態である（expect(true).toBe(true)のため）
  </done>
</task>

## Verification

1. Rechartsが正しくインストールされている
   - `npm list recharts`でバージョン3.8.0が表示される

2. テストファイルが実行可能である
   - `npm test -- src/components/stats/StatsCard.test.ts --run`で全テストがパスする

## Success Criteria

1. package.jsonにrecharts依存が追加されている
2. StatsCard.test.tsに8つの要件に対応するテストスタブが定義されている
3. テスト実行コマンドがエラーなく完了する

## Output

完了後、`.planning/phases/09-stats/09-01-SUMMARY.md`を作成する
