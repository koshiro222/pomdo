---
phase: 16-card-header-unification
plan: "01"
type: execute
wave: 1
depends_on:
  - "16-00"
files_modified:
  - src/App.tsx
  - src/components/bgm/BgmPlayer.tsx
  - src/components/todos/TodoList.tsx
autonomous: true
requirements:
  - HEADER-01
  - HEADER-02
  - HEADER-03
must_haves:
  truths:
    - "タイマーカードの左上に「Pomodoro」テキストが表示される"
    - "BGMカードの左上に「BGM」テキスト、右上にListボタンが配置される"
    - "TodoListカードの左上に「Tasks」テキスト、右上にバッジが配置される"
    - "3カードのヘッダーテキストスタイルが同一（text-xs uppercase tracking-widest font-bold text-cf-text）"
  artifacts:
    - path: "src/App.tsx"
      provides: "TimerWidgetヘッダーセクション"
      contains: "Pomodoro"
    - path: "src/components/bgm/BgmPlayer.tsx"
      provides: "BGMヘッダーレイアウト（左テキスト・右ボタン）"
      pattern: "flex items-center justify-between mb-4"
    - path: "src/components/todos/TodoList.tsx"
      provides: "TodoListヘッダー（テキストのみ、統一スタイル）"
      contains: "text-xs uppercase tracking-widest font-bold"
  key_links:
    - from: "src/App.tsx TimerWidget"
      to: "ヘッダーテキスト"
      pattern: "text-xs uppercase tracking-widest font-bold text-cf-text"
    - from: "src/components/bgm/BgmPlayer.tsx"
      to: "flexコンテナ"
      pattern: "flex items-center justify-between mb-4"
    - from: "src/components/todos/TodoList.tsx"
      to: "ヘッダーh3"
      pattern: "text-xs uppercase tracking-widest font-bold text-cf-text"
---

<objective>
全3カード（タイマー・BGM・TodoList）のヘッダーテキストスタイルと配置を統一し、視覚的一貫性を確保する。

Purpose: ユーザーが3つのカードを見た際に、一貫したヘッダーデザインでどのカードか即座に識別できるようにする。
Output: 統一されたヘッダースタイル（`text-xs uppercase tracking-widest font-bold text-cf-text`）を持つ3つのカードコンポーネント。
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-card-header-unification/16-CONTEXT.md
@.planning/phases/16-card-header-unification/16-RESEARCH.md
@.planning/phases/16-card-header-unification/16-UI-SPEC.md

<interfaces>
<!-- 統一ヘッダースタイル仕様（CONTEXT.md決定） -->
```tsx
// 全カード共通のヘッダーテキストスタイル
className="text-xs uppercase tracking-widest font-bold text-cf-text"

// ヘッダーコンテナ（左右配置）
className="flex items-center justify-between mb-4"
```

<!-- 既存コンポーネントの現在状態 -->
<!-- TimerWidget (src/App.tsx line 64-88): ヘッダーなし -->
<!-- BgmPlayer (src/components/bgm/BgmPlayer.tsx line 80-96): 3要素レイアウト（Listボタン | BGMテキスト | スペーサー） -->
<!-- TodoList (src/components/todos/TodoList.tsx line 76-84): text-lg font-bold + CheckSquareアイコン -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimerWidgetにヘッダーセクション追加（HEADER-01）</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx（現在のTimerWidget構造を確認）
  </read_first>
  <action>
TimerWidget関数（line 21-89）のreturn文内にヘッダーセクションを追加する。

現在の構造（line 64-88）:
```tsx
return (
  <div className="flex flex-col h-full items-center p-6">
    <TimerDisplay ... />
    <audio ... />
  </div>
)
```

変更後の構造:
```tsx
return (
  <div className="flex flex-col h-full items-center p-4">
    {/* ヘッダー */}
    <div className="w-full flex items-center justify-between mb-4">
      <p className="text-xs uppercase tracking-widest font-bold text-cf-text">
        Pomodoro
      </p>
    </div>

    {/* 既存のタイマー表示 */}
    <TimerDisplay ... />

    <audio ... />
  </div>
)
```

具体的な変更:
1. 外側divのpaddingを `p-6` → `p-4` に変更
2. TimerDisplayの直前にヘッダーセクション（`div.w-full.flex.items-center.justify-between.mb-4`）を追加
3. ヘッダー内に「Pomodoro」テキストを `text-xs uppercase tracking-widest font-bold text-cf-text` スタイルで配置
4. ヘッダーは左寄せのみ（右上要素なし）、境界線なし（border-bなし）
</action>
  <verify>
    <automated>npm test -- --run src/components/timer/__tests__/card-header.test.ts && grep -n "Pomodoro" src/App.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsxに"Pomodoro"という文字列が含まれる
    - TimerWidget内に`className="text-xs uppercase tracking-widest font-bold text-cf-text"`を持つp要素が存在する
    - ヘッダーコンテナが`flex items-center justify-between mb-4`クラスを持つ
  </acceptance_criteria>
  <done>
    TimerWidgetの左上に「Pomodoro」テキストが統一スタイルで表示される。paddingがp-4に統一される。
  </done>
</task>

<task type="auto">
  <name>Task 2: BgmPlayerヘッダーレイアウト変更（HEADER-02）</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx（現在のヘッダー構造 line 79-97 を確認）
  </read_first>
  <action>
BgmPlayerのヘッダーセクション（line 79-97）を以下のように変更する。

現在の構造（line 79-97）:
```tsx
{/* ヘッダー */}
<div className="flex items-center justify-between mb-3">
  <motion.button ... >
    <List ... />
  </motion.button>
  <p className="text-xs uppercase tracking-widest text-cf-subtext font-bold">
    BGM
  </p>
  <div className="w-5" /> {/* スペーサー */}
</div>
```

変更後の構造:
```tsx
{/* ヘッダー */}
<div className="flex items-center justify-between mb-4">
  <p className="text-xs uppercase tracking-widest font-bold text-cf-text">
    BGM
  </p>
  <motion.button
    {...hoverAnimation}
    {...tapAnimation}
    onClick={() => setIsExpanded(!isExpanded)}
    className="p-3 rounded-xl text-cf-subtext hover:text-cf-primary transition-colors"
    title={isExpanded ? '縮小' : 'プレイリスト'}
    aria-label={isExpanded ? '縮小' : 'プレイリスト'}
  >
    <motion.div animate={{ rotate: isExpanded ? 180 : 0 }} transition={{ duration: 0.3 }}>
      <List className="w-5 h-5" />
    </motion.div>
  </motion.button>
</div>
```

具体的な変更:
1. margin-bottomを `mb-3` → `mb-4` に変更
2. 要素順序を「Listボタン | BGMテキスト | スペーサー」から「BGMテキスト | Listボタン」に変更
3. スペーサー要素（`<div className="w-5" />`）を削除
4. 「BGM」テキストの色を `text-cf-subtext` → `text-cf-text` に変更
5. Listボタンの実装はそのまま維持（onClick, hoverAnimation, tapAnimation, title, aria-label全て維持）
</action>
  <verify>
    <automated>npm test -- --run src/components/bgm/__tests__/card-header.test.ts && grep -n "flex items-center justify-between mb-4" src/components/bgm/BgmPlayer.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsxに`className="w-5"`を持つスペーサーdivが存在しない
    - ヘッダーのmargin-bottomが`mb-4`に変更されている
    - 「BGM」テキストが`text-cf-text`クラスを持つ（`text-cf-subtext`ではない）
    - ヘッダー内の要素順序が「BGMテキスト → Listボタン」になっている
  </acceptance_criteria>
  <done>
    BGMカードのヘッダーが左上に「BGM」テキスト、右上にListボタンの配置になる。スペーサーが削除され、mb-4に統一される。
  </done>
</task>

<task type="auto">
  <name>Task 3: TodoListヘッダースタイル変更（HEADER-03）</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（現在のヘッダー構造 line 74-84 を確認）
  </read_first>
  <action>
TodoListのヘッダーセクション（line 74-84）を以下のように変更する。

現在の構造（line 74-84）:
```tsx
{/* ヘッダー */}
<div className="p-6 border-b border-white/10">
  <div className="flex items-center justify-between mb-4">
    <h3 className="text-lg font-bold flex items-center gap-2 text-cf-text">
      <CheckSquare className="text-cf-primary" />
      Tasks
    </h3>
    <span className="text-xs bg-cf-primary/20 text-cf-primary px-2 py-0.5 rounded-full font-bold">
      {remainingTodos} Left
    </span>
  </div>
```

変更後の構造:
```tsx
{/* ヘッダー */}
<div className="p-6 border-b border-white/10">
  <div className="flex items-center justify-between mb-4">
    <h3 className="text-xs uppercase tracking-widest font-bold text-cf-text">
      Tasks
    </h3>
    <span className="text-xs bg-cf-primary/20 text-cf-primary px-2 py-0.5 rounded-full font-bold">
      {remainingTodos} Left
    </span>
  </div>
```

具体的な変更:
1. h3のスタイルを `text-lg font-bold flex items-center gap-2 text-cf-text` → `text-xs uppercase tracking-widest font-bold text-cf-text` に変更
2. CheckSquareアイコン（`<CheckSquare className="text-cf-primary" />`）を削除
3. h3の内容をアイコン+テキストからテキストのみに変更
4. 「X Left」バッジのスタイルは変更なし（`text-xs bg-cf-primary/20 text-cf-primary px-2 py-0.5 rounded-full font-bold`）
5. CheckSquareのimport（line 5）を削除

※ トップレベルのimportステートメント（line 5）からCheckSquareを削除することを忘れないこと:
```tsx
// 変更前
import { CheckSquare } from 'lucide-react'

// 変更後（CheckSquareを削除）
// import文からCheckSquareを削除（他のlucide-reactアイコンがあれば残す）
```
</action>
  <verify>
    <automated>npm test -- --run src/components/todos/__tests__/card-header.test.ts && test $(grep -c "CheckSquare" src/components/todos/TodoList.tsx) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - src/components/todos/TodoList.tsxに"CheckSquare"という文字列が0回出現する
    - h3要素が`text-xs uppercase tracking-widest font-bold text-cf-text`クラスを持つ
    - 「X Left」バッジ（span要素）が`bg-cf-primary/20 text-cf-primary`クラスを持つ（変更なし）
  </acceptance_criteria>
  <done>
    TodoListカードのヘッダーテキストが統一スタイル（text-xs uppercase tracking-widest font-bold）に変更される。CheckSquareアイコンが削除され、テキストのみになる。
  </done>
</task>

</tasks>

<verification>
## 視覚的確認（実行後）

1. `npm run dev` で開発サーバーを起動
2. ブラウザで http://localhost:5173 にアクセス
3. 3カード（タイマー・BGM・TodoList）のヘッダーを確認:
   - 全てのヘッダーテキストが左上に配置されている
   - 全てのヘッダーテキストが同一の視覚ウェイト（text-xs, uppercase, tracking-widest, font-bold）を持つ
   - BGMとTodoListの右上要素（Listボタン、バッジ）が適切に配置されている

## 自動テスト

```bash
# テスト実行（全カードヘッダーテスト）
npm test -- --run

# ヘッダー統一の確認
grep -n "text-xs uppercase tracking-widest font-bold text-cf-text" src/App.tsx src/components/bgm/BgmPlayer.tsx src/components/todos/TodoList.tsx

# BGMスペーサー削除の確認
grep -c 'className="w-5"' src/components/bgm/BgmPlayer.tsx || echo "スペーサー削除済み"

# TodoList CheckSquare削除の確認
grep -c "CheckSquare" src/components/todos/TodoList.tsx || echo "CheckSquare削除済み"
```
</verification>

<success_criteria>
1. タイマーカードの左上に「Pomodoro」テキストが `text-xs uppercase tracking-widest font-bold` スタイルで表示される
2. BGMカードのヘッダーテキストが左上に配置され、Listボタンが右上に独立して配置される
3. TodoListカードのヘッダーテキストが `text-xs uppercase tracking-widest font-bold` スタイルに変更され、他カードと見た目が揃う
4. 3カードを横並びで見たとき、各カード左上のヘッダーテキストが同一の視覚ウェイトと大文字スタイルで統一されている
</success_criteria>

<output>
After completion, create `.planning/phases/16-card-header-unification/16-01-SUMMARY.md`
</output>
