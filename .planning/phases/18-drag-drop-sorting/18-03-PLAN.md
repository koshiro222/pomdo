---
phase: 18-drag-drop-sorting
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified: [package.json, src/components/todos/TodoItem.tsx, src/components/todos/TodoList.tsx]
autonomous: true
requirements: [DND-01]
user_setup: []

must_haves:
  truths:
    - "ユーザーがドラッグハンドルでタスクを掴んで上下に移動できる"
    - "ドロップするとタスクが新しい位置に配置される"
    - "ドラッグ中に他のアイテムがスムーズにスライドする"
  artifacts:
    - path: "package.json"
      provides: "@dnd-kit依存関係"
      contains: "@dnd-kit/core"
    - path: "src/components/todos/TodoItem.tsx"
      provides: "useSortableフック統合"
      exports: ["SortableTodoItem"]
    - path: "src/components/todos/TodoList.tsx"
      provides: "DndContext, SortableContext"
      contains: "DndContext"
  key_links:
    - from: "TodoList.tsx DndContext"
      to: "TodoItem.tsx useSortable"
      via: "SortableContext items"
      pattern: "SortableContext"
    - from: "TodoList.tsx onDragEnd"
      to: "useTodos.ts reorderTodo"
      via: "handleDragEnd callback"
      pattern: "reorderTodo"
---

<objective>
@dnd-kitライブラリを導入し、TodoItem/TodoListコンポーネントにドラッグ&ドロップ機能を統合する。

Purpose: ユーザーがドラッグハンドルでタスクを並び替えできるUIを実装する。
Output: DnD機能付きTodoItem、DndContext統合TodoList
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-drag-drop-sorting/18-CONTEXT.md
@.planning/phases/18-drag-drop-sorting/18-RESEARCH.md
@.planning/phases/18-drag-drop-sorting/18-01-SUMMARY.md
@.planning/phases/18-drag-drop-sorting/18-02-SUMMARY.md

<interfaces>
<!-- 既存のTodoItem構造 -->
From src/components/todos/TodoItem.tsx:
```tsx
interface TodoItemProps {
  id: string
  title: string
  completed: boolean
  completedPomodoros?: number
  isNew?: boolean
  isSelected?: boolean
  onClick?: () => void
  onToggle: (id: string) => void
  onDelete: (id: string) => void
}

// ドラッグハンドル（L55-62）
<motion.button
  {...tapAnimation}
  onClick={(e) => e.stopPropagation()}
  className="opacity-30 group-hover:opacity-50 transition-opacity cursor-grab active:cursor-grabbing text-cf-subtext"
>
  <GripVertical className="text-sm" />
</motion.button>
```

<!-- 既存のTodoList構造 -->
From src/components/todos/TodoList.tsx:
```tsx
// useTodosからreorderTodoを取得可能（18-02で追加済み）
const { todos, ..., reorderTodo } = useTodos()

// AnimatePresence内でレンダリング（L157-172）
<AnimatePresence mode="popLayout">
  {filteredTodos.map((todo: Todo) => (
    <TodoItem key={todo.id} ... />
  ))}
</AnimatePresence>
``>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: @dnd-kitライブラリをインストール</name>
  <files>package.json</files>
  <read_first>
    - package.json（現在の依存関係を確認）
  </read_first>
  <behavior>
    - @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilitiesがインストールされる
  </behavior>
  <action>
package.jsonに@dnd-kitライブラリを追加:

```bash
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

バージョン確認（RESEARCH.md推奨）:
- @dnd-kit/core: 6.3.1
- @dnd-kit/sortable: 10.0.0
- @dnd-kit/utilities: 3.2.2
  </action>
  <verify>
    <automated>grep -q "@dnd-kit/core" package.json && echo "@dnd-kit installed" || echo "MISSING: @dnd-kit"</automated>
  </verify>
  <done>
    - package.jsonに@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilitiesが存在する
    - npm installが成功する
  </done>
</task>

<task type="auto">
  <name>Task 2: TodoItemにuseSortableフックを統合</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在のTodoItem実装を確認）
    - src/lib/animation.ts（expandInVariantsを確認）
  </read_first>
  <behavior>
    - TodoItemがuseSortableフックを使用してドラッグ可能になる
    - ドラッグハンドルにlistenersとattributesがバインドされる
    - ドラッグ中のアイテムが半透明になる
    - 既存のlayout propとexpandInVariantsが維持される
  </behavior>
  <action>
src/components/todos/TodoItem.tsxを修正:

1. importを追加:
```tsx
import { useSortable } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
```

2. コンポーネント先頭でuseSortableフックを使用:
```tsx
export default function TodoItem({
  id,
  // ...props
}: TodoItemProps) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id })

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
    zIndex: isDragging ? 1000 : 'auto',
  }

  // ...existing code
}
```

3. motion.divをdivでラップし、setNodeRefとstyleを適用:
```tsx
return (
  <div ref={setNodeRef} style={style} className={isDragging ? 'relative' : ''}>
    <motion.div
      layout
      onClick={onClick}
      {...hoverAnimation}
      variants={expandInVariants}
      initial={isNew ? 'hidden' : false}
      animate="visible"
      exit="exit"
      className={`group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors ${
        completed ? 'opacity-60' : ''
      } ${isSelected && !completed ? 'border-l-2 border-cf-primary' : ''} ${onClick && !completed ? 'cursor-pointer' : ''}`}
    >
      {/* ドラッグハンドルにlistenersとattributesを追加 */}
      <motion.button
        {...tapAnimation}
        {...attributes}
        {...listeners}
        onClick={(e) => e.stopPropagation()}
        className="opacity-30 group-hover:opacity-50 transition-opacity cursor-grab active:cursor-grabbing text-cf-subtext"
      >
        <GripVertical className="text-sm" />
      </motion.button>
      {/* ...rest of component */}
    </motion.div>
  </div>
)
```

重要: motion.divにはlayout propを維持し、外側のdivでdnd-kitのtransformを制御する。
  </action>
  <verify>
    <automated>grep -q "useSortable" src/components/todos/TodoItem.tsx && echo "useSortable integrated" || echo "MISSING: useSortable"</automated>
  </verify>
  <done>
    - TodoItemがuseSortableフックを使用している
    - ドラッグハンドルにlistenersとattributesがバインドされている
    - ドラッグ中のアイテムが半透明（opacity: 0.5）になる
    - npm run buildが成功する
  </done>
</task>

<task type="auto">
  <name>Task 3: TodoListにDndContextとSortableContextを追加</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（現在のTodoList実装を確認）
    - src/hooks/useTodos.ts（reorderTodoメソッドを確認）
  </read_first>
  <behavior>
    - TodoListがDndContextでラップされ、ドラッグイベントを処理する
    - SortableContextが全todos（filteredTodosではない）を管理する
    - onDragEndでreorderTodoが呼び出される
    - フィルタリング状態に関わらずD&Dが有効
  </behavior>
  <action>
src/components/todos/TodoList.tsxを修正:

1. importを追加:
```tsx
import { DndContext, closestCenter, PointerSensor, useSensor, useSensors, DragEndEvent } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy, arrayMove } from '@dnd-kit/sortable'
```

2. useTodosからreorderTodoを取得:
```tsx
const { todos, selectedTodoId, loading, addTodo, updateTodo, deleteTodo, setSelectedTodoId, reorderTodo } = useTodos()
```

3. センサー設定を追加（コンポーネント内）:
```tsx
const sensors = useSensors(
  useSensor(PointerSensor, {
    activationConstraint: {
      distance: 8, // 8px移動でドラッグ開始（クリック誤検出防止）
    },
  })
)
```

4. onDragEndハンドラを追加:
```tsx
const handleDragEnd = (event: DragEndEvent) => {
  const { active, over } = event
  if (over && active.id !== over.id) {
    const oldIndex = todos.findIndex((t) => t.id === active.id)
    const newIndex = todos.findIndex((t) => t.id === over.id)
    if (oldIndex !== -1 && newIndex !== -1) {
      reorderTodo(todos[oldIndex].id, newIndex)
    }
  }
}
```

5. AnimatePresenceをDndContextとSortableContextでラップ:
```tsx
<DndContext
  sensors={sensors}
  collisionDetection={closestCenter}
  onDragEnd={handleDragEnd}
>
  <SortableContext items={todos.map(t => t.id)} strategy={verticalListSortingStrategy}>
    {filteredTodos.length === 0 ? (
      <div className="text-cf-subtext text-center py-8">
        No tasks yet
      </div>
    ) : (
      <AnimatePresence mode="popLayout">
        {filteredTodos.map((todo: Todo) => (
          <TodoItem
            key={todo.id}
            id={todo.id}
            title={todo.title}
            completed={todo.completed}
            completedPomodoros={todo.completedPomodoros}
            isNew={newTodoId === todo.id}
            isSelected={selectedTodoId === todo.id}
            onClick={() => handleTodoClick(todo)}
            onToggle={(id) => updateTodo(id, { completed: !todo.completed })}
            onDelete={deleteTodo}
          />
        ))}
      </AnimatePresence>
    )}
  </SortableContext>
</DndContext>
```

重要: SortableContextのitemsには全todosを使用（filteredTodosではない）。これによりフィルタリング中も正しい位置計算が可能。
  </action>
  <verify>
    <automated>grep -q "DndContext" src/components/todos/TodoList.tsx && grep -q "SortableContext" src/components/todos/TodoList.tsx && echo "DnD integrated" || echo "MISSING: DnD"</automated>
  </verify>
  <done>
    - TodoListがDndContextとSortableContextでラップされている
    - SortableContextのitemsが全todosを含んでいる
    - onDragEndでreorderTodoが呼び出される
    - npm run buildが成功する
  </done>
</task>

</tasks>

<verification>
- TypeScriptコンパイル: `npm run build`が成功
- DnD統合: DndContextとSortableContextが正しく配置されている
- イベント処理: onDragEndがreorderTodoを呼び出す
</verification>

<success_criteria>
- @dnd-kitライブラリがインストールされている
- TodoItemがuseSortableフックを使用している
- TodoListがDndContext/SortableContextでラップされている
- ドラッグ&ドロップでタスクの並び替えが可能
</success_criteria>

<output>
After completion, create `.planning/phases/18-drag-drop-sorting/18-03-SUMMARY.md`
</output>
