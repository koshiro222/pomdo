---
phase: 15-todolist-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/todos/TodoList.tsx
  - src/components/todos/TodoItem.tsx
autonomous: true
requirements:
  - TODO-01
  - TODO-02
  - TODO-03
  - TODO-04

must_haves:
  truths:
    - "TodoListカードのヘッダー領域に選択中タスクのハイライトセクションが表示される"
    - "ハイライトセクションにタスク名と完了数（X done）が表示される"
    - "Completeボタンクリックでタスクが完了する"
    - "Nextボタンクリックで次のタスクが選択される"
    - "TodoItemの選択中スタイルが左ボーダー（border-l-2 border-cf-primary）で表示される"
    - "タスク未選択時にハイライトセクションが表示されない"
  artifacts:
    - path: "src/components/todos/TodoList.tsx"
      provides: "ハイライトセクションとアクションボタン"
      contains: "AnimatePresence"
      contains: "Current Task"
      contains: "✓ Complete"
      contains: "→ Next"
    - path: "src/components/todos/TodoItem.tsx"
      provides: "選択中タスクの視覚的強調"
      contains: "border-l-2 border-cf-primary"
  key_links:
    - from: "src/components/todos/TodoList.tsx"
      to: "useTodos hook"
      via: "selectedTodo, handleComplete, handleSelectNext"
      pattern: "useTodos"
    - from: "src/components/todos/TodoList.tsx"
      to: "TodoItem.tsx"
      via: "isSelected prop"
      pattern: "isSelected="
---

<objective>
TodoListカードにCurrentTaskCardの機能を統合し、選択中タスクのハイライトセクションとアクションボタンを実装する。TodoItemの選択中スタイルをボーダーのみに変更する。

Purpose: Phase 14でCurrentTaskCardをDOMから削除したため、その機能をTodoListカード内に統合し、ユーザーがタスクの確認とPomodoro操作をTodoList内で完結できるようにする。
Output: 更新されたTodoList.tsx（ハイライトセクション追加）、TodoItem.tsx（選択中スタイル変更）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-todolist-ui/15-CONTEXT.md
@.planning/phases/15-todolist-ui/15-RESEARCH.md
@.planning/phases/15-todolist-ui/15-UI-SPEC.md
</context>

<interfaces>

<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useTodos.ts:
```typescript
export type { Todo, NewTodo, UpdateTodo }

export function useTodos() {
  return {
    todos: Todo[],
    selectedTodoId: string | null,
    setSelectedTodoId: (id: string | null) => void,
    updateTodo: (id: string, updates: UpdateTodo) => Promise<Todo | null>,
    deleteTodo: (id: string) => Promise<boolean>,
    // ...other properties
  }
}
```

From src/components/todos/TodoItem.tsx:
```typescript
interface TodoItemProps {
  id: string
  title: string
  completed: boolean
  completedPomodoros?: number
  isNew?: boolean
  isSelected?: boolean  // <-- このpropを既に受け取っている
  onClick?: () => void
  onToggle: (id: string) => void
  onDelete: (id: string) => void
}
```

From src/lib/animation.ts:
```typescript
export const slideInVariants: Variants = {
  hidden: { opacity: 0, x: -20 },
  visible: { opacity: 1, x: 0, transition: { duration: 0.3, ease: [0, 0, 0.2, 1] } },
  exit: { opacity: 0, x: 20, transition: { duration: 0.2, ease: [0.4, 0, 1, 1] } },
}

export const tapAnimation = { whileTap: { scale: 0.95 } }
export const hoverAnimation = { whileHover: { scale: 1.02 } }
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TodoList.tsxにハイライトセクションとアクションボタンを追加</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（現在の実装）
    - src/components/tasks/CurrentTaskCard.tsx（ロジック移管元の参考）
    - src/hooks/useTodos.ts（使用可能な関数）
    - src/lib/animation.ts（アニメーションバリアント）
  </read_first>
  <behavior>
    - 選択中タスクがある場合、ヘッダー領域（タイトルとフィルタータブの間）にハイライトセクションが表示される
    - ハイライトセクションには「Current Task」ラベル、タスク名、完了数（X done）が表示される
    - Completeボタンクリックでタスクが完了し、選択が解除される
    - Nextボタンクリックで次の未完了タスクが選択される
    - AnimatePresenceでスムーズなアニメーションを実現
  </behavior>
  <action>
    TodoList.tsxを以下のように変更する：

    1. **インポート追加**（ファイル先頭）:
       - `AnimatePresence`をframer-motionから既にimport済み

    2. **selectedTodoの計算**（useTodos呼び出し後）:
       ```typescript
       const selectedTodo = todos.find((t: Todo) => t.id === selectedTodoId && !t.completed)
       const hasMoreTodos = todos.some((t: Todo) => !t.completed && t.id !== selectedTodoId)
       ```

    3. **ハンドラー関数の追加**（handleTodoClickの後）:
       ```typescript
       const handleComplete = async () => {
         if (selectedTodo) {
           await updateTodo(selectedTodo.id, { completed: true })
           setSelectedTodoId(null)
         }
       }

       const handleSelectNext = () => {
         const nextTodo = todos.find((t: Todo) => !t.completed && t.id !== selectedTodoId)
         if (nextTodo) {
           setSelectedTodoId(nextTodo.id)
         }
       }
       ```

    4. **ハイライトセクションの追加**（ヘッダー内、タイトル行の直後、フィルタータブの前）:
       タイトル行（`<div className="flex items-center justify-between mb-4">...</div>`）の直後に追加：
       ```tsx
       {/* ハイライトセクション */}
       <AnimatePresence mode="popLayout">
         {selectedTodo && (
           <motion.div
             variants={slideInVariants}
             initial="hidden"
             animate="visible"
             exit="exit"
             className="bg-white/5 rounded-xl p-3 mb-4 flex items-center justify-between"
           >
             <div className="flex-1 min-w-0">
               <p className="text-xs uppercase tracking-widest text-cf-subtext font-bold mb-1">
                 Current Task
               </p>
               <p className="text-base font-bold text-cf-text truncate">
                 {selectedTodo.title}
               </p>
               <p className="text-sm text-cf-subtext">
                 {selectedTodo.completedPomodoros || 0} done
               </p>
             </div>
             <div className="flex gap-2 flex-shrink-0 ml-3">
               <motion.button
                 {...tapAnimation}
                 onClick={handleComplete}
                 className="bg-cf-primary hover:bg-cf-primary/80 text-white text-sm font-bold py-2 px-3 rounded-lg transition-colors"
               >
                 ✓ Complete
               </motion.button>
               {hasMoreTodos && (
                 <motion.button
                   {...tapAnimation}
                   onClick={handleSelectNext}
                   className="bg-white/10 hover:bg-white/20 text-cf-text text-sm font-bold py-2 px-3 rounded-lg transition-colors"
                 >
                   → Next
                 </motion.button>
               )}
             </div>
           </motion.div>
         )}
       </AnimatePresence>
       ```

    **重要な制約（CONTEXT.mdより）**:
    - 完了数は「X done」形式のみ（目標数/estimateは表示しない）
    - ボタンテキストは「✓ Complete」「→ Next」（短い形式）
    - タップターゲットサイズはpy-2 px-3以上
    - AnimatePresenceのmodeは"popLayout"（レイアウトシフト防止）
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    <manual>grep -n "Current Task" src/components/todos/TodoList.tsx</manual>
    <manual>grep -n "✓ Complete" src/components/todos/TodoList.tsx</manual>
    <manual>grep -n "→ Next" src/components/todos/TodoList.tsx</manual>
    <manual>grep -n "AnimatePresence" src/components/todos/TodoList.tsx</manual>
  </verify>
  <done>
    - TodoList.tsxにハイライトセクションが追加されている
    - 選択中タスクがある場合のみハイライトセクションが表示される
    - Complete/Nextボタンが機能する
    - ユニットテストがパスする
    - TypeScriptビルドが成功する
  </done>
</task>

<task type="auto">
  <name>Task 2: TodoItem.tsxの選択中スタイルをボーダーのみに変更</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在の実装）
  </read_first>
  <behavior>
    - isSelected && !completedの場合、左ボーダー（border-l-2 border-cf-primary）が表示される
    - 背景色は変更されない（既存のbg-white/5のまま）
  </behavior>
  <action>
    TodoItem.tsxのmotion.divのclassNameを変更する。

    **現在（L49-51）**:
    ```tsx
    className={`group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors ${
      completed ? 'opacity-60' : ''
    } ${isSelected && !completed ? 'bg-cf-primary/20 border-cf-primary/50' : ''} ${onClick && !completed ? 'cursor-pointer' : ''}`}
    ```

    **変更後**:
    ```tsx
    className={`group flex items-center gap-3 px-4 py-3 bg-white/5 rounded-xl border border-white/5 hover:border-cf-primary/30 transition-colors ${
      completed ? 'opacity-60' : ''
    } ${isSelected && !completed ? 'border-l-2 border-cf-primary' : ''} ${onClick && !completed ? 'cursor-pointer' : ''}`}
    ```

    変更点：
    - `bg-cf-primary/20 border-cf-primary/50` を `border-l-2 border-cf-primary` に変更
    - 背景色変更を削除し、左ボーダーのみで強調表示

    **CONTEXT.mdの決定事項**:
    - 「選択中タスクはリスト内に残す。強調表示: 左ボーダー（border-l-2 border-cf-primary）。背景色は変更せずボーダーのみで区別」
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    <manual>grep -n "border-l-2 border-cf-primary" src/components/todos/TodoItem.tsx</manual>
    <manual>! grep -q "bg-cf-primary/20 border-cf-primary/50" src/components/todos/TodoItem.tsx</manual>
  </verify>
  <done>
    - TodoItem.tsxの選択中スタイルがborder-l-2 border-cf-primaryに変更されている
    - bg-cf-primary/20 border-cf-primary/50が削除されている
    - ユニットテストがパスする
    - TypeScriptビルドが成功する
  </done>
</task>

</tasks>

<verification>
## 自動検証
- `npm test -- --run` - 全ユニットテストがパスすること
- `npm run build` - TypeScriptビルドが成功すること

## 手動検証
1. `npm run dev`で開発サーバーを起動
2. TodoListカードでタスクを追加
3. タスクをクリックして選択
4. ハイライトセクションが表示されることを確認
5. Completeボタンでタスクが完了することを確認
6. Nextボタンで次のタスクが選択されることを確認
7. TodoItemに左ボーダーが表示されることを確認
</verification>

<success_criteria>
- [x] TodoList.tsxにハイライトセクションが追加されている
- [x] 選択中タスクがある場合のみハイライトセクションが表示される
- [x] ハイライトセクションに「Current Task」ラベル、タスク名、完了数が表示される
- [x] Completeボタンクリックでタスクが完了する
- [x] Nextボタンクリックで次のタスクが選択される
- [x] TodoItemの選択中スタイルがborder-l-2 border-cf-primaryに変更されている
- [x] ユニットテストが全てパスする
- [x] TypeScriptビルドが成功する
</success_criteria>

<output>
After completion, create `.planning/phases/15-todolist-ui/15-01-SUMMARY.md`
</output>
