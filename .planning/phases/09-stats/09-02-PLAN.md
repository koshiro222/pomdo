---
phase: 09-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/stats/StatsCard.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-04
user_setup: []

must_haves:
  truths:
    - "タブ切り替えUI（Today/Week/Month）が表示される"
    - "Todayタブで今日の統計（集中時間・セッション数）が表示される"
    - "Weekタブで週次統計が表示される"
    - "Weekタブで日別セッション数の棒グラフが表示される"
  artifacts:
    - path: "src/components/stats/StatsCard.tsx"
      provides: "StatsCardメインコンポーネント"
      min_lines: 150
      exports: ["default"]
      contains: "useState<'today' | 'week' | 'month'>"
  key_links:
    - from: "src/components/stats/StatsCard.tsx"
      to: "recharts"
      via: "import of BarChart, ComposedChart, ResponsiveContainer, XAxis, YAxis, Tooltip, Bar"
      pattern: "import.*from 'recharts'"
    - from: "src/components/stats/StatsCard.tsx"
      to: "src/hooks/usePomodoro.ts"
      via: "usePomodoro hook"
      pattern: "const { sessions, loading } = usePomodoro\\(\\)"
---

## Objective

タブ切り替えUIとToday/Week統計を実装する。Rechartsを使用して週次統計の棒グラフを表示し、既存の自前実装を置き換える。

## Purpose

ユーザーが今日・週次の統計を切り替えて確認できるようにする。RechartsのBarChartを使用することで、レスポンシブ対応・アニメーション・Tooltipを自動的に获得する。

## Output

- StatsCard.tsxにタブ切り替えUIが実装されている
- Todayタブで今日の統計が表示される
- Weekタブで週次統計と棒グラフが表示される

## Execution Context

@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-stats/09-CONTEXT.md
@.planning/phases/09-stats/09-RESEARCH.md
@.planning/phases/09-stats/09-UI-SPEC.md
@src/components/stats/StatsCard.tsx

## Interfaces

From src/hooks/usePomodoro.ts:
```typescript
type Session = {
  id: string
  userId?: string
  todoId: string | null
  type: 'work' | 'short_break' | 'long_break'
  startedAt: string
  completedAt: string | null
  durationSecs: number
  createdAt: string
}

interface UsePomodoroReturn {
  sessions: Session[]
  loading: boolean
  error: string | null
  startSession: (type: SessionType, durationSecs: number, todoId?: string) => Promise<Session | null>
  completeSession: (sessionId: string) => Promise<void>
  fetchSessions: () => Promise<void>
}
```

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: タブ切り替えUIとToday統計を実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（既存の実装を確認するため）
    - .planning/phases/09-stats/09-UI-SPEC.md（タブパターンを確認するため）
  </read_first>
  <behavior>
    STAT-01: Todayタブがデフォルトで表示される
    STAT-01: 今日の集中時間とセッション数が正しく表示される
    タブ切り替え: Today/Week/Monthボタンが表示される
  </behavior>
  <action>
    StatsCard.tsxを拡張し、タブ切り替えUIとToday統計を表示する。

    実装内容：
    1. useStateでactiveTabを管理（デフォルト: 'today'）
    2. タブボタン配列を定義（Today, Week, Month）
    3. activeTabに応じて条件レンダリング
    4. 今日の統計表示をTodayStatsコンポーネントとして抽出

    タブ切り替えUI:
    ```tsx
    const [activeTab, setActiveTab] = useState<'today' | 'week' | 'month'>('today')

    const tabs = [
      { id: 'today' as const, label: 'Today' },
      { id: 'week' as const, label: 'Week' },
      { id: 'month' as const, label: 'Month' },
    ]

    return (
      <div className="glass rounded-3xl p-6 h-full flex flex-col overflow-y-auto min-h-0">
        <p className="text-xs uppercase tracking-widest text-cf-subtext font-bold mb-4">
          Stats
        </p>

        {/* タブ切り替え */}
        <div className="flex gap-2 mb-4">
          {tabs.map(tab => (
            <button
              key={tab.id}
              onClick={() => setActiveTab(tab.id)}
              className={`px-4 py-2 rounded-lg ${
                activeTab === tab.id ? 'bg-cf-primary text-white' : 'bg-white/10'
              }`}
            >
              {tab.label}
            </button>
          ))}
        </div>

        {/* タブコンテンツ */}
        {activeTab === 'today' && <TodayStats sessions={sessions} />}
        {/* 他のタブは後続のタスクで実装 */}
      </div>
    )
    ```

    今日の統計は既存の実装（Clockアイコン、Targetアイコン、集中時間、セッション数）を再利用する。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-01" --run</automated>
  </verify>
  <done>
    - StatsCard.tsxにactiveTab stateが定義されている
    - タブボタン（Today/Week/Month）が表示される
    - Todayタブで今日の統計（集中時間・セッション数）が表示される
    - src/components/stats/StatsCard.test.tsのSTAT-01テストがパスする
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Week統計と棒グラフを実装</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（Task 1の変更を確認するため）
    - .planning/phases/09-stats/09-RESEARCH.md（Rechartsパターンを確認するため）
  </read_first>
  <behavior>
    STAT-02: Weekタブで直近7日間の統計が表示される
    STAT-04: 日別セッション数の棒グラフが表示される
    棒グラフ: RechartsのBarChartを使用、色は#22c55e
  </behavior>
  <action>
    Weekタブの統計表示と棒グラフを実装する。

    実装内容：
    1. 直近7日間のセッションデータを集計
    2. RechartsのBarChartで日別セッション数を表示
    3. Tooltipで日付とセッション数を表示

    データ変換ロジック:
    ```tsx
    interface WeeklyData {
      date: string
      sessions: number
    }

    const weeklyData: WeeklyData[] = []
    for (let i = 6; i >= 0; i--) {
      const date = new Date()
      date.setDate(date.getDate() - i)
      const dateStr = date.toDateString()

      const daySessions = sessions.filter(
        s => s.type === 'work' && s.completedAt !== null && new Date(s.startedAt).toDateString() === dateStr
      ).length

      weeklyData.push({
        date: getDayLabel(dateStr), // 'Today' or 'Mon'/'Tue' etc.
        sessions: daySessions
      })
    }
    ```

    BarChart描画:
    ```tsx
    import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'

    <ResponsiveContainer width="100%" height={200}>
      <BarChart data={weeklyData}>
        <XAxis dataKey="date" />
        <YAxis />
        <Tooltip />
        <Bar dataKey="sessions" fill="#22c55e" name="セッション数" />
      </BarChart>
    </ResponsiveContainer>
    ```

    既存の自前divベース実装は削除し、Rechartsに置き換える。
  </action>
  <verify>
    <automated>npm test -- src/components/stats/StatsCard.test.ts -t "STAT-02" --run</automated>
  </verify>
  <done>
    - Weekタブで直近7日間の統計が表示される
    - BarChartで日別セッション数が表示される
    - 棒グラフの色が#22c55eである
    - src/components/stats/StatsCard.test.tsのSTAT-02、STAT-04テストがパスする
  </done>
</task>

## Verification

1. タブ切り替えが機能する
   - Today/Weekボタンをクリックすると、対応するコンテンツが表示される

2. 統計が正しく集計される
   - 今日の統計: 今日のセッションのみが集計される
   - 週次統計: 直近7日間のセッションが集計される

3. グラフが正しく表示される
   - npm run devでグラフが描画されることを目視確認

## Success Criteria

1. タブ切り替えUIが実装されている
2. Todayタブで今日の統計が表示される
3. Weekタブで週次統計と棒グラフが表示される
4. 棒グラフがRechartsを使用して実装されている

## Output

完了後、`.planning/phases/09-stats/09-02-SUMMARY.md`を作成する
