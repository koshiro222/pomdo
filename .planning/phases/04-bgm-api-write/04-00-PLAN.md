---
phase: 04-bgm-api-write
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/unit/bgm.test.ts, tests/unit/helpers/r2-mock.ts, tests/unit/helpers/trpc-context.ts]
autonomous: true
requirements: []
must_haves:
  truths:
    - "テストファイルが存在し、Vitest実行でエラーにならない"
    - "R2モックがBGM_BUCKETのput/delete操作をシミュレート可能"
    - "tRPCコンテキストヘルパーがadminユーザーと一般ユーザーの両方を生成可能"
  artifacts:
    - path: "tests/unit/bgm.test.ts"
      provides: "BGM mutationテストスタブ"
      min_lines: 20
    - path: "tests/unit/helpers/r2-mock.ts"
      provides: "R2バケットモック"
      exports: ["createMockR2Bucket"]
    - path: "tests/unit/helpers/trpc-context.ts"
      provides: "tRPCテスト用コンテキスト"
      exports: ["createMockContext", "createAdminContext"]
  key_links: []
---

<objective>
Wave 0: テストインフラ構築

BGM Write APIのテスト実行に必要なインフラを整える。R2モック、tRPCコンテキストヘルパー、テストスタブを作成し、各タスクの検証コマンドが実行可能な状態にする。

Purpose: Nyquistルール準拠のため、全タスクの<automated>検証コマンドが参照するファイルを先行して作成する
Output: テストファイル3つ、モックヘルパー2つ
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bgm-api-write/04-VALIDATION.md
@vitest.config.ts
@tests/bgm-router.test.ts
</context>

<interfaces>
<!-- R2モックインターフェース -->
From tests/unit/helpers/r2-mock.ts:
```typescript
export interface MockR2Bucket {
  put: (key: string, value: ArrayBuffer, options?: { httpMetadata?: { contentType: string } }) => Promise<void>
  delete: (key: string) => Promise<void>
  get: (key: string) => Promise<{ arrayBuffer?: () => Promise<ArrayBuffer> } | null>
}

export function createMockR2Bucket(): MockR2Bucket
```

<!-- tRPCコンテキストヘルパー -->
From tests/unit/helpers/trpc-context.ts:
```typescript
export function createMockContext(overrides?: {
  user?: { id: string; email: string; name: string; role: 'user' | 'admin' } | null
  db?: any
  schema?: any
}): {
  user: SessionUser | null
  db: any
  schema: any
}

export function createAdminContext(): {
  user: SessionUser & { role: 'admin' }
  db: any
  schema: any
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: R2バケットモックを作成</name>
  <files>tests/unit/helpers/r2-mock.ts</files>
  <read_first>
  - functions/api/[[route]].ts（BGM_BUCKETバインディング確認）
  - https://developers.cloudflare.com/r2/api/workers/workers-api-reference/（R2インターフェース）
  </read_first>
  <action>
R2バケットのモック実装を作成：

1. `tests/unit/helpers/`ディレクトリを作成（存在しない場合）
2. `r2-mock.ts`ファイルを作成し、以下を実装：
   - `MockR2Bucket`インターフェース定義（put, delete, getメソッド）
   - `createMockR2Bucket()`関数実装：
     - 内部Mapでファイルデータを保持
     - `put(key, value, options)`: Mapに保存、putされたキーを記録
     - `delete(key)`: Mapから削除、deleteされたキーを記録
     - `get(key)`: Mapから取得、存在しなければnull返却
     - `getPutKeys()`: putされたキーのリストを返すヘルパー（テスト検証用）
     - `getDeletedKeys()`: 削除されたキーのリストを返すヘルパー（テスト検証用）
   - JSDocで使用例を記載

実装例：
```typescript
export function createMockR2Bucket(): MockR2Bucket {
  const storage = new Map<string, ArrayBuffer>()
  const putKeys: string[] = []
  const deletedKeys: string[] = []

  return {
    put: async (key, value) => {
      storage.set(key, value)
      putKeys.push(key)
    },
    delete: async (key) => {
      storage.delete(key)
      deletedKeys.push(key)
    },
    get: async (key) => {
      const value = storage.get(key)
      if (!value) return null
      return { arrayBuffer: async () => value }
    },
    getPutKeys: () => [...putKeys],
    getDeletedKeys: () => [...deletedKeys],
  }
}
```

Edge Runtime環境のためArrayBufferを使用、Bufferは使用しないこと。
  </action>
  <verify>
    <automated>grep -q "export function createMockR2Bucket" tests/unit/helpers/r2-mock.ts && grep -q "put:" tests/unit/helpers/r2-mock.ts && grep -q "delete:" tests/unit/helpers/r2-mock.ts</automated>
  </verify>
  <done>
  - createMockR2Bucket関数がエクスポートされている
  - put/delete/getメソッドが実装されている
  - テスト検証用ヘルパー（getPutKeys/getDeletedKeys）が実装されている
  </done>
</task>

<task type="auto">
  <name>Task 2: tRPCコンテキストヘルパーを作成</name>
  <files>tests/unit/helpers/trpc-context.ts</files>
  <read_first>
  - src/app/routers/context.ts（SessionUser型、Contextインターフェース）
  - functions/lib/schema.ts（bgmTracksスキーマ）
  </read_first>
  <action>
tRPCテスト用のコンテキスト生成ヘルパーを実装：

1. `trpc-context.ts`ファイルを作成
2. 以下の関数を実装：

**createMockContext(overrides?)**:
- デフォルト値：ゲストユーザー（user: null）、モックDB、スキーマ
- overridesでuser/db/schemaを上書き可能
- 戻り値型はContextインターフェースに準拠

**createAdminContext()**:
- adminロールのユーザーを含むコンテキストを生成
- user: { id: "admin-id", email: "admin@example.com", name: "Admin User", role: "admin" }
- createMockContextを呼び出し、adminユーザーを渡す

**createUserContext()**:
- 一般ユーザーロールのコンテキストを生成
- user: { id: "user-id", email: "user@example.com", name: "Regular User", role: "user" }

モックDB・スキーマは、実際のDB操作を伴わない簡易モックを使用（{}を返すダミー関数等）。Phase 04ではR2操作が主な検証対象のため、DB操作の詳細なモックは不要。

実装例：
```typescript
import type { SessionUser } from '@/app/routers/context'

const mockSchema = {
  bgmTracks: {}, // ダミー
}

const mockDb = {
  select: () => mockDb,
  insert: () => mockDb,
  update: () => mockDb,
  delete: () => mockDb,
  from: () => mockDb,
  where: () => mockDb,
  values: () => mockDb,
  set: () => mockDb,
  returning: () => [],
  limit: () => mockDb,
}

export function createMockContext(overrides?: {
  user?: SessionUser | null
  db?: any
  schema?: any
}) {
  return {
    user: overrides?.user ?? null,
    db: overrides?.db ?? mockDb,
    schema: overrides?.schema ?? mockSchema,
  }
}

export function createAdminContext() {
  return createMockContext({
    user: {
      id: "admin-id",
      email: "admin@example.com",
      name: "Admin User",
      role: "admin",
    },
  })
}
```
  </action>
  <verify>
    <automated>grep -q "export function createMockContext" tests/unit/helpers/trpc-context.ts && grep -q "export function createAdminContext" tests/unit/helpers/trpc-context.ts && grep -q "role: 'admin'" tests/unit/helpers/trpc-context.ts</automated>
  </verify>
  <done>
  - createMockContext関数がエクスポートされている
  - createAdminContext関数がエクスポートされている
  - adminロールユーザーを含むコンテキストが生成可能
  </done>
</task>

<task type="auto">
  <name>Task 3: BGM mutationテストスタブを作成</name>
  <files>tests/unit/bgm.test.ts</files>
  <read_first>
  - tests/bgm-router.test.ts（既存のテスト構造）
  - src/app/routers/bgm.ts（bgmRouter構造）
  </read_first>
  <action>
BGM mutationのテストスタブファイルを作成：

1. `tests/unit/bgm.test.ts`ファイルを作成
2. 以下のテストケースを記述（describeブロックのみ、itブロックは空）：

```typescript
import { describe, it, expect } from 'vitest'
import { createMockR2Bucket } from './helpers/r2-mock'
import { createAdminContext, createUserContext } from './helpers/trpc-context'

describe('bgmRouter - create mutation', () => {
  it('管理者がトラックを追加できる', async () => {
    // TODO: implement
  })

  it('非管理者は403エラー', async () => {
    // TODO: implement
  })

  it('ファイルサイズ超過で400エラー', async () => {
    // TODO: implement
  })

  it('Base64デコードしてR2にアップロード', async () => {
    // TODO: implement
  })
})

describe('bgmRouter - update mutation', () => {
  it('管理者がトラックを更新できる', async () => {
    // TODO: implement
  })

  it('非管理者は403エラー', async () => {
    // TODO: implement
  })

  it('存在しないトラックは404エラー', async () => {
    // TODO: implement
  })

  it('部分フィールド更新が可能', async () => {
    // TODO: implement
  })
})

describe('bgmRouter - delete mutation', () => {
  it('管理者がトラックを削除できる', async () => {
    // TODO: implement
  })

  it('非管理者は403エラー', async () => {
    // TODO: implement
  })

  it('存在しないトラックは404エラー', async () => {
    // TODO: implement
  })

  it('DB削除後にR2からも削除', async () => {
    // TODO: implement
  })

  it('R2削除失敗時もDB削除は確定', async () => {
    // TODO: implement
  })
})
```

各テストケースのコメントに、期待する動作を日本語で簡潔に記述。
  </action>
  <verify>
    <automated>npm test -- --run bgm 2>&1 | grep -q "bgmRouter - create mutation" && npm test -- --run bgm 2>&1 | grep -q "bgmRouter - update mutation" && npm test -- --run bgm 2>&1 | grep -q "bgmRouter - delete mutation"</automated>
  </verify>
  <done>
  - tests/unit/bgm.test.tsが存在する
  - 3つのdescribeブロックが存在する（create, update, delete）
  - 各describeに少なくとも4つのitブロックが存在する
  - `npm test -- --run bgm`でテストが実行され、何らかの出力が表示される
  </done>
</task>

</tasks>

<verification>
Wave 0完了条件：
- [ ] tests/unit/helpers/r2-mock.tsが存在し、createMockR2Bucket関数をエクスポート
- [ ] tests/unit/helpers/trpc-context.tsが存在し、createMockContext/createAdminContext関数をエクスポート
- [ ] tests/unit/bgm.test.tsが存在し、describeブロックが定義されている
- [ ] `npm test -- --run bgm`でテストが実行可能
</verification>

<success_criteria>
- [ ] 全てのテストヘルパーファイルが作成されている
- [ ] R2モックがput/delete操作をシミュレート可能
- [ ] tRPCコンテキストヘルパーがadmin/userコンテキストを生成可能
- [ ] テストスタブが実行可能（エラーにならない）
</success_criteria>

<output>
After completion, create `.planning/phases/04-bgm-api-write/04-00-SUMMARY.md`
</output>
