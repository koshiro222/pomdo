---
phase: 12-physical-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/bgm/BgmPlayer.tsx
  - src/components/todos/TodoItem.tsx
  - src/components/bgm/TrackItem.tsx
autonomous: true
requirements:
  - TOUCH-01
  - TOUCH-02
user_setup: []

must_haves:
  truths:
    - "ユーザーは全ての対話要素を44px以上のタッチターゲットで操作できる"
    - "ユーザーは対話可能な要素にカーソルを合わせた際pointer cursorを確認できる"
  artifacts:
    - path: "src/index.css"
      provides: "グローバルcursor定義"
      contains: "button { cursor: pointer; }"
    - path: "src/components/bgm/BgmPlayer.tsx"
      provides: "次/前/プレイリストボタンのタッチターゲット"
      contains: "p-3"
    - path: "src/components/todos/TodoItem.tsx"
      provides: "削除ボタンのタッチターゲット"
      contains: "p-3"
    - path: "src/components/bgm/TrackItem.tsx"
      provides: "編集/削除ボタンのタッチターゲット"
      contains: "p-3"
  key_links:
    - from: "src/index.css"
      to: "全button要素"
      via: "@layer base"
      pattern: "button \\{ cursor: pointer"
---

<objective>
タッチターゲットサイズ確保とカーソルポインター統一による物理的インタラクション改善

Purpose: モバイルデバイスでのタッチ操作性を向上させ、全ての対話要素で一貫したカーソルフィードバックを提供する
Output: 44px以上のタッチターゲットとグローバルcursor定義
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-physical-interaction/12-CONTEXT.md
@.planning/phases/12-physical-interaction/12-RESEARCH.md
@.planning/phases/12-physical-interaction/12-UI-SPEC.md

<interfaces>
<!-- Phase 11で確立されたパターン -->

From src/index.css (Phase 11):
```css
@layer base {
  *:focus-visible {
    outline: 2px solid var(--df-accent-focus);
    outline-offset: 2px;
  }
}
```
Phase 11のfocus-visibleパターンと同様に、@layer base内にbuttonのグローバルスタイルを追加する。

From src/components/bgm/BgmPlayer.tsx:
```tsx
// 現在の小さなボタン（タッチターゲット不足）
<motion.button
  onClick={handlePrevTrack}
  className="text-cf-text hover:text-cf-primary transition-colors"
>
  <SkipBack className="w-6 h-6" />
</motion.button>
```

From src/components/todos/TodoItem.tsx:
```tsx
// 現在の削除ボタン（タッチターゲット不足）
<motion.button
  className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
  aria-label="削除"
>
  <Trash2 className="text-lg" />
</motion.button>
```

From src/components/bgm/TrackItem.tsx:
```tsx
// 現在の編集/削除ボタン（タッチターゲット不足）
<motion.button
  className="text-cf-subtext hover:text-cf-text"
  aria-label="編集"
>
  <Edit2 size={16} />
</motion.button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: グローバルcursor定義追加 (TOUCH-02)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css（現在の@layer base構造を確認）
  </read_first>
  <behavior>
    - @layer base内に`button { cursor: pointer; }`を追加
    - 既存のfocus-visibleスタイルの後に追加
    - ドラッグハンドルのcursor-grabはTailwindクラスで上書き可能（優先順位問題なし）
  </behavior>
  <action>
    src/index.cssの`@layer base`ブロック内に、`*:focus-visible`ルールの後に以下を追加:

    ```css
    /* Phase 12: グローバルcursor定義 */
    button {
      cursor: pointer;
    }

    /* ドラッグハンドルはTailwindクラスで上書き（cursor-grab active:cursor-grabbing） */
    ```

    追加位置は`*:focus-visible`ルールの後、`body`ルールの前とする。
  </action>
  <verify>
    <automated>grep -q "button {" src/index.css && grep -q "cursor: pointer" src/index.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.cssに`button {`が存在する
    - src/index.cssに`cursor: pointer`が存在する
    - @layer base内に追加されている
    - 既存のfocus-visibleスタイルが維持されている
  </acceptance_criteria>
  <done>
    - src/index.cssの@layer base内に`button { cursor: pointer; }`が存在する
    - 全てのbutton要素にpointerカーソルが適用される
  </done>
</task>

<task type="auto">
  <name>Task 2: BgmPlayerタッチターゲット拡大 (TOUCH-01)</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx（現在のボタン構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - 次/前ボタン（SkipBack/SkipForward）にp-3追加
    - プレイリストボタン（List）にp-3追加
    - ホバー時に視覚的フィードバック（hover:bg-white/10）を追加
    - 再生ボタン（w-14 h-14 = 56px）は既に44px以上なので変更不要
    - 音量スライダーのつまみは除外（特別なUIパターン）
  </behavior>
  <action>
    src/components/bgm/BgmPlayer.tsxの以下のボタンを修正:

    1. **プレイリストボタン（List）** - 行81-91:
       ```tsx
       // Before
       <motion.button
         {...tapAnimation}
         onClick={() => setIsExpanded(!isExpanded)}
         className="text-cf-subtext hover:text-cf-primary cursor-pointer transition-colors"
         ...
       >
         <List className="w-5 h-5" />
       </motion.button>

       // After
       <motion.button
         {...tapAnimation}
         onClick={() => setIsExpanded(!isExpanded)}
         className="p-3 text-cf-subtext hover:text-cf-primary hover:bg-white/10 transition-colors"
         ...
       >
         <List className="w-5 h-5" />
       </motion.button>
       ```

    2. **前ボタン（SkipBack）** - 行117-126:
       ```tsx
       // Before
       <motion.button
         ...hoverAnimation}
         {...tapAnimation}
         onClick={handlePrevTrack}
         disabled={hasError}
         className="text-cf-text hover:text-cf-primary transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
         ...
       >
         <SkipBack className="w-6 h-6" />
       </motion.button>

       // After
       <motion.button
         {...hoverAnimation}
         {...tapAnimation}
         onClick={handlePrevTrack}
         disabled={hasError}
         className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
         ...
       >
         <SkipBack className="w-6 h-6" />
       </motion.button>
       ```

    3. **次ボタン（SkipForward）** - 行139-148:
       ```tsx
       // Before
       <motion.button
         {...hoverAnimation}
         {...tapAnimation}
         onClick={handleNextTrack}
         disabled={hasError}
         className="text-cf-text hover:text-cf-primary transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
         ...
       >
         <SkipForward className="w-6 h-6" />
       </motion.button>

       // After
       <motion.button
         {...hoverAnimation}
         {...tapAnimation}
         onClick={handleNextTrack}
         disabled={hasError}
         className="p-3 text-cf-text hover:text-cf-primary hover:bg-white/10 transition-colors disabled:text-cf-subtext disabled:cursor-not-allowed"
         ...
       >
         <SkipForward className="w-6 h-6" />
       </motion.button>
       ```

    **注意**: 再生ボタン（Play/Pause）は`w-14 h-14`（56px）で既に要件を満たしているため変更不要。
    音量スライダーのつまみ（16px）は特別なUIパターンとして除外。
  </action>
  <verify>
    <automated>grep -c "p-3" src/components/bgm/BgmPlayer.tsx | xargs -I{} sh -c 'if [ {} -ge 3 ]; then echo "PASS"; else echo "FAIL: expected 3+ p-3 occurrences, got {}"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsxに`p-3`が3回以上出現する
    - プレイリストボタンのclassNameに`p-3`が含まれる
    - 前ボタンのclassNameに`p-3`が含まれる
    - 次ボタンのclassNameに`p-3`が含まれる
    - 全ての対象ボタンに`hover:bg-white/10`が含まれる
  </acceptance_criteria>
  <done>
    - BgmPlayerの次/前/プレイリストボタンにp-3が追加されている
    - ホバー時にbg-white/10の背景色フィードバックが表示される
    - 全ての小さなボタンが44px以上のタッチターゲットを持つ
  </done>
</task>

<task type="auto">
  <name>Task 3: TodoItem削除ボタンタッチターゲット拡大 (TOUCH-01)</name>
  <files>src/components/todos/TodoItem.tsx</files>
  <read_first>
    - src/components/todos/TodoItem.tsx（現在の削除ボタン構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - 削除ボタン（Trash2）にp-3追加
    - ホバー時に視覚的フィードバック（hover:bg-white/10）を追加
    - ドラッグハンドル（GripVertical）はPhase 11で常時表示済み、cursor-grab維持
  </behavior>
  <action>
    src/components/todos/TodoItem.tsxの削除ボタンを修正（行95-105）:

    ```tsx
    // Before
    <motion.button
      {...tapAnimation}
      onClick={(e) => {
        e.stopPropagation()
        onDelete(id)
      }}
      className="opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger"
      aria-label="削除"
    >
      <Trash2 className="text-lg" />
    </motion.button>

    // After
    <motion.button
      {...tapAnimation}
      onClick={(e) => {
        e.stopPropagation()
        onDelete(id)
      }}
      className="p-3 opacity-0 group-hover:opacity-100 transition-opacity text-cf-subtext hover:text-cf-danger hover:bg-white/10"
      aria-label="削除"
    >
      <Trash2 className="text-lg" />
    </motion.button>
    ```

    **注意**: ドラッグハンドル（GripVertical）は`cursor-grab active:cursor-grabbing`を維持し、グローバルcursor定義を上書き。
  </action>
  <verify>
    <automated>grep -q "p-3.*opacity-0.*group-hover:opacity-100" src/components/todos/TodoItem.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/todos/TodoItem.tsxの削除ボタンclassNameに`p-3`が含まれる
    - 削除ボタンclassNameに`hover:bg-white/10`が含まれる
    - `aria-label="削除"`が維持されている
  </acceptance_criteria>
  <done>
    - TodoItemの削除ボタンにp-3が追加されている
    - ホバー時にbg-white/10の背景色フィードバックが表示される
    - 削除ボタンが44px以上のタッチターゲットを持つ
  </done>
</task>

<task type="auto">
  <name>Task 4: TrackItem編集/削除ボタンタッチターゲット拡大 (TOUCH-01)</name>
  <files>src/components/bgm/TrackItem.tsx</files>
  <read_first>
    - src/components/bgm/TrackItem.tsx（現在の編集/削除ボタン構造を確認）
    - .planning/phases/12-physical-interaction/12-UI-SPEC.md（実装パターン確認）
  </read_first>
  <behavior>
    - 編集ボタン（Edit2）にp-3追加
    - 削除ボタン（Trash2）にp-3追加
    - ホバー時に視覚的フィードバック（hover:bg-white/10）を追加
  </behavior>
  <action>
    src/components/bgm/TrackItem.tsxのアクションボタンを修正（行51-66）:

    ```tsx
    // Before
    <div
      data-testid="action-buttons"
      className="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity"
    >
      <motion.button
        {...tapAnimation}
        onClick={() => setShowEditDialog(true)}
        className="text-cf-subtext hover:text-cf-text"
        aria-label="編集"
      >
        <Edit2 size={16} />
      </motion.button>
      <motion.button
        {...tapAnimation}
        onClick={() => setShowDeleteConfirm(true)}
        className="text-cf-subtext hover:text-cf-danger"
        aria-label="削除"
      >
        <Trash2 size={16} />
      </motion.button>
    </div>

    // After
    <div
      data-testid="action-buttons"
      className="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity"
    >
      <motion.button
        {...tapAnimation}
        onClick={() => setShowEditDialog(true)}
        className="p-3 text-cf-subtext hover:text-cf-text hover:bg-white/10 transition-colors"
        aria-label="編集"
      >
        <Edit2 size={16} />
      </motion.button>
      <motion.button
        {...tapAnimation}
        onClick={() => setShowDeleteConfirm(true)}
        className="p-3 text-cf-subtext hover:text-cf-danger hover:bg-white/10 transition-colors"
        aria-label="削除"
      >
        <Trash2 size={16} />
      </motion.button>
    </div>
    ```
  </action>
  <verify>
    <automated>grep -c "p-3" src/components/bgm/TrackItem.tsx | xargs -I{} sh -c 'if [ {} -ge 2 ]; then echo "PASS"; else echo "FAIL: expected 2+ p-3 occurrences, got {}"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - src/components/bgm/TrackItem.tsxに`p-3`が2回以上出現する
    - 編集ボタンのclassNameに`p-3`が含まれる
    - 削除ボタンのclassNameに`p-3`が含まれる
    - 両ボタンに`hover:bg-white/10`が含まれる
    - `aria-label="編集"`と`aria-label="削除"`が維持されている
  </acceptance_criteria>
  <done>
    - TrackItemの編集/削除ボタンにp-3が追加されている
    - ホバー時にbg-white/10の背景色フィードバックが表示される
    - 編集/削除ボタンが44px以上のタッチターゲットを持つ
  </done>
</task>

</tasks>

<verification>
## 自動検証

1. **グローバルcursor定義 (TOUCH-02)**
   - src/index.cssに`button { cursor: pointer; }`が存在すること

2. **タッチターゲット (TOUCH-01)**
   - BgmPlayer: 3箇所のp-3追加（次/前/プレイリスト）
   - TodoItem: 1箇所のp-3追加（削除）
   - TrackItem: 2箇所のp-3追加（編集/削除）

## 手動検証（Phase 12-02で実施）

- npm run dev起動後、ブラウザで各ボタンをホバーしてカーソルがpointerになることを確認
- DevToolsでボタンのcomputed sizeが44px以上であることを確認
</verification>

<success_criteria>
- src/index.cssの@layer baseに`button { cursor: pointer; }`が追加されている
- BgmPlayerの次/前/プレイリストボタンにp-3が追加されている
- TodoItemの削除ボタンにp-3が追加されている
- TrackItemの編集/削除ボタンにp-3が追加されている
- 全ての小さなアイコンボタンが44px以上のタッチターゲットを持つ
- npm run buildが成功する
</success_criteria>

<output>
After completion, create `.planning/phases/12-physical-interaction/12-01-SUMMARY.md`
</output>
