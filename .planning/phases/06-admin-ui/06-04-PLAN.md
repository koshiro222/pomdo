---
phase: 06-admin-ui
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified: ["src/components/bgm/TrackItem.tsx"]
autonomous: true
requirements: ["UI-05", "UI-06"]
gap_closure: true

must_haves:
  truths:
    - "編集ボタンクリックでEditTrackDialogが表示される"
    - "削除ボタンクリックでDeleteConfirmDialogが表示される"
    - "ダイアログで操作完了後、TrackListが更新される"
  artifacts:
    - path: "src/components/bgm/TrackItem.tsx"
      provides: "TrackItemコンポーネント（ダイアログ連携完了）"
      min_lines: 90
    - path: "src/components/bgm/EditTrackDialog.tsx"
      provides: "編集ダイアログ（既存）"
      exports: ["EditTrackDialog"]
    - path: "src/components/bgm/DeleteConfirmDialog.tsx"
      provides: "削除確認ダイアログ（既存）"
      exports: ["DeleteConfirmDialog"]
  key_links:
    - from: "src/components/bgm/TrackItem.tsx"
      to: "src/components/bgm/EditTrackDialog.tsx"
      via: "conditional rendering in AnimatePresence"
      pattern: "{showEditDialog && <EditTrackDialog"
    - from: "src/components/bgm/TrackItem.tsx"
      to: "src/components/bgm/DeleteConfirmDialog.tsx"
      via: "conditional rendering in AnimatePresence"
      pattern: "{showDeleteConfirm && <DeleteConfirmDialog"
---

<objective>
TrackItem.tsxのプレースホルダーを実際のダイアログコンポーネントに置き換え、編集・削除機能を完全に接続する

Purpose: 77-83行目に残っているプレースホルダーを削除し、既に実装済みのEditTrackDialogとDeleteConfirmDialogを接続して、トラック編集・削除UIを完成させる
Output: 機能する編集・削除ダイアログが接続されたTrackItemコンポーネント
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-ui/06-CONTEXT.md
@.planning/phases/06-admin-ui/06-VERIFICATION.md
@src/components/bgm/EditTrackDialog.tsx
@src/components/bgm/DeleteConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrackItemにダイアログコンポーネントを接続</name>
  <files>src/components/bgm/TrackItem.tsx</files>
  <read_first>
    - src/components/bgm/TrackItem.tsx（修正対象ファイル）
    - src/components/bgm/EditTrackDialog.tsx（インポート先、関数シグネチャ確認）
    - src/components/bgm/DeleteConfirmDialog.tsx（インポート先、関数シグネチャ確認）
  </read_first>
  <action>
    既に実装済みのEditTrackDialogとDeleteConfirmDialogをTrackItem.tsxに接続します：

    1. インポート追加（ファイル先頭）：
       ```typescript
       import { AnimatePresence } from 'framer-motion'
       import { EditTrackDialog } from './EditTrackDialog'
       import { DeleteConfirmDialog } from './DeleteConfirmDialog'
       ```

    2. 77-83行目のプレースホルダーを削除し、以下に置き換え：
       ```typescript
       {/* ダイアログ */}
       <AnimatePresence>
         {showEditDialog && (
           <EditTrackDialog
             track={track}
             onClose={() => setShowEditDialog(false)}
           />
         )}
         {showDeleteConfirm && (
           <DeleteConfirmDialog
             track={track}
             onClose={() => setShowDeleteConfirm(false)}
           />
         )}
       </AnimatePresence>
       ```

    注意点：
    - EditTrackDialogは既にBgmTrack型を期待しているが、TrackItemのTrack型と互換性がある
    - DeleteConfirmDialogも同様
    - AnimatePresenceで囲むことで、ダイアログのアニメーションが正しく機能する
  </action>
  <verify>
    <automated>grep -n "EditTrackDialog\|DeleteConfirmDialog" src/components/bgm/TrackItem.tsx</automated>
  </verify>
  <done>
    - インポート文が存在する（EditTrackDialog, DeleteConfirmDialog, AnimatePresence）
    - 77-83行目のプレースホルダーが削除されている
    - showEditDialogがtrueのときEditTrackDialogがレンダリングされる
    - showDeleteConfirmがtrueのときDeleteConfirmDialogがレンダリングされる
    - 両方のダイアログがtrackとonClose propsを受け取る
  </done>
</task>

</tasks>

<verification>
1. 編集ボタン（Edit2アイコン）をクリックするとEditTrackDialogが表示される
2. 削除ボタン（Trash2アイコン）をクリックするとDeleteConfirmDialogが表示される
3. ダイアログ内での操作（更新/削除）が完了すると、ダイアログが閉じる
4. 操作後、TrackListが更新される（tRPC invalidationによる）
</verification>

<success_criteria>
- TrackItem.tsxからプレースホルダーが完全に削除されている
- EditTrackDialogとDeleteConfirmDialogが正しくインポートされ、条件レンダリングされている
- 編集・削除機能がエンドツーエンドで動作する
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-ui/06-04-SUMMARY.md`
</output>
