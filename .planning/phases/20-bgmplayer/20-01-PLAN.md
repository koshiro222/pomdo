---
phase: 20-bgmplayer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/bgm/BgmPlayer.tsx
autonomous: false
requirements:
  - BGMAN-03
  - BGMAN-04
user_setup: []

must_haves:
  truths:
    - "アルバムアートはBGM再生中に点滅+パルスアニメーションを表示する"
    - "BGM停止時、アルバムアートは静止した状態で表示される"
    - "回転アニメーション（album-art-spinningクラス）がコードベースから完全に削除されている"
  artifacts:
    - path: "src/components/bgm/BgmPlayer.tsx"
      provides: "AlbumArtコンポーネント（アニメーション適用済み）"
      exports: ["AlbumArt", "BgmPlayer"]
      min_lines: 50
  key_links:
    - from: "AlbumArt背景div"
      to: ".album-art-blinking/.album-art-pulsing"
      via: "isPlaying条件付きclassName"
      pattern: '\$\{isPlaying \? .album-art-blinking album-art-pulsing. : .\}'
    - from: "AlbumArt背景div"
      to: "colorプロパティ"
      via: "style属性のcurrentColor用"
      pattern: "color: color"
---

<objective>
AlbumArtコンポーネントを修正し、再生中のみ点滅+パルスアニメーションを表示し、回転アニメーションを完全に削除する

目的: 回転する円盤から「鼓動」する表現への刷新を実現
出力: 修正されたAlbumArtコンポーネント、削除されたalbum-art-spinningクラス
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-bgmplayer/20-CONTEXT.md
@src/index.css
@src/components/bgm/BgmPlayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlbumArt背景divに新しいアニメーションクラスを適用</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx（現在のAlbumArt構造を確認）
    - src/index.css（.album-art-blinkingと.album-art-pulsingクラス定義を確認）
    - .planning/phases/20-bgmplayer/20-CONTEXT.md（実装決定事項を確認）
  </read_first>
  <action>
AlbumArtコンポーネントの背景div（17行目）を修正：

現在の実装:
```tsx
className={`w-full h-full rounded-2xl flex items-center justify-center ${isPlaying ? 'album-art-spinning' : ''}`}
style={{
  background: `linear-gradient(135deg, ${color}40, ${color}20)`,
  boxShadow: `0 8px 24px ${color}40`,
}}
```

以下のように変更:
```tsx
className={`w-full h-full rounded-2xl flex items-center justify-center ${isPlaying ? 'album-art-blinking album-art-pulsing' : ''}`}
style={{
  background: `linear-gradient(135deg, ${color}40, ${color}20)`,
  color: color, // pulse-glowのcurrentColor用に追加
  boxShadow: `0 8px 24px ${color}40`,
}}
```

変更点:
1. `album-art-spinning`を`album-art-blinking album-art-pulsing`に置換
2. `color: color`をstyleに追加（pulse-glowのcurrentColor継承用）

注意:
- 内側サークルdiv（25行目）の`album-art-spinning`はTask 2で削除
- `animationDuration: '12s'`プロパティはTask 2で削除
  </action>
  <verify>
grep -n "album-art-blinking album-art-pulsing" src/components/bgm/BgmPlayer.tsx | grep -q "w-full h-full rounded-2xl" && echo "OK" || echo "FAIL"
  </verify>
  <done>
背景divにisPlaying=trueの時に`album-art-blinking`と`album-art-pulsing`クラスが適用され、colorプロパティが設定されている
  </done>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsx 17行目に`album-art-blinking album-art-pulsing`が含まれる
    - src/components/bgm/BgmPlayer.tsx 17行目のstyleに`color: color`が含まれる
    - 条件式が`${isPlaying ? 'album-art-blinking album-art-pulsing' : ''}`の形式
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: album-art-spinningクラスの完全削除</name>
  <files>src/components/bgm/BgmPlayer.tsx</files>
  <read_first>
    - src/components/bgm/BgmPlayer.tsx（album-art-spinningの使用箇所を確認）
    - .planning/phases/20-bgmplayer/20-CONTEXT.md（削除対象の確認）
  </read_first>
  <action>
AlbumArtコンポーネントからalbum-art-spinningクラスを完全に削除:

1. 内側サークルdiv（25行目）の修正:
現在:
```tsx
className={`w-20 h-20 rounded-full flex items-center justify-center ${isPlaying ? 'album-art-spinning' : ''}`}
style={{
  background: `linear-gradient(135deg, ${color}60, ${color}30)`,
  animationDuration: '12s',
}}
```

変更後:
```tsx
className="w-20 h-20 rounded-full flex items-center justify-center"
style={{
  background: `linear-gradient(135deg, ${color}60, ${color}30)`,
}}
```

2. 削除内容:
   - `${isPlaying ? 'album-art-spinning' : ''}`部分を削除（classNameは静的な文字列に）
   - `animationDuration: '12s'`プロパティを削除

3. 全コードベースでの削除確認:
```bash
grep -r "album-art-spinning" src/
```
結果は何も返ってはならない（src/ディレクトリ内から完全削除）

注意:
- 内側サークルdiv自体は削除せず、アニメーションのみ削除
- Musicアイコンの`album-art-paused`クラス（31行目）は維持（停止時のスタティック表示用）
  </action>
  <verify>
! grep -r "album-art-spinning" src/ && echo "OK: 完全削除" || echo "FAIL: まだ残っている"
  </verify>
  <done>
コードベース全体から`album-art-spinning`クラスが完全に削除され、内側サークルdivはアニメーションなしの静止状態で表示されている
  </done>
  <acceptance_criteria>
    - src/components/bgm/BgmPlayer.tsx 25行目に`album-art-spinning`が含まれない
    - src/components/bgm/BgmPlayer.tsx 25行目に`animationDuration: '12s'`が含まれない
    - `grep -r "album-art-spinning" src/`の結果が空（0行）
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
AlbumArtコンポーネントのアニメーション刷新実装:
- 背景divにblink + pulse-glowアニメーションを条件付き適用
- album-art-spinningクラスの完全削除（2箇所）
- 内側サークルdivのアニメーション削除
  </what-built>
  <how-to-verify>
1. 開発サーバーが起動していることを確認: `npm run dev`
2. ブラウザで http://localhost:5173 にアクセス
3. BGMカードのアルバムアートを確認:
   - 停止状態: 完全に静止していること（回転なし、点滅なし）
   - 再生ボタンをクリック
   - 再生中: アルバムアートが点滅（opacity変化）しながらパルス（glowエフェクト）していること
   - 回転していないことを確認（円盤回転アニメーションが消えていること）
4. もう一度再生ボタンをクリックして停止
5. 停止状態: 即座に静止に戻ることを確認
6. 開発者ツールで要素を検証:
   - 背景divのclassListに`album-art-blinking`と`album-art-pulsing`が再生中のみ含まれていること
   - `album-art-spinning`がどの要素にも含まれていないこと
  </how-to-verify>
  <resume-signal>
Type "approved" または修正内容を説明
  </resume-signal>
</task>

</tasks>

<verification>
## 全体検証

1. **要件カバレッジ:**
   - [ ] BGMAN-03: 再生中のみアニメーションが動作する（isPlaying条件付きクラス適用）
   - [ ] BGMAN-03: 停止時は静止する（isPlaying=false時にアニメーションクラス不適用）
   - [ ] BGMAN-04: album-art-spinningがコードベースから完全削除されている

2. **CSS定義確認:**
   ```bash
   grep -A 5 "@keyframes blink" src/index.css | grep -E "(opacity|scale)"
   grep -A 8 "@keyframes pulse-glow" src/index.css | grep "box-shadow"
   ```

3. **コンポーネント実装確認:**
   ```bash
   grep "album-art-blinking\|album-art-pulsing" src/components/bgm/BgmPlayer.tsx
   grep "album-art-spinning" src/components/bgm/BgmPlayer.tsx || echo "完全削除OK"
   ```

4. **動作確認:**
   - 再生中: 点滅（opacity 0.6↔1.0）+ パルス（glow広がる）
   - 停止時: 完全静止
   - 回転なし
</verification>

<success_criteria>
## 成功基準

1. **機能要件:**
   - アルバムアートはBGM再生中に点滅+パルスアニメーションを表示する
   - BGM停止時、アルバムアートは静止した状態で表示される
   - 回転アニメーション（album-art-spinningクラス）がコードベースから完全に削除されている

2. **技術要件:**
   - Phase 19で定義されたCSS keyframes（blink, pulse-glow）が使用されている
   - colorプロパティが設定され、currentColor継承が機能している
   - isPlayingプロパティによる条件付きアニメーション制御が実装されている

3. **品質要件:**
   - アニメーション期間は2秒（光感受性エピレプシー対策）
   - GPUアクセラレーション有効（transform + opacityのみ使用）
   - 再生/停止の切り替えが即座に反映される
</success_criteria>

<output>
完了後、`.planning/phases/20-bgmplayer/20-01-SUMMARY.md`を作成
</output>
