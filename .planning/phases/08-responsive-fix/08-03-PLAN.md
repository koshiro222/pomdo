---
phase: 08-responsive-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/components/todos/TodoList.tsx
  - src/components/stats/StatsCard.tsx
autonomous: true
requirements:
  - RESP-02
must_haves:
  truths:
    - "ユーザーは一貫したスクロール挙動を体験する（ブレイクポイントで変わらない）"
    - "TodoリストとStatsカードの内容が多い場合、カード内でスクロールできる"
  artifacts:
    - path: "src/components/todos/TodoList.tsx"
      provides: "Todoリスト表示"
      contains: "overflow-y-auto min-h-0"
    - path: "src/components/stats/StatsCard.tsx"
      provides: "統計カード表示"
      contains: "overflow-y-auto min-h-0"
  key_links:
    - from: "TodoList内部コンテナ"
      to: "親カード"
      via: "flex-1 overflow"
      pattern: "overflow-y-auto min-h-0"
    - from: "StatsCard内部コンテナ"
      to: "親カード"
      via: "flex-1 overflow"
      pattern: "overflow-y-auto min-h-0"
---

<objective>
TodoListとStatsCardにoverflow設定を追加し、一貫したスクロール挙動を実現する。

Purpose: main要素からsm:overflow-hiddenを削除した後、各カード内でコンテンツが多い場合にスクロールできるようにする。min-h-0はFlexbox/Grid内でoverflowを有効にするために必須。
Output: 修正されたTodoList.tsx、StatsCard.tsx（overflow-y-auto min-h-0追加）
</objective>

<execution_context>
@/Users/koshiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koshiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-responsive-fix/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/todos/TodoList.tsx:
```typescript
// Current line 91:
<div className="flex-1 overflow-y-auto p-4 flex flex-col gap-3">

// Target:
<div className="flex-1 overflow-y-auto min-h-0 p-4 flex flex-col gap-3">
```

From src/components/stats/StatsCard.tsx:
```typescript
// Current line 65:
<div className="glass rounded-3xl p-6 h-full flex flex-col">

// Target:
<div className="glass rounded-3xl p-6 h-full flex flex-col overflow-y-auto min-h-0">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodoList.tsxにmin-h-0を追加</name>
  <files>src/components/todos/TodoList.tsx</files>
  <read_first>
    - src/components/todos/TodoList.tsx（Todoリストコンテナのクラスを確認）
  </read_first>
  <action>
    src/components/todos/TodoList.tsxのTodoリストコンテナ（line 91付近）にmin-h-0を追加する:

    変更前: `<div className="flex-1 overflow-y-auto p-4 flex flex-col gap-3">`
    変更後: `<div className="flex-1 overflow-y-auto min-h-0 p-4 flex flex-col gap-3">`

    ※ 既にoverflow-y-autoが存在するため、min-h-0のみ追加する。
    ※ min-h-0はFlexbox内でoverflowを有効にするための重要なテクニック（Flexboxのデフォルトmin-height: autoを上書き）。
  </action>
  <verify>
    <automated>grep -n 'overflow-y-auto min-h-0' src/components/todos/TodoList.tsx | head -1 | grep -q 'overflow-y-auto min-h-0' && echo "PASS: overflow-y-auto min-h-0 found in TodoList" || echo "FAIL: not found"</automated>
  </verify>
  <done>
    - TodoList.tsxのTodoリストコンテナにmin-h-0が追加されている
  </done>
  <acceptance_criteria>
    - `grep 'overflow-y-auto min-h-0' src/components/todos/TodoList.tsx` が成功する
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: StatsCard.tsxにoverflow-y-auto min-h-0を追加</name>
  <files>src/components/stats/StatsCard.tsx</files>
  <read_first>
    - src/components/stats/StatsCard.tsx（StatsCardコンテナのクラスを確認）
  </read_first>
  <action>
    src/components/stats/StatsCard.tsxのルートコンテナ（line 65付近）にoverflow-y-auto min-h-0を追加する:

    変更前: `<div className="glass rounded-3xl p-6 h-full flex flex-col">`
    変更後: `<div className="glass rounded-3xl p-6 h-full flex flex-col overflow-y-auto min-h-0">`

    これにより:
    - StatsCardの内容が多い場合、カード内でスクロール可能になる
    - min-h-0でFlexbox内のoverflowを有効化
  </action>
  <verify>
    <automated>grep -n 'overflow-y-auto min-h-0' src/components/stats/StatsCard.tsx | head -1 | grep -q 'overflow-y-auto min-h-0' && echo "PASS: overflow-y-auto min-h-0 found in StatsCard" || echo "FAIL: not found"</automated>
  </verify>
  <done>
    - StatsCard.tsxのルートコンテナにoverflow-y-auto min-h-0が追加されている
  </done>
  <acceptance_criteria>
    - `grep 'overflow-y-auto min-h-0' src/components/stats/StatsCard.tsx` が成功する
  </acceptance_criteria>
</task>

</tasks>

<verification>
## 検証手順

1. **型チェック**: `npm run build` が成功すること
2. **TodoList確認**: Todoリストコンテナにoverflow-y-auto min-h-0が存在すること
3. **StatsCard確認**: StatsCardルートコンテナにoverflow-y-auto min-h-0が存在すること
</verification>

<success_criteria>
- `npm run build` がエラーなく完了する
- TodoList.tsxに`overflow-y-auto min-h-0`が存在する
- StatsCard.tsxに`overflow-y-auto min-h-0`が存在する
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-fix/08-03-SUMMARY.md`
</output>
